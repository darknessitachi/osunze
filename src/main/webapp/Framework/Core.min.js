(function () {
	var b = window.Zving,
	a = Object.prototype.toString;
	b.Constant = {};
	b.toArray = function (j, k, c) {
		if (!j || !j.length) {
			return []
		}
		if (typeof j.length == "number") {
			var h = [];
			var f = 0,
			e = j.length;
			for (; f < e; f++) {
				h.push(j[f])
			}
			return h.slice(k || 0, c || h.length)
		}
	};
	b.isBoolean = function (c) {
		return typeof c === "boolean"
	};
	b.isNumber = function (c) {
		return typeof c === "number" && isFinite(c)
	};
	b.isInt = function (c) {
		return b.isNumber(c) && (/^\-?\d+$/).test("" + c)
	};
	b.isString = function (c) {
		return typeof c === "string"
	};
	b.isEmpty = function (e, c) {
		return e === null || e === undefined || (a.apply(e) === "[object Array]" && !e.length) || (!c ? e === "" : false)
	};
	b.isNull = function (c) {
		return c === null || typeof c === "undefined"
	};
	b.isNotNull = function (c) {
		return !isNull(c)
	};
	b.isObject = function (c) {
		return !!c && typeof c === "object"
	};
	b.isPlainObject = function (c) {
		return !!c && c.constructor === Object
	};
	b.isFunction = function (c) {
		return !!c && a.apply(c) === "[object Function]"
	};
	b.isArray = function (c) {
		return !!c && a.apply(c) === "[object Array]"
	};
	b.isArrayLike = function (c) {
		if (!c || c.document || c.nodeType || b.isFunction(c)) {
			return false
		}
		return typeof c === "object" && typeof c.length === "number"
	};
	b.isElement = function (c) {
		return !!c && c.nodeName && c.nodeType === 1
	};
	b.isDocument = function (c) {
		return c.URL && !!c.readyState
	};
	b.isWindow = function (c) {
		return c.document && c.navigator && !!c.location
	};
	b.isDate = b.isDateTime = function (c) {
		return !!c && a.apply(c) === "[object Date]"
	};
	b.isPrimitive = function (c) {
		return b.isString(c) || b.isNumber(c) || b.isBoolean(c)
	};
	b.isIterable = function (c) {
		if (b.isArray(c) || c.callee) {
			return true
		}
		if (/NodeList|HTMLCollection/.test(a.call(c))) {
			return true
		}
		return ((typeof c.nextNode != "undefined" || c.item) && b.isNumber(c.length))
	};
	b.each = function (j, h, f) {
		if (b.isEmpty(j, true)) {
			return
		}
		if (!b.isIterable(j) || b.isPrimitive(j)) {
			j = [j]
		}
		for (var e = 0, c = j.length; e < c; e++) {
			if (h.call(f || j[e], j[e], e, j) === false) {
				return e
			}
		}
	};
	b.iterate = function (f, e, c) {
		if (b.isEmpty(f)) {
			return
		}
		if (b.isIterable(f)) {
			b.each(f, e, c);
			return
		} else {
			if (b.isObject(f)) {
				for (var h in f) {
					if (f.hasOwnProperty(h)) {
						if (e.call(c || f, h, f[h], f) === false) {
							return
						}
					}
				}
			}
		}
	};
	b.namespace = function () {
		var f = arguments,
		c = f.length,
		n = null,
		h,
		e,
		m,
		k = (f[c - 1] === true && c--);
		for (h = 0; h < c; h++) {
			m = ("" + f[h]).split(".");
			n = k ? Zving : this;
			for (e = (Zving[m[0]] === n) ? 1 : 0; e < m.length; ++e) {
				n = n[m[e]] = n[m[e]] || {}

			}
		}
		return n
	};
	b.noopFn = function () {}

})();
Zving.Function = {};
Zving.Function.bind = function (b, c, a) {
	a = Zving.isArray(a) ? a : Array.prototype.slice.call(arguments, 2);
	return function () {
		return b.apply(c, a.concat(Array.prototype.slice.call(arguments)))
	}
};
Zving.Function.bindEvent = Zving.Function.bindAsEventListener = function (b, c, a) {
	a = Zving.isArray(a) ? a : Array.prototype.slice.call(arguments, 2);
	return function (e) {
		return b.apply(c, [e || window.event].concat(a))
	}
};
Zving.Function.delay = function (e, b, f, a) {
	a = Zving.isArray(a) ? a : Array.prototype.slice.call(arguments, 3);
	var c = Zving.Function.bind(e, f, a);
	if (b > 0) {
		return setTimeout(c, b)
	}
	c();
	return 0
};
Zving.Function.methodize = function (b, a) {
	if (a) {
		return function () {
			return b.apply(null, [this[a]].concat(Array.prototype.slice.call(arguments)))
		}
	}
	return function () {
		return b.apply(null, [this].concat(Array.prototype.slice.call(arguments)))
	}
};
Zving.Function.rwrap = function (b, c, a) {
	a |= 0;
	return function () {
		var e = b.apply(this, arguments);
		if (a >= 0) {
			e = arguments[a]
		}
		return c ? new c(e) : e
	}
};
Zving.Function.createTargeted = function (c, a, b) {
	return function () {
		if (a.target === arguments[0]) {
			c.apply(b, Array.prototype.slice.call(arguments))
		}
	}
};
Zving.Function.createBuffered = function (c, a, e, b) {
	e.task = new Zving.Util.DelayedTask();
	return function () {
		e.task.delay(Zving.isObject(a) ? a.buffer : a, c, b, Array.prototype.slice.call(arguments))
	}
};
Zving.Function.createSingle = function (f, e, a, c, b) {
	return function (h) {
		Zving.EventManager.removeListener(e, a, c, b);
		return f.apply(b || e, arguments)
	}
};
Zving.Function.createDelayed = function (c, a, e, b) {
	return function () {
		var f = new Zving.Util.DelayedTask(),
		h = Array.prototype.slice.call(arguments);
		if (!e.tasks) {
			e.tasks = []
		}
		e.tasks.push(f);
		f.delay(Zving.isObject(a) ? a.buffer : a || 10, function () {
			e.tasks.remove(f);
			c.apply(b, h)
		}, b)
	}
};
Zving.Function.createThrottled = function (f, b, e) {
	var h,
	a,
	c,
	k,
	j = function () {
		f.apply(e || this, c);
		h = new Date().getTime()
	};
	return function () {
		a = new Date().getTime() - h;
		c = arguments;
		clearTimeout(k);
		if (!h || (a >= b)) {
			j()
		} else {
			k = setTimeout(j, b - a)
		}
	}
};
Zving.Function.createInterceptor = function (c, b, a) {
	var e = c;
	if (!z.isFunction(b)) {
		return c
	} else {
		return function () {
			var h = this,
			f = arguments;
			b.target = h;
			b.method = c;
			return (b.apply(a || h || window, f) !== false) ? c.apply(h || window, f) : null
		}
	}
};
Zving.Function.createSequence = function (c, b, a) {
	if (!z.isFunction(b)) {
		return c
	} else {
		return function () {
			var e = c.apply(this || window, arguments);
			b.apply(a || this || window, arguments);
			return e
		}
	}
};
(function () {
	var c = window.Zving,
	b = {};
	var e = function (h) {
		for (var f in h) {
			if (!h.hasOwnProperty(f)) {
				continue
			}
			this[f] = h[f]
		}
	};
	var a = Object.prototype.constructor;
	b.extend = function (f, m, k) {
		if (typeof m === "object") {
			k = m;
			m = f;
			f = k.constructor != a ? k.constructor : function () {
				m.apply(this, arguments)
			}
		}
		var j = function () {},
		h,
		l = m.prototype;
		j.prototype = l;
		h = f.prototype = new j();
		h.constructor = f;
		f.superclass = l;
		if (l.constructor == a) {
			l.constructor = m
		}
		f.override = function (n) {
			b.override(f, n)
		};
		h.override = e;
		b.override(f, k);
		f.extend = function (n) {
			return b.extend(f, n)
		};
		return f
	};
	b.override = function (f, j) {
		if (j) {
			var h = f.prototype;
			c.extra(h, j);
			if (c.isIE && j.hasOwnProperty("toString")) {
				h.toString = j.toString
			}
		}
	};
	c.Class = b
})();
(function () {
	var c = window.Zving,
	a = Zving.Function,
	b = {};
	b.methodize = function (j, e) {
		var f = {},
		h;
		for (h in j) {
			if (j.hasOwnProperty(h) && typeof j[h] === "function" && !/^_/.test(h)) {
				f[h] = a.methodize(j[h], e)
			}
		}
		return f
	};
	b.extend = function (e, f) {
		c.Class.override(e, b.methodize(f))
	};
	b.rwrap = function (l, m, k) {
		var h = {};
		if (undefined === k || null === k) {
			k = []
		}
		var j = 0,
		e = k.length;
		for (; j < e; j++) {
			var f = k[j];
			if (typeof f === "string" && typeof l[f] === "function") {
				h[f] = a.rwrap(l[f], m, -1)
			}
		}
		return c.extraIf(h, l)
	};
	c.Helper = b
})();
(function () {
	var a = {};
	a.trim = function (b) {
		return b.replace(/^\s\s*/, "").replace(/\s\s*$/, "")
	};
	a.format = function (h) {
		var b = h.split("?");
		var f = [];
		for (var e = 0; e < b.length; e++) {
			var c = b[e];
			if (c.charAt(c.length - 1) == "\\") {
				f.push(c.substring(0, c.length - 1));
				f.push("?")
			} else {
				f.push(c);
				f.push(arguments[e + 1])
			}
		}
		return f.join("")
	};
	a.tmpl = function (f, e, c, b) {
		return f.replace(/\{([\w_$]+)\}/g, function (k, h) {
			var j = e[h];
			if (j === undefined || j === null) {
				if (c === undefined || c === null) {
					return ""
				}
				switch (c) {
				case 0:
					return "";
				case 1:
					return h;
				default:
					return k
				}
			}
			if ("[object Function]" == Object.prototype.toString.call(j)) {
				j = j(h)
			}
			return b ? encodeURIComponent(j) : j
		})
	};
	a.camelize = function (b) {
		return b.replace(/\-(\w)/ig, function (e, c) {
			return c.toUpperCase()
		})
	};
	a.decamelize = function (b) {
		return b.replace(/[A-Z]/g, function (c) {
			return "-" + c.toLowerCase()
		})
	};
	a.startsWith = a.startWith = function (c, b) {
		return c.indexOf(b) === 0
	};
	a.endsWith = a.endWith = function (e, b) {
		var c = e.lastIndexOf(b);
		return c >= 0 && c === e.length - b.length
	};
	a.leftPad = function (h, j, f) {
		if (!isNaN(f)) {
			var b = "",
			e;
			for (e = h.length; e < f; e++) {
				b = b.concat(j)
			}
			b = b.concat(h);
			return b
		}
		return null
	};
	a.rightPad = function (h, j, f) {
		if (!isNaN(f)) {
			var b = h,
			e;
			for (e = h.length; e < f; e++) {
				b = b.concat(j)
			}
			return b
		}
		return null
	};
	a.encodeURL = function (b) {
		return encodeURI(b).replace(/\=/g, "%3D").replace(/\+/g, "%2B").replace(/\?/g, "%3F").replace(/\&/g, "%26")
	};
	a.decodeURI = function (b) {
		return decodeURI(b.replace(/%3D/g, "=").replace(/%2B/g, "+").replace(/%3F/g, "?").replace(/%26/g, "&"))
	};
	a.htmlEncode = function (b) {
		return b.replace(/&/g, "&amp;").replace(/\"/g, "&quot;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/ /g, "&nbsp;")
	};
	a.htmlDecode = function (b) {
		return b.replace(/\&quot;/g, '"').replace(/\&lt;/g, "<").replace(/\&gt;/g, ">").replace(/\&nbsp;/g, " ").replace(/\&amp;/g, "&")
	};
	a.javaEncode = function (b) {
		if (!b) {
			return b
		}
		return b.replace("\\", "\\\\").replace("\r\n", "\n").replace("\n", "\\n").replace('"', '\\"').replace("'", "\\'")
	};
	a.javaDecode = function (b) {
		if (!b) {
			return b
		}
		return b.replace("\\\\", "\\").replace("\\n", "\n").replace("\\r", "\r").replace('\\"', '"').replace("\\'", "'")
	};
	a.escapeReg = function (b) {
		return b.replace(new RegExp("([.*+?^=!:\x24{}()|[\\]/\\\\])", "g"), "\\$1")
	};
	a.isNumber = function (b) {
		if (a.trim(b) == "") {
			return false
		}
		return (/^\-?\d+(\.\d+)?$/).test("" + b)
	};
	a.isInt = function (b) {
		if (a.trim(b) == "") {
			return false
		}
		return (/^\-?\d+$/).test("" + b)
	};
	a.toInt = function (b) {
		return parseInt(b, 10)
	};
	a.isTime = function (e) {
		if (a.trim(e) == "") {
			return false
		}
		var b = e.split(":");
		if (b.length !== 3) {
			return false
		}
		if (!a.isNumber(b[0]) || !a.isNumber(b[1]) || !a.isNumber(b[2])) {
			return false
		}
		var c = new Date();
		c.setHours(b[0]);
		c.setMinutes(b[1]);
		c.setSeconds(b[2]);
		return c.toString().indexOf("Invalid") < 0
	};
	a.isDate = function (e) {
		if (a.trim(e) == "") {
			return false
		}
		var b = e.split("-");
		if (b.length !== 3) {
			return false
		}
		if (!a.isNumber(b[0]) || !a.isNumber(b[1]) || !a.isNumber(b[2])) {
			return false
		}
		var c = new Date();
		c.setFullYear(b[0]);
		c.setMonth(b[1]);
		c.setDate(b[2]);
		return c.toString().indexOf("Invalid") < 0
	};
	a.isDateTime = function (c) {
		if (a.trim(c) == "") {
			return false
		}
		if (c.indexOf(" ") < 0) {
			return isDate(c)
		}
		var b = c.split(" ");
		if (b.length < 2) {
			return false
		}
		return a.isDate(b[0]) && a.isTime(b[1])
	};
	Zving.Helper.extend(String, a);
	Zving.String = a
})();
(function () {
	var a = {};
	a.forEach = a.each = function (c, j, h) {
		var f = 0,
		b = c.length;
		for (; f < b; f++) {
			try {
				j.call(h || c[f], c[f], f, c)
			} catch (e) {}

		}
		return c
	};
	a.clone = function (c) {
		var b = c.length;
		var f = [],
		e = 0;
		for (; e < b; e++) {
			if (typeof(c[e]) === "undefined" || c[e] === null) {
				f[e] = c[e];
				continue
			}
			if (c[e].constructor === Array) {
				f[e] = c[e].clone()
			} else {
				f[e] = c[e]
			}
		}
		return f
	};
	a.insert = function (b, e, h) {
		if (isNaN(e) || e < 0 || e > b.length) {
			b.push(h)
		} else {
			var c = b.slice(e),
			f;
			b[e] = h;
			for (f = 0; f < c.length; f++) {
				b[e + 1 + f] = c[f]
			}
		}
		return b
	};
	a.remove = function (b, f, e) {
		var h,
		c;
		for (c = 0; c < b.length; c++) {
			if (f === b[c]) {
				h = b.splice(c, 1)[0]
			}
		}
		if (e) {
			return h
		} else {
			h = null;
			return b
		}
	};
	a.indexOf = function (c, f) {
		var b = c.length,
		e = 0;
		for (; e < b; e++) {
			if (c[e] === f) {
				return e
			}
		}
		return -1
	};
	a.contains = function (b, c) {
		return a.indexOf(b, c) !== -1
	};
	Zving.Helper.extend(Array, a);
	Zving.Array = a
})();
(function () {
	var a = {};
	a.format = function (f, c) {
		var b;
		c = c || "yyyy-MM-dd";
		var h = f.getFullYear();
		var e = {
			M : f.getMonth() + 1,
			d : f.getDate(),
			h : f.getHours(),
			m : f.getMinutes(),
			s : f.getSeconds()
		};
		c = c.replace(/(y+)/ig, function (l, k) {
				var j = Math.min(4, k.length);
				return (h + "").substr(4 - j)
			});
		for (b in e) {
			c = c.replace(new RegExp("(" + b + "+)", "g"), function (k, j) {
					return (e[b] < 10 && j.length > 1) ? "0" + e[b] : e[b]
				})
		}
		return c
	};
	Zving.Helper.extend(Date, a);
	Zving.Date = a
})();
(function () {
	var c = window.Zving,
	b = {};
	b.evalJs = function (e, f) {
		return new Function("opts", e)(f)
	};
	b.evalExp = function (e, f) {
		return new Function("opts", "return " + e + ";")(f)
	};
	b.globalEval = function (e) {
		if (window.execScript) {
			window.execScript(e)
		} else {
			window["eval"].call(window, e)
		}
	};
	b.runFuncBind$elem = function (l, e) {
		var j = /([\w\.]+)(\((.*)\);?)?$/.exec(e);
		if (j[1]) {
			var k = namespace(j[1]);
			var f = [];
			if (j[3]) {
				var h = b.getReferId(l);
				j[3].replace(/\bthis\b/g, h);
				f = window["eval"]("[" + j[3] + "]")
			}
			if (typeof k == "function") {
				k.apply(l, f)
			} else {
				Console.error("请确认用于插入内容模块的方法 " + e + " 存在并且没有语法错误。")
			}
		}
	};
	b.rectContains = function (f, e) {
		return f.left <= e.left && f.right >= e.right && f.top <= e.top && f.bottom >= e.bottom
	};
	b.rectIntersect = function (j, h) {
		var k = Math.max(j.top, h.top),
		m = Math.min(j.right, h.right),
		e = Math.min(j.bottom, h.bottom),
		f = Math.max(j.left, h.left);
		if (e >= k && m >= f) {
			return {
				top : k,
				right : m,
				bottom : e,
				left : f
			}
		} else {
			return null
		}
	};
	var a = 0;
	b.getReferId = function (e) {
		if (typeof e == "object" || typeof e == "function") {
			var f = "_z_" + (++a);
			window[f] = e;
			return f
		}
	};
	b.random = function (f, e) {
		return Math.floor(Math.random() * (e - f + 1) + f)
	};
	b.DelayedTask = function (j, h, e) {
		var k = this,
		l,
		f = function () {
			clearInterval(l);
			l = null;
			j.apply(h, e || [])
		};
		k.delay = function (n, p, o, m) {
			k.cancel();
			j = p || j;
			h = o || h;
			e = m || e;
			l = setInterval(f, n)
		};
		k.cancel = function () {
			if (l) {
				clearInterval(l);
				l = null
			}
		}
	};
	c.Util = b
})();
(function () {
	var z = window.Zving,
	DataTable,
	DataRow,
	DataCollection,
	_ZVING_NULL,
	javaEncode = z.String.javaEncode;
	z.Constant.Null = _ZVING_NULL = "_ZVING_NULL";
	DataTable = function () {
		this.Columns = null;
		this.Values = null;
		this.Rows = null;
		this.ColMap = {};
		DataTable.prototype.getRowCount = function () {
			return this.Rows.length
		};
		DataTable.prototype.getColCount = function () {
			return this.Columns.length
		};
		DataTable.prototype.getColName = function (i) {
			return this.Columns[i]
		};
		DataTable.prototype.get2 = function (i, j) {
			return this.Rows[i].get2(j)
		};
		DataTable.prototype.get = function (i, str) {
			return this.Rows[i].get(str)
		};
		DataTable.prototype.getDataRow = function (i) {
			return this.Rows[i]
		};
		DataTable.prototype.deleteRow = function (i) {
			this.Values.splice(i, 1);
			this.Rows.splice(i, 1);
			for (var k = i; k < this.Rows.length; k++) {
				this.Rows[k].Index = k
			}
		};
		DataTable.prototype.insertRow = function (i, values) {
			this.Values.splice(i, 0, values);
			this.Rows.splice(i, 0, new DataRow(this, i));
			for (var k = i; k < this.Rows.length; k++) {
				this.Rows[k].Index = k
			}
		};
		DataTable.prototype.insertColumn = function (name) {
			if (this.hasColumn(name)) {
				return
			}
			var col = {};
			col.Name = name.toLowerCase();
			col.Type = 1;
			this.Columns.push(col);
			this.ColMap[col.Name] = this.Columns.length - 1;
			for (var i = 0; i < this.Values.length; i++) {
				this.Values[i].push(null)
			}
		};
		DataTable.prototype.hasColumn = function (name) {
			name = name.toLowerCase();
			for (var j = 0; j < this.Columns.length; j++) {
				if (this.Columns[j].Name === name) {
					return true
				}
			}
			return false
		};
		DataTable.prototype.init = function (cols, values) {
			this.Values = values;
			this.Columns = [];
			this.Rows = [];
			var i;
			for (i = 0; i < cols.length; i++) {
				var col = {};
				col.Name = cols[i][0].toLowerCase();
				col.Type = cols[i][1];
				this.Columns[i] = col;
				this.ColMap[col.Name] = i
			}
			for (i = 0; i < values.length; i++) {
				var row = new DataRow(this, i);
				this.Rows[i] = row
			}
		};
		DataTable.prototype.toString = function () {
			var arr = [],
			i;
			arr.push("<columns><![CDATA[[");
			for (i = 0; i < this.Columns.length; i++) {
				if (i !== 0) {
					arr.push(",")
				}
				arr.push("[");
				arr.push('"' + this.Columns[i].Name + '",');
				arr.push(this.Columns[i].Type);
				arr.push("]")
			}
			arr.push("]]]></columns>");
			arr.push("<values><![CDATA[[");
			for (i = 0; i < this.Values.length; i++) {
				if (i != 0) {
					arr.push(",")
				}
				arr.push("[");
				for (var j = 0; j < this.Columns.length; j++) {
					if (j != 0) {
						arr.push(",")
					}
					if (this.Values[i][j] == null || typeof(this.Values[i][j]) == "undefined") {
						arr.push('"_ZVING_NULL"')
					} else {
						var v = this.Values[i][j];
						if (typeof(v) == "string") {
							v = javaEncode(v)
						}
						arr.push('"' + v + '"')
					}
				}
				arr.push("]")
			}
			arr.push("]]]></values>");
			return arr.join("")
		}
	};
	DataRow = function (dt, index) {
		this.DT = dt;
		this.Index = index
	};
	DataRow.prototype.get2 = function (i) {
		return this.DT.Values[this.Index][i]
	};
	DataRow.prototype.getColCount = function () {
		return this.DT.Columns.length
	};
	DataRow.prototype.getColName = function (i) {
		return this.DT.Columns[i]
	};
	DataRow.prototype.get = function (str) {
		str = str.toLowerCase();
		var c = this.DT.ColMap[str];
		if (typeof(c) == "undefined") {
			return null
		}
		return this.DT.Values[this.Index][c]
	};
	DataRow.prototype.set = function (str, value) {
		str = str.toLowerCase();
		var c = this.DT.ColMap[str];
		if (typeof(c) == "undefined") {
			return
		}
		this.DT.Values[this.Index][c] = value
	};
	DataRow.prototype.set2 = function (i, value) {
		this.DT.Values[this.Index][i] = value
	};
	DataCollection = function () {
		this.map = {};
		this.valuetype = {};
		this.keys = []
	};
	DataCollection.prototype.get = function (ID) {
		if (typeof(ID) === "number") {
			return this.map[this.keys[ID]]
		}
		return this.map[ID]
	};
	DataCollection.prototype.getKey = function (index) {
		return this.keys[index]
	};
	DataCollection.prototype.size = function () {
		return this.keys.length
	};
	DataCollection.prototype.remove = function (ID) {
		var obj;
		if (typeof(ID) === "number") {
			if (ID < this.keys.length) {
				obj = this.map[this.keys[ID]];
				delete this.map[this.keys[ID]];
				delete this.valuetype[this.keys[ID]];
				this.keys.splice(ID, 1);
				return obj
			}
		} else {
			var i;
			for (i = 0; i < this.keys.length; i++) {
				if (this.keys[i] == ID) {
					obj = this.map[ID];
					this.map[ID] = null;
					delete this.map[ID];
					delete this.valuetype[ID];
					this.keys.splice(i, 1);
					return obj
				}
			}
		}
		return null
	};
	DataCollection.prototype.toQueryString = function () {
		var arr = [],
		i;
		for (i = 0; i < this.keys.length; i++) {
			if (this.map[this.keys[i]] == null || this.map[this.keys[i]] == "") {
				continue
			}
			if (i !== 0) {
				arr.push("&")
			}
			arr.push(this.keys[i] + "=" + this.map[this.keys[i]])
		}
		return arr.join("")
	};
	DataCollection.prototype.parseXML = function (xmlDoc) {
		var coll = xmlDoc.documentElement;
		if (!coll) {
			return false
		}
		if (coll.nodeName == "parsererror") {
			z.Console.error(coll.innerText ? coll.innerText : coll.textContent);
			return
		}
		var nodes = coll.childNodes;
		var len = nodes.length,
		i;
		for (i = 0; i < len; i++) {
			var node = nodes[i];
			var Type = node.getAttribute("Type");
			var ID = node.getAttribute("ID");
			this.valuetype[ID] = Type;
			if (Type === "String") {
				var v = node.getAttribute("Value") ? node.getAttribute("Value") : node.firstChild.nodeValue;
				if (v === _ZVING_NULL) {
					v = null
				}
				this.map[ID] = v
			} else {
				if (Type === "StringArray") {
					this.map[ID] = eval("[" + node.firstChild.nodeValue + "]")
				} else {
					if (Type === "Map") {
						this.map[ID] = eval("(" + node.firstChild.nodeValue + ")")
					} else {
						if (Type === "DataTable" || Type === "Schema" || Type === "SchemaSet") {
							this.parseDataTable(node, "DataTable")
						} else {
							this.map[ID] = node.getAttribute("Value") ? node.getAttribute("Value") : node.firstChild.nodeValue
						}
					}
				}
			}
			this.keys.push(ID)
		}
		return true
	};
	DataCollection.prototype.parseJson = function (jsonStr) {
		var mapx = eval('(' + jsonStr + ')');
		for(var ID in mapx){
			this.keys.push(ID);
			this.map[ID] = mapx[ID];
		}
		return true
	};
	DataCollection.prototype.toJson = function () {
		var arr = [];
		for (i = 0; i < this.keys.length; i++) {
			var ID = this.keys[i];
			var thisType = this.valuetype[ID];
			var v = this.map[ID];
			if (/^(long|int|float|double)$/i.test(thisType)) {
				arr.push('\"' + ID + '\":' + v )
			} else if (thisType == "String") {
				if (v == null || typeof(v) == "undefined") {
					arr.push('\"' + ID + '\":\"\"')
				} else {
					arr.push('\"' + ID + '\":\"' + v + '\"')
				}
			}
		}
		return '{'+arr.join(',')+'}'
	};
	DataCollection.prototype.add = function (ID, Value, Type) {
		if (typeof this.map[ID] == "undefined") {
			this.keys.push(ID)
		}
		this.map[ID] = Value;
		if (Type) {
			this.valuetype[ID] = Type
		} else {
			if (Value && Value.getDataRow) {
				this.valuetype[ID] = "DataTable"
			} else {
				this.valuetype[ID] = "String"
			}
		}
	};
	DataCollection.prototype.addAll = function (dc) {
		if (!dc) {
			return
		}
		if (!dc.valuetype) {
			z.Console.error("DataCollection.addAll()要求参数必须是一个DataCollection!");
			return
		}
		var size = dc.size(),
		i;
		for (i = 0; i < size; i++) {
			var k = dc.keys[i];
			var v = dc.map[k];
			var t = dc.valuetype[k];
			this.add(k, v, t)
		}
	};
	z.DataRow = DataRow;
	z.DataTable = DataTable;
	z.DataCollection = DataCollection
})();
(function () {
	var a = {};
	a.parse = (function () {
		var b = /[\u0000\u00ad\u0600-\u0604\u070f\u17b4\u17b5\u200c-\u200f\u2028-\u202f\u2060-\u206f\ufeff\ufff0-\uffff]/g;
		var c;
		function e(j, m) {
			var l = m[j];
			if (l && typeof l === "object") {
				for (var h in l) {
					var f = e(h, l);
					if (f !== undefined) {
						l[h] = f
					} else {
						delete l[h]
					}
				}
			}
			return c.call(m, j, l)
		}
		return function (j, f) {
			b.lastIndex = 0;
			if (b.test(j)) {
				j = j.replace(b, function (k) {
						return "\\u" + ("0000" + k.charCodeAt(0).toString(16)).slice(-4)
					})
			}
			if (j && /^[\],:{}\s]*$/.test(j.replace(/\\(?:["\\\/bfnrt]|u[0-9a-fA-F]{4})/g, "@").replace(/"[^"\\\n\r]*"|true|false|null|-?\d+(?:\.\d*)?(?:[eE][+\-]?\d+)?/g, "]").replace(/(?:^|:|,)(?:\s*\[)+/g, ""))) {
				var h = new Function("return " + j)();
				c = f;
				return typeof f === "function" ? e("", {
					"" : h
				}) : h
			}
			throw new SyntaxError("Invalid JSON format in executing JSON.parse")
		}
	})();
	a.stringify = (function () {
		function h(m) {
			return m < 10 ? "0" + m : m
		}
		Date.prototype.toJSON = function (m) {
			return isFinite(this.valueOf()) ? this.getUTCFullYear() + "-" + h(this.getUTCMonth() + 1) + "-" + h(this.getUTCDate()) + "T" + h(this.getUTCHours()) + ":" + h(this.getUTCMinutes()) + ":" + h(this.getUTCSeconds()) + "Z" : null
		};
		String.prototype.toJSON = Number.prototype.toJSON = Boolean.prototype.toJSON = function (m) {
			return this.valueOf()
		};
		var b;
		var l;
		var j;
		var k = /[\\\"\x00-\x1f\x7f-\x9f\u00ad\u0600-\u0604\u070f\u17b4\u17b5\u200c-\u200f\u2028-\u202f\u2060-\u206f\ufeff\ufff0-\uffff]/g;
		var f = {
			"\b" : "\\b",
			"\t" : "\\t",
			"\n" : "\\n",
			"\f" : "\\f",
			"\r" : "\\r",
			'"' : '\\"',
			"\\" : "\\\\"
		};
		function e(m) {
			k.lastIndex = 0;
			return k.test(m) ? '"' + m.replace(k, function (n) {
				var o = f[n];
				return typeof o === "string" ? o : "\\u" + ("0000" + n.charCodeAt(0).toString(16)).slice(-4)
			}) + '"' : '"' + m + '"'
		}
		function c(w, o) {
			var u = l,
			x = o[w],
			t = [],
			p,
			n,
			q,
			m;
			if (x && typeof x.toJSON == "function") {
				x = x.toJSON(w)
			}
			if (typeof j == "function") {
				x = j.call(o, w, x)
			}
			if (x === null) {
				return String(x)
			} else {
				if (typeof x == "undefined") {
					return x
				}
			}
			switch (typeof x) {
			case "string":
				return e(x);
			case "number":
				return isFinite(x) ? String(x) : "null";
			case "boolean":
				return String(x);
			case "object":
				l += b;
				if (Object.prototype.toString.apply(x) === "[object Array]") {
					for (p = 0, len = x.length; p < len; p++) {
						t[p] = c(p, x) || "null"
					}
					m = t.length == 0 ? "[]" : l ? "[\n" + l + t.join(",\n" + l) + "\n" + u + "]" : "[" + t.join(",") + "]";
					l = u;
					return m
				}
				if (j && Object.prototype.toString.apply(j) === "[object Array]") {
					for (p = 0, len = j.length; p < len; p++) {
						n = j[p];
						if (typeof n == "string") {
							q = c(n, x);
							if (q) {
								t.push(e(n) + (l ? ": " : ":") + q)
							}
						}
					}
				} else {
					for (n in x) {
						q = c(n, x);
						if (q) {
							t.push(e(n) + (l ? ": " : ":") + q)
						}
					}
				}
				m = t.length == 0 ? "{}" : l ? "{\n" + l + t.join(",\n" + l) + "\n" + u + "}" : "{" + t.join(",") + "}";
				l = u;
				return m
			}
		}
		return function (o, n, m) {
			if (n != null && Object.prototype.toString.apply(n) !== "[object Array]" && Object.prototype.toString.apply(n) === "[object Function]") {
				throw new Error("Invalid type of 2nd argument in JSON.stringify, only Array and Function allowed")
			}
			l = b = "";
			if (m && typeof m === "number") {
				b = new Array(m + 1).join(" ")
			} else {
				if (m && typeof m === "string") {
					b = m
				}
			}
			j = n;
			return c("", {
				"" : o
			})
		}
	})();
	a.toQuery = function (e, h) {
		var b = [],
		f,
		c = h || function (j) {
			return encodeURIComponent(j)
		};
		z.iterate(e, function (j, k) {
			if (z.isArray(k)) {
				f = k.length;
				while (f--) {
					b.push(j + "=" + c(k[f], j))
				}
			} else {
				b.push(j + "=" + c(k, j))
			}
		});
		return b.join("&")
	};
	a.keys = function (e) {
		var b = [];
		for (var c in e) {
			if (e.hasOwnProperty(c)) {
				b.push(c)
			}
		}
		return b
	};
	a.values = function (e) {
		var b = [];
		for (var c in e) {
			if (e.hasOwnProperty(c)) {
				b.push(e[c])
			}
		}
		return b
	};
	Zving.JSON = a
})();
(function () {
	var b = window.Zving;
	var a = function (e, c) {
		this.name = c;
		this.obj = e;
		this.listeners = []
	};
	a.prototype.isEvent = true;
	a.prototype.addListener = function (f, e, c) {
		var h = this,
		j;
		e = e || h.obj;
		if (!h.isListening(f, e)) {
			j = h.createListener(f, e, c);
			if (h.firing) {
				h.listeners = h.listeners.slice(0)
			}
			h.listeners.push(j)
		}
	};
	a.prototype.createListener = function (h, f, c) {
		c = c || {};
		f = f || this.obj;
		var j = {
			fn : h,
			scope : f,
			options : c,
			ev : this
		},
		e = h;
		if (c.single) {
			e = Zving.Function.createSingle(e, j, c, f)
		}
		if (c.delay) {
			e = Zving.Function.createDelayed(e, j, c, f)
		}
		if (c.buffer) {
			e = Zving.Function.createBuffered(e, j, c, f)
		}
		j.fireFn = e;
		return j
	};
	a.prototype.findListener = function (f, e) {
		var j = this.listeners,
		c = j.length,
		h;
		e = e || this.obj;
		while (c--) {
			h = j[c];
			if (h) {
				if (h.fn == f && (h.scope == e || h.scope == this.obj)) {
					return c
				}
			}
		}
		return -1
	};
	a.prototype.isListening = function (e, c) {
		return this.findListener(e, c) !== -1
	};
	a.prototype.removeListener = function (h, f) {
		var e,
		l,
		c,
		j = this;
		e = j.findListener(h, f);
		if (e != -1) {
			l = j.listeners[e];
			if (j.firing) {
				j.listeners = j.listeners.slice(0)
			}
			if (l.task) {
				l.task.cancel();
				delete l.task
			}
			c = l.tasks && l.tasks.length;
			if (c) {
				while (c--) {
					l.tasks[c].cancel()
				}
				delete l.tasks
			}
			j.listeners.splice(e, 1);
			return true
		}
		return false
	};
	a.prototype.clearListeners = function () {
		var e = this,
		f = e.listeners,
		c = f.length;
		while (c--) {
			e.removeListener(f[c].fn, f[c].scope)
		}
	};
	a.prototype.fire = function () {
		var j = this,
		h = j.listeners,
		c = h.length,
		f = 0,
		e,
		k;
		if (c > 0) {
			j.firing = true;
			for (; f < c; f++) {
				k = h[f];
				e = arguments.length ? Array.prototype.slice.call(arguments, 0) : [];
				if (k.options) {
					e.push(k.options)
				}
				if (!k.fireFn) {
					throw "通过CustEvent.addListener注册的方法有误，无法执行"
				}
				if (k && k.fireFn.apply(k.scope || j.obj || window, e) === false) {
					return (j.firing = false)
				}
			}
		}
		j.firing = false;
		return true
	};
	b.CustEvent = a
})();
(function () {
	var b = window.Zving;
	var a = /^(?:scope|delay|buffer|single)$/;
	b.Observable = function (h) {
		var c = h,
		f = c.events;
		c.events = f || {};
		c.fireEvent = function () {
			var l = Array.prototype.slice.call(arguments),
			m = l[0].toLowerCase(),
			k = true,
			j = c.events[m],
			e,
			n;
			if (c.eventsSuspended === true) {
				e = c.eventQueue;
				if (e) {
					e.push(l)
				}
			} else {
				if (b.isObject(j) && j.bubble) {
					if (j.fire.apply(j, l.slice(1)) === false) {
						return false
					}
					n = c.getBubbleTarget && c.getBubbleTarget();
					if (n && n.enableBubble) {
						if (!n.events[m] || !b.isObject(n.events[m]) || !n.events[m].bubble) {
							n.enableBubble(m)
						}
						return n.fireEvent.apply(n, l)
					}
				} else {
					if (b.isObject(j)) {
						l.shift();
						k = j.fire.apply(j, l)
					}
				}
			}
			return k
		};
		c.addListener = function (k, n, m, q) {
			var p,
			r,
			l,
			j;
			if (b.isObject(k)) {
				q = k;
				for (p in q) {
					r = q[p];
					if (!a.test(p)) {
						c.addListener(p, r.fn || r, r.scope || q.scope, r.fn ? r : q)
					}
				}
			} else {
				k = k.toLowerCase();
				j = c.events[k] || true;
				if (typeof(j) === "boolean") {
					c.events[k] = j = new b.CustEvent(c, k)
				}
				j.addListener(n, m, b.isObject(q) ? q : {})
			}
		};
		c.removeListener = function (j, l, k) {
			var e = c.events[j.toLowerCase()];
			if (b.isObject(e)) {
				e.removeListener(l, k)
			}
		};
		c.purgeListeners = function () {
			var k = c.events,
			e,
			j;
			for (j in k) {
				e = k[j];
				if (b.isObject(e)) {
					e.clearListeners()
				}
			}
		};
		c.addEvents = function (k) {
			c.events = c.events || {};
			if (typeof(k) === "string") {
				var e = arguments,
				j = e.length;
				while (j--) {
					c.events[e[j]] = c.events[e[j]] || true
				}
			} else {
				extraIf(c.events, k)
			}
			if (c.listeners) {
				c.on(c.listeners);
				delete c.listeners
			}
		};
		c.hasListener = function (j) {
			var k = c.events[j];
			return b.isObject(k) && k.listeners.length > 0
		};
		c.suspendEvents = function (e) {
			c.eventsSuspended = true;
			if (e && !c.eventQueue) {
				c.eventQueue = []
			}
		};
		c.resumeEvents = function () {
			var e = e,
			j = e.eventQueue || [];
			e.eventsSuspended = false;
			delete e.eventQueue;
			j.each(function (k) {
				e.fireEvent.apply(e, k)
			})
		};
		c.enableBubble = function (m) {
			if (!b.isEmpty(m)) {
				m = b.isArray(m) ? m : Array.prototype.slice.call(arguments);
				for (var l = 0, e = m.length; l < e; l++) {
					var k = m[l];
					k = k.toLowerCase();
					var j = c.events[k] || true;
					if (typeof j === "boolean") {
						j = new b.CustEvent(c, k);
						c.events[k] = j
					}
					j.bubble = true
				}
			}
		};
		c.on = c.addListener;
		c.un = c.removeListener
	}
})();
(function () {
	if (Zving.isIE || !window.HTMLElement || !HTMLElement.prototype) {
		return
	}
	var f = {};
	f.insertAdjacentElement = function (h, j) {
		switch (h.toLowerCase()) {
		case "beforebegin":
			this.parentNode.insertBefore(j, this);
			return this.previousSibling;
		case "afterbegin":
			this.insertBefore(j, this.firstChild);
			return this.firstChild;
		case "beforeend":
			this.appendChild(j);
			return this.lastChild;
		case "afterend":
			if (this.nextSibling) {
				this.parentNode.insertBefore(j, this.nextSibling)
			} else {
				this.parentNode.appendChild(j)
			}
			return this.nextSibling
		}
	};
	f.insertAdjacentHTML = function (j, l) {
		var k = this.ownerDocument.createRange();
		k.setStartBefore(this);
		var h = k.createContextualFragment(l);
		return this.insertAdjacentElement(j, h)
	};
	f.insertAdjacentText = function (j, h) {
		return this.insertAdjacentElement(j, document.createTextNode(h))
	};
	if (!HTMLElement.prototype.contains) {
		f.contains = function (h) {
			return !!(this.compareDocumentPosition(h) & 16)
		}
	}
	function e(j) {
		var h = function (m) {
			var l = m.currentTarget,
			k = m.relatedTarget;
			if (l != k && !l.contains(k)) {
				j.call(l, m)
			}
		};
		j._wrapFunc = h;
		return h
	}
	function b(j) {
		var h = function (k) {
			if (typeof k.detail != "undefined" && typeof k.wheelDelta == "undefined") {
				k.wheelDelta = -k.detail * 40
			}
			j.call(this, k)
		};
		j._wrapFunc = h;
		return h
	}
	f.attachEvent = function (j, h) {
		j = j.replace(/^on/i, "");
		if (j == "mouseenter") {
			this.addEventListener("mouseover", e(h), false)
		} else {
			if (j == "mouseleave") {
				this.addEventListener("mouseout", e(h), false)
			} else {
				if (j == "mousewheel" && /Firefox/i.test(navigator.userAgent)) {
					this.addEventListener("DOMMouseScroll", b(h), false)
				} else {
					this.addEventListener(j, h, false)
				}
			}
		}
	};
	f.detachEvent = function (j, h) {
		j = j.replace(/^on/i, "");
		if (j == "mouseenter") {
			this.removeEventListener("mouseover", h._wrapFunc, false)
		} else {
			if (j == "mouseleave") {
				this.removeEventListener("mouseout", h._wrapFunc, false)
			} else {
				if (j == "mousewheel" && /Firefox/i.test(navigator.userAgent)) {
					this.removeEventListener("DOMMouseScroll", h, false)
				} else {
					this.removeEventListener(j, h, false)
				}
			}
		}
	};
	f.fireEvent = function (k) {
		k = k.replace(/^on/i, "");
		var h;
		var j = this.ownerDocument || document;
		if (/mouse|click/i.test(k)) {
			h = j.createEvent("MouseEvents");
			h.initMouseEvent(k, true, true, j.defaultView, 1, 0, 0, 0, 0, false, false, false, false, 0, null)
		} else {
			h = j.createEvent("Events");
			h.initEvent(k, true, true)
		}
		return this.dispatchEvent(h)
	};
	Zving.extraIf(HTMLElement.prototype, f);
	window.constructor.prototype.attachEvent = HTMLDocument.prototype.attachEvent = HTMLElement.prototype.attachEvent;
	window.constructor.prototype.detachEvent = HTMLDocument.prototype.detachEvent = HTMLElement.prototype.detachEvent;
	window.constructor.prototype.fireEvent = HTMLDocument.prototype.fireEvent = HTMLElement.prototype.fireEvent;
	window.constructor.prototype.__defineGetter__("event", function () {
		var k = arguments.callee.caller,
		h = null,
		j = 0;
		for (; k != null; j++, k = k.caller) {
			h = k.arguments[0];
			if (h && typeof h == "object" && /Event/.test(h)) {
				return h
			}
			if (j > 999) {
				return null
			}
		}
		return null
	});
	var a = HTMLElement.prototype;
	a.__defineSetter__("outerHTML", function (j) {
		var h = this.ownerDocument.createRange();
		h.setStartBefore(this);
		var k = h.createContextualFragment(j);
		this.parentNode.replaceChild(k, this);
		return j
	});
	a.__defineGetter__("outerHTML", function () {
		var h,
		k,
		j = this.attributes,
		l = "<" + this.tagName.toLowerCase();
		for (k = 0; k < j.length; k++) {
			h = j[k];
			if (h.specified) {
				l += " " + h.name + '="' + h.value + '"'
			}
		}
		if (!this.hasChildNodes) {
			return l + ">"
		}
		return l + ">" + this.innerHTML + "</" + this.tagName.toLowerCase() + ">"
	});
	a.__defineSetter__("innerText", function (h) {
		this.textContent = h
	});
	a.__defineGetter__("innerText", function () {
		return this.textContent
	});
	a.__defineSetter__("outerText", function (h) {
		this.parentNode.replaceChild(document.createTextNode(s), this);
		return s
	});
	a.__defineGetter__("outerText", function () {
		var h = this.ownerDocument.createRange();
		h.selectNodeContents(this);
		return h.toString()
	});
	a.__defineGetter__("currentStyle", function () {
		return this.ownerDocument.defaultView.getComputedStyle(this, null)
	});
	a.__defineGetter__("parentElement", function () {
		return (this.parentNode === this.ownerDocument) ? null : this.parentNode
	});
	a.__defineGetter__("children", function () {
		var j = [],
		h;
		for (h = 0; h < this.childNodes.length; h++) {
			var k = this.childNodes[h];
			if (k.nodeType === 1) {
				j.push(k)
			}
		}
		return j
	});
	a.__defineGetter__("canHaveChildren", function () {
		return !/^(area|base|basefont|col|frame|hr|img|br|input|isindex|link|meta|param)$/i.test(this.tagName)
	});
	HTMLDocument.prototype.__defineGetter__("all", function () {
		return this.getElementsByName("*")
	});
	HTMLDocument.prototype.parentWindow = document.defaultView;
	HTMLCollection.prototype.item = function (h) {
		return this[h]
	};
	var c = Event.prototype;
	c.__defineGetter__("srcElement", function () {
		var h = this.target;
		while (h && h.nodeType != 1) {
			h = h.parentNode
		}
		return h
	});
	c.__defineGetter__("fromElement", function () {
		var h = (this.type === "mouseover" && this.relatedTarget) || (this.type === "mouseout" && this.target) || null;
		while (h && h.nodeType != 1) {
			h = h.parentNode
		}
		return h
	});
	c.__defineGetter__("toElement", function () {
		var h = (this.type === "mouseover" && this.target) || (this.type === "mouseout" && this.relatedTarget) || null;
		while (h && h.nodeType != 1) {
			h = h.parentNode
		}
		return h
	});
	c.__defineGetter__("x", function () {
		return this.pageX
	});
	c.__defineGetter__("y", function () {
		return this.pageY
	});
	c.__defineGetter__("offsetX", function () {
		return this.layerX
	});
	c.__defineGetter__("offsetY", function () {
		return this.layerY
	});
	c.__defineSetter__("returnValue", function (h) {
		if (!h) {
			this.preventDefault()
		}
		return h
	});
	c.__defineSetter__("cancelBubble", function (h) {
		if (h) {
			this.stopPropagation()
		}
		return h
	});
	if (!window.execScript) {
		window.execScript = new Function("window.eval(arguments[0])")
	}
})();
(function () {
	var h = window.Zving,
	f;
	h.support = {};
	var e = document.createElement("input");
	var c = "autocomplete,autofocus,list,placeholder,max,min,multiple,pattern,required,step".split(",");
	for (var b = 0, a = c.length; b < a; b++) {
		h.support[c[b]] = !!(c[b]in e)
	}
	e = null;
	h.g = h.getDom = f = function (j, k) {
		if (!j) {
			return j
		}
		if (typeof j == "string") {
			return (k || document).getElementById(j.replace(/^#/, ""))
		}
		if (!j.nodeName && j.length && j[0].nodeName) {
			return j[0]
		}
		return j
	};
	h.getByTag = function (o, m) {
		if (arguments.length === 1) {
			m = o;
			o = document
		}
		if (typeof(m) !== "string") {
			throw location.pathname + " - $T : 参数错误 " + name
		}
		o = f(o);
		var n = o.getElementsByTagName(m || "*");
		var k = [];
		var j = n.length;
		for (var l = 0; l < j; l++) {
			k.push(n[l])
		}
		return k
	};
	h.getByName = function (k) {
		if (typeof(k) !== "string") {
			throw location.pathname + " - $N : 参数错误 " + k
		}
		var m = document.getElementsByName(k);
		if (!m || m.length === 0) {
			return null
		}
		var j = [];
		for (var l = 0; l < m.length; l++) {
			var n = m[l];
			if (n.getAttribute("ztype") === "select" && n.tagName == "input") {
				n = n.parentNode
			}
			j.push(n)
		}
		return j
	};
	h.getValues = function (l) {
		var n = h.getByName(l);
		if (!n || !n.length) {
			return null
		}
		var j = [];
		for (var m = 0; m < n.length; m++) {
			var k = h.Node.getValue(n[m]);
			if (k !== null) {
				j.push(k)
			}
		}
		return j.length === 0 ? null : j
	};
	h.setValues = function (m, o) {
		o = String(o);
		var p = h.getByName(m);
		if (!p) {
			return
		}
		if (!p[0]) {
			return h.Node.setValue(p, o)
		}
		var n;
		if (p[0].type == "checkbox") {
			if (o === null) {
				o = new Array(4)
			}
			var k = o;
			if (!isArray(o)) {
				k = o.split(",")
			}
			for (n = 0; n < p.length; n++) {
				for (var l = 0; l < k.length; l++) {
					if (p[n].value == k[l]) {
						h.Node.setValue(p[n], k[l]);
						break
					}
					h.Node.setValue(p[n], k[l])
				}
			}
			return
		}
		for (n = 0; n < p.length; n++) {
			h.Node.setValue(p[n], o)
		}
	};
	h.toXMLDOM = function (m) {
		var l;
		if (h.isIE) {
			try {
				l = new ActiveXObject("Microsoft.XMLDOM")
			} catch (j) {
				l = new ActiveXObject("Msxml2.DOMDocument")
			}
			l.loadXML(m)
		} else {
			var k = new DOMParser();
			l = k.parseFromString(m, "text/xml")
		}
		return l
	};
	window.setTimeoutEx = function (n, l, m) {
		if (typeof n == "function") {
			var k = Array.prototype.slice.call(arguments, 2);
			var j = function () {
				n.apply(null, k)
			};
			return window.setTimeout(j, l)
		}
		return window.setTimeout(n, l)
	};
	h.elCache = {}

})();
(function () {
	var e = window.Zving,
	c = e.getDom;
	var b = /^(?:br|frame|hr|img|input|link|meta|range|spacer|wbr|area|param|col)$/i,
	a = {};
	a.createNode = a.createDom = function (j, h, l) {
		if (!j) {
			throw "传入createNode方法的参数错误"
		}
		if (e.isObject(j)) {
			l = h || document;
			h = j;
			j = h.tagName || h.tag || "div"
		}
		l = l || document;
		var k = l.createElement(j);
		for (var f in h) {
			if (h.hasOwnProperty(f)) {
				if (!/tag|tagName|children|cn|html$/i.test(f)) {
					if (f == "cls" || f == "className") {
						k.className = h[f]
					} else {
						k.setAttribute(f, h[f])
					}
				}
			}
		}
		k.appendTo = function (m) {
			m.appendChild(this);
			this.appendTo = null;
			return this
		};
		return k
	};
	a.createHtml = function (n) {
		var j = "",
		h,
		m,
		l,
		f,
		p;
		if (e.isString(n)) {
			j = n
		} else {
			if (e.isArray(n)) {
				for (var k = 0; k < n.length; k++) {
					if (n[k]) {
						j += createHtml(n[k])
					}
				}
			} else {
				j += "<" + (n.tag = n.tag || "div");
				e.iterate(n, function (o, q) {
					if (!/tag|children|cn|html$/i.test(o)) {
						if (e.isObject(q)) {
							j += " " + o + '="';
							e.iterate(q, function (t, r) {
								j += t + ":" + r + ";"
							});
							j += '"'
						} else {
							j += " " + ({
								cls : "class",
								htmlFor : "for"
							}
								[o] || o) + '="' + q + '"'
						}
					}
				});
				if (b.test(n.tag)) {
					j += "/>"
				} else {
					j += ">";
					p = n.children || n.cn;
					if (p) {
						j += a.createHtml(p)
					} else {
						if (n.html) {
							j += n.html
						}
					}
					j += "</" + n.tag + ">"
				}
			}
		}
		return j
	};
	a.removeNode = e.isIE && !e.isIE8 ? (function () {
			Zving._RycDiv = null;
			return function (h) {
				h = c(h);
				if (!h || !h.nodeName) {
					return
				}
				var f = h.ownerDocument.parentWindow.Zving;
				if (h && h.tagName != "BODY") {
					if (f.EventManager) {
						f.EventManager.removeAll(h);
						f.detachNodeMethod(h)
					}
					f._RycDiv = f._RycDiv || h.ownerDocument.createElement("div");
					f._RycDiv.appendChild(h);
					f._RycDiv.innerHTML = "";
					delete f.elCache[e.id(h)]
				}
				setTimeout(CollectGarbage, 1)
			}
		}
			()) : (function (h) {
			h = c(h);
			if (!h || !h.nodeName) {
				return
			}
			var f = h.ownerDocument.parentWindow.Zving;
			if (h && h.parentNode && h.tagName != "BODY") {
				if (f.EventManager) {
					f.EventManager.removeAll(h);
					f.detachNodeMethod(h)
				}
				h.parentNode.removeChild(h);
				delete f.elCache[e.id(h)]
			}
		});
	a.createNodeByHtml = function (o, p) {
		var f = /^(<[\w\W]+>)$/m;
		var h = /^<(\w+)\s*\/?>(?:<\/\1>)?$/;
		var m = /<|&#?\w+;/;
		var j;
		var l = f.exec(o);
		p = p || document;
		if (l && l[1]) {
			var n = h.exec(o);
			if (n) {
				j = p.createElement(n[1])
			} else {
				var k = p.createElement("div");
				k.innerHTML = o;
				j = k.firstChild
			}
		} else {
			if (!m.test(o)) {
				j = p.createTextNode(o)
			} else {
				alert(["createNodeByHtml方法参数错误", o])
			}
		}
		if (j && j.nodeType) {
			j.appendTo = function (q) {
				q.appendChild(this);
				this.appendTo = null;
				return this
			};
			return j
		} else {
			alert(["createNodeByHtml方法参数错误", o, j])
		}
	};
	a.addToCache = function (f, h) {
		if (!f) {
			return
		}
		h = h || e.id(f);
		if (h && !e.elCache[h]) {
			e.elCache[h] = {
				el : f,
				data : {},
				events : {}

			}
		}
	};
	a.computePosition = function (B, j, A, f, x, n, y, m) {
		var G = m ? m.document : document;
		var k = e.isQuirks ? G.body.clientWidth : G.documentElement.clientWidth;
		var o = e.isQuirks ? G.body.clientHeight : G.documentElement.clientHeight;
		var u = Math.max(G.documentElement.scrollLeft, G.body.scrollLeft);
		var F = Math.max(G.documentElement.scrollTop, G.body.scrollTop);
		var D,
		C;
		x = x || "";
		var v = x.indexOf("l") !== -1;
		var p = x.indexOf("t") !== -1;
		var q = x.indexOf("r") !== -1;
		var E = x.indexOf("b") !== -1;
		if (f - F + y - o < 0) {
			if (B - u + n - k < 0) {
				return {
					x : v ? A - n : B,
					y : p ? j - y : f
				}
			} else {
				return {
					x : q ? B : A - n,
					y : p ? j - y : f
				}
			}
		}
		if (B - u + n - k < 0) {
			return {
				x : v ? A - n : B,
				y : E ? f : j - y
			}
		} else {
			return {
				x : q ? B : A - n,
				y : E ? f : j - y
			}
		}
	};
	a.computePositionEx = function (f, l) {
		var k = e.Node.getPositionEx(f);
		var j = e.Node.getSize(f);
		var h = e.Node.getSize(l);
		return a.computePosition(k.x, k.y, k.x + j.width, k.y + j.height, null, h.width, h.height, e.rootWin)
	};
	a.getFrameOffset = a.getFrameXY = function (h) {
		var j = {
			x : 0,
			y : 0
		},
		f;
		h = h || window;
		if (h.tagName) {
			if (h.tagName === "IFRAME") {
				h = h.contentWindow
			} else {
				h = h.ownerDocument.defaultView || h.ownerDocument.parentWindow
			}
		}
		while (h != h.parent && h != Zving.rootWin) {
			if (h.frameElement) {
				f = Zving.Node.getPosition(h.frameElement);
				j.x += f.x;
				j.y += f.y
			}
			sw = Math.max(h.document.body.scrollLeft, h.document.documentElement.scrollLeft);
			sh = Math.max(h.document.body.scrollTop, h.document.documentElement.scrollTop);
			j.x -= sw;
			j.y -= sh;
			h = h.parent
		}
		h = null;
		return {
			x : j.x,
			y : j.y,
			left : j.x,
			top : j.y
		}
	};
	a.getViewport = function (j) {
		var l,
		k;
		if (j) {
			if (e.isDocument(j)) {
				l = j;
				k = l.parentWindow
			} else {
				if (e.isWindow(j)) {
					l = j.document;
					k = j
				}
			}
		}
		l = l || document;
		k = k || window;
		var f,
		h;
		if (e.isIE) {
			f = l.compatMode == "BackCompat" ? l.body.clientWidth : l.documentElement.clientWidth;
			h = l.compatMode == "BackCompat" ? l.body.clientHeight : l.documentElement.clientHeight
		} else {
			f = k.innerWidth;
			h = k.innerHeight
		}
		return {
			width : f,
			height : h
		}
	};
	a.disable = function (j) {
		j = c(j);
		if (j.tagName.toLowerCase() === "form") {
			var k = j.elements;
			for (var h = 0; h < k.length; h++) {
				try {
					$(k[h]).disable()
				} catch (f) {
					alert("error Node.disable():" + k[h].outerHTML)
				}
			}
		} else {
			if (j.getAttribute("ztype") && j.getAttribute("ztype").toLowerCase() === "select") {
				e.Selector.setDisabled(j, true)
			} else {
				if ($(j).hasClass("z-btn")) {
					$(j).addClass("z-btn-disabled");
					if (j.onclick) {
						j._onclick_ = j.onclick
					}
					j.onclick = null;
					j._disabled = true
				} else {
					if ($(j).hasClass("zPushBtn")) {
						$(j).addClass("zPushBtnDisabled");
						if (j.onclick) {
							j._onclick_ = j.onclick
						}
						j.onclick = null
					} else {
						$(j).addClass("disabled");
						j.disabled = "true";
						if (j.getAttribute("ztype") && /date|time/i.test(j.getAttribute("ztype"))) {
							e.DateTime.setDisabled(j, true)
						}
					}
				}
			}
		}
	};
	a.available = function (k, f) {
		k = (k + "").match(/^#?([\w\-]+)$/)[1];
		if (!k || typeof f != "function") {
			return
		}
		var h = 1,
		j = setInterval(function () {
				if (document.getElementById(k) && (f() || 1) || ++h > 500) {
					clearInterval(j)
				}
			}, 40)
	};
	e.Dom = a
})();
(function () {
	var b = window.Zving,
	a = {};
	b.getEvent = a.getEvent = function (c, h) {
		c = c || (h || window).event;
		if (!c) {
			var f = arguments.callee.caller,
			e = 0;
			for (; f != null; e++, f = f.caller) {
				c = f.arguments[0];
				if (c && typeof c == "object" && (/Event/.test(c) || typeof c.type == "string")) {
					return c
				}
				if (e > 999) {
					return null
				}
			}
		}
		return c
	};
	b.fixEvent = a.fixEvent = function (e, k) {
		var j = k ? k.document : document;
		if (!e) {
			return null
		}
		var c = Math.max(j.documentElement.scrollLeft, j.body.scrollLeft);
		var f = Math.max(j.documentElement.scrollTop, j.body.scrollTop);
		if (typeof e.pageX === "undefined") {
			e.pageX = e.clientX + c - j.body.clientLeft
		}
		if (typeof e.pageY === "undefined") {
			e.pageY = e.clientY + f - j.body.clientTop
		}
		if (typeof e.preventDefault === "undefined") {
			e.preventDefault = function () {
				e.returnValue = false
			}
		}
		if (typeof e.stopPropagation === "undefined") {
			e.stopPropagation = function () {
				e.cancelBubble = true
			}
		}
		if (typeof e.srcElement === "undefined") {
			var h = e.target;
			while (h && h.nodeType != 1) {
				h = h.parentNode
			}
			e.srcElement = h
		}
		if (e.type == "DOMMouseScroll" && typeof e.detail != "undefined" && typeof e.wheelDelta == "undefined") {
			e.wheelDelta = -e.detail * 40
		}
		return e
	};
	b.stopEvent = a.stopEvent = function (c) {
		c = b.getEvent(c);
		if (!c) {
			return
		}
		if (c.preventDefault) {
			c.preventDefault();
			c.stopPropagation()
		}
		c.cancelBubble = true;
		c.returnValue = false
	};
	b.cancelEvent = a.cancelEvent = function (c) {
		c = b.getEvent(c);
		if (!c) {
			return
		}
		if (c.stopPropagation) {
			c.stopPropagation()
		} else {
			c.cancelBubble = true
		}
	};
	b.preventEvent = a.preventEvent = function (c) {
		c = b.getEvent(c);
		if (!c) {
			return
		}
		if (c.preventDefault) {
			c.preventDefault()
		} else {
			c.returnValue = false;
			try {
				if (c.ctrlKey || c.keyCode > 111 && c.keyCode < 124) {
					c.keyCode = -1
				}
			} catch (f) {}

		}
	};
	a.keys = {
		enter : 13,
		up : 38,
		down : 40,
		left : 37,
		right : 39,
		esc : 27,
		space : 32,
		backspace : 8,
		tab : 9,
		"delete" : 46
	};
	a.lockScroll = function (c) {
		c = c || window;
		if (b.isFirefox) {
			c.addEventListener("DOMMouseScroll", c.Zving.stopEvent, true)
		} else {
			c.document.body.attachEvent("onmousewheel", c.Zving.stopEvent)
		}
		if (c != c.parent && c != Zving.rootWin) {
			c = c.parent;
			a.lockScroll(c)
		}
	};
	a.unlockScroll = function (c) {
		c = c || window;
		if (b.isFirefox) {
			c.removeEventListener("DOMMouseScroll", c.Zving.stopEvent, true)
		} else {
			c.document.body.detachEvent("onmousewheel", c.Zving.stopEvent);
			c.document.body.detachEvent("onmousewheel", c.Zving.stopEvent)
		}
		if (c != c.parent && c != Zving.rootWin) {
			c = c.parent;
			a.unlockScroll(c)
		}
	};
	a.getEventPosition = a.getEventXY = function (c) {
		var h,
		f;
		c = b.getEvent(c);
		var j = {
			x : c.clientX,
			y : c.clientY
		};
		h = c.srcElement.ownerDocument;
		f = h.defaultView || h.parentWindow;
		var e = b.Dom.getFrameXY(f);
		j.x += e.x;
		j.y += e.y;
		return j
	};
	a.getEventPositionLocal = a.getEventPageXY = function (c) {
		c = b.getEvent(c);
		var f = {
			x : c.clientX,
			y : c.clientY
		};
		var e;
		if (b.isGecko) {
			e = c.srcElement.ownerDocument.defaultView
		} else {
			e = c.srcElement.ownerDocument.parentWindow
		}
		f.x += Math.max(e.document.body.scrollLeft, e.document.documentElement.scrollLeft);
		f.y += Math.max(e.document.body.scrollTop, e.document.documentElement.scrollTop);
		return f
	};
	b.Event = a
})();
(function () {
	var o = window.Zving,
	q = o.elCache,
	j = {},
	p = {};
	var e = [];
	function c(x) {
		var B = false,
		w = 0,
		u = e.length,
		y = false,
		A;
		if (x) {
			var v;
			if ((x.URL && x.readyState) || (x.location && x.navigator)) {
				v = (x.parentWindow || x).Zving;
				for (; w < u; ++w) {
					A = e[w];
					if (A.el == x) {
						B = A.id;
						break
					}
				}
				if (!B) {
					if (x.URL && x.readyState) {
						B = "z-document"
					} else {
						if (x.location && x.navigator) {
							B = "z-window"
						}
					}
					x.id = B;
					e.push({
						id : B,
						el : x
					});
					y = true
				}
			} else {
				if (x.tagName) {
					B = o.id(x);
					v = x.ownerDocument.parentWindow.Zving
				} else {
					alert("EventManager.addListener传入的参数类型不对" + x)
				}
			}
			if (v && v.elCache && !v.elCache[B]) {
				v.Dom.addToCache(x, B);
				if (y) {
					v.elCache[B].skipGC = true
				}
			}
			if (!q[B]) {
				o.Dom.addToCache(x, B);
				if (y) {
					q[B].skipGC = true
				}
			}
		}
		return B
	}
	function a(w, y, C, x, v, E) {
		w = o.getDom(w);
		var u = c(w),
		A = q[u] || {},
		D = A.events || {},
		B;
		if (w.attachEvent) {
			w.attachEvent(y, v)
		} else {
			w.addEventListener(y.replace(/^on/i, ""), v, false)
		}
		B = v;
		D[y] = D[y] || [];
		D[y].push([C, v, E, B, x]);
		if (w == document && y === "onmousedown") {
			j.stoppedMouseDownEvent.addListener(v)
		}
	}
	function b(v, w, u) {
		return function (y) {
			y = o.getEvent(y);
			var x = new o.Util.DelayedTask(v);
			if (!u.tasks) {
				u.tasks = []
			}
			u.tasks.push(x);
			x.delay(w.delay || 10, v, null, [y])
		}
	}
	function h(A, y, u, C, D) {
		var v = (!u || typeof u === "boolean") ? {}

		 : u,
		w = o.getDom(A),
		x;
		C = C || v.fn;
		D = D || v.scope;
		if (!w) {
			console.log(A, arguments.callee.caller.caller);
			throw 'Error listening for "' + y + '". Element "' + A + "\" doesn't exist."
		}
		var B = function (H) {
			var F;
			if (v.delegate) {
				var E = jQuery.find(v.delegate, w);
				var G = (H.srcElement || H.target);
				while (G && G != w) {
					if (E.contains(G)) {
						F = G;
						break
					}
					G = G.parentNode
				}
				if (!F) {
					return
				}
			} else {
				F = H.srcElement
			}
			if (v.stopEvent) {
				if (H.preventDefault) {
					H.preventDefault();
					H.stopPropagation()
				}
				H.cancelBubble = true;
				H.returnValue = false
			}
			if (v.preventDefault) {
				if (H.preventDefault) {
					H.preventDefault()
				}
				H.returnValue = false
			}
			if (v.stopPropagation) {
				if (H.stopPropagation) {
					H.stopPropagation()
				}
				H.cancelBubble = true
			}
			C.call(D || w, H, F, v)
		};
		if (v.delay) {
			B = b(B, v, C)
		}
		a(w, y, C, x, B, D);
		return B
	}
	function r(w, B, F, G) {
		w = o.getDom(w);
		var u = c(w),
		D = (w && q[u] && (q[u].events)[B]) || [],
		v,
		A,
		x,
		y,
		C,
		E;
		for (A = 0, C = D.length; A < C; A++) {
			if (o.isArray(E = D[A]) && E[0] === F && (!G || E[2] === G)) {
				if (E[4]) {
					E[4].cancel()
				}
				y = F.tasks && F.tasks.length;
				if (y) {
					while (y--) {
						F.tasks[y].cancel()
					}
					delete F.tasks
				}
				v = E[1];
				if (w.detachEvent) {
					w.detachEvent(B, E[3] || v)
				} else {
					w.removeEventListener(B.replace(/^on/i, ""), E[3] || v, false)
				}
				if (v && w == document && B === "onmousedown") {
					j.stoppedMouseDownEvent.removeListener(v)
				}
				D.splice(A, 1);
				if (D.length === 0) {
					delete q[u].events[B]
				}
				for (y in q[u].events) {
					if (q[u].events.hasOwnProperty(y)) {
						return false
					}
				}
				q[u].events = {};
				return false
			}
		}
	}
	j.on = j.addListener = function (w, u, y, x, v) {
		u = /^on/i.test(u) ? u : "on" + u;
		h(w, u, v, y, x)
	};
	j.un = j.removeListener = function (v, u, x, w) {
		u = /^on/i.test(u) ? u : "on" + u;
		r(v, u, x, w)
	};
	j.removeAll = function (w) {
		w = o.getDom(w);
		var v = c(w),
		C = q[v] || {},
		F = C.events || {},
		B,
		A,
		D,
		y,
		E,
		x,
		u;
		for (y in F) {
			if (F.hasOwnProperty(y)) {
				B = F[y];
				for (A = 0, D = B.length; A < D; A++) {
					E = B[A];
					if (E[4]) {
						E[4].cancel()
					}
					if (E[0].tasks && E[0].tasks.length) {
						x = E[0].tasks.length;
						while (x--) {
							E[0].tasks[x].cancel()
						}
						delete E.tasks
					}
					u = E[1];
					if (w.detachEvent) {
						w.detachEvent(y, E[3] || u)
					} else {
						w.removeEventListener(y.replace(/^on/i, ""), fnc[3] || u, false)
					}
					if (u && w == document && y === "onmousedown") {
						j.stoppedMouseDownEvent.removeListener(u)
					}
				}
			}
		}
		if (q[v]) {
			q[v].events = {}

		}
	};
	j.getListeners = function (x, u) {
		x = o.getDom(x);
		var A = c(x),
		v = q[A] || {},
		y = v.events || {},
		w = [];
		if (y && y[u]) {
			return y[u]
		} else {
			return null
		}
	};
	j.purgeElement = function (w, u, y) {
		w = o.getDom(w);
		var v = c(w),
		C = q[v] || {},
		D = C.events || {},
		x,
		B,
		A;
		if (y) {
			if (D && D.hasOwnProperty(y)) {
				B = D[y];
				for (x = 0, A = B.length; x < A; x++) {
					j.removeListener(w, y, B[x][0])
				}
			}
		} else {
			j.removeAll(w)
		}
		if (u && w && w.childNodes) {
			for (x = 0, A = w.childNodes.length; x < A; x++) {
				j.purgeElement(w.childNodes[x], u, y)
			}
		}
	};
	j._unload = function () {
		var u;
		for (u in o.elCache) {
			if (o.elCache.hasOwnProperty(u) && u.el != window) {
				j.removeAll(u)
			}
		}
	};
	j.stoppedMouseDownEvent = new o.CustEvent();
	var l,
	t,
	n = 0,
	m = 0;
	j.doResizeEvent = function () {
		var v = o.Dom.getViewport(),
		x = v.height,
		u = v.width;
		if (m !== x || n !== u) {
			l.fire(n = u, m = x)
		}
	};
	j.onWindowResize = function (w, v, u) {
		if (!l) {
			l = new o.CustEvent();
			t = new o.Util.DelayedTask(this.doResizeEvent);
			$(window).on("resize", this.fireWindowResize)
		}
		l.addListener(w, v, u)
	};
	j.fireWindowResize = function () {
		if (l) {
			t.delay(100)
		}
	};
	j.removeResizeListener = function (v, u) {
		if (l) {
			l.removeListener(v, u)
		}
	};
	var f = window,
	k = document;
	if (window != o.rootWin) {
		f = o.rootWin;
		k = o.rootWin.document
	}
	p.allEvents = {};
	p.on = function (u, v) {
		f.$(k).on("alldocments_" + u + "." + o.pageId, v);
		if (!p.allEvents[u]) {
			p.allEvents[u] = true
		}
	};
	p.un = p.off = function (u, v) {
		f.$(k).off("alldocments_" + u + "." + o.pageId, v)
	};
	p.fire = p.trigger = function (u) {
		if (typeof u == "string") {
			u = "alldocments_" + u
		} else {
			u.type = "alldocments_" + u.type
		}
		f.$(k).trigger(u)
	};
	p._unload = function () {
		for (var u in p.allEvents) {
			f.$(k).off("alldocments_" + u + "." + o.pageId)
		}
	};
	$(document).on("click", p.trigger);
	$(document).on("mousedown", p.trigger);
	$(document).on("mouseup", p.trigger);
	o.EventManager = j;
	o.AllDocumentsEvent = p
})();
/*! Copyright (c) 2011 Brandon Aaron (http://brandonaaron.net)
 * Licensed under the MIT License (LICENSE.txt).
 *
 * Thanks to: http://adomas.org/javascript-mouse-wheel/ for some pointers.
 * Thanks to: Mathias Bank(http://www.mathias-bank.de) for a scope bug fix.
 * Thanks to: Seamus Leahy for adding deltaX and deltaY
 *
 * Version: 3.0.6
 *
 * Requires: 1.2.2+
 */
(function (e) {
	var b = ["DOMMouseScroll", "mousewheel"];
	if (e.event.fixHooks) {
		for (var a = b.length; a; ) {
			e.event.fixHooks[b[--a]] = e.event.mouseHooks
		}
	}
	e.event.special.mousewheel = {
		setup : function () {
			if (this.addEventListener) {
				for (var f = b.length; f; ) {
					this.addEventListener(b[--f], c, false)
				}
			} else {
				this.onmousewheel = c
			}
		},
		teardown : function () {
			if (this.removeEventListener) {
				for (var f = b.length; f; ) {
					this.removeEventListener(b[--f], c, false)
				}
			} else {
				this.onmousewheel = null
			}
		}
	};
	e.fn.extend({
		mousewheel : function (f) {
			return f ? this.bind("mousewheel", f) : this.trigger("mousewheel")
		},
		unmousewheel : function (f) {
			return this.unbind("mousewheel", f)
		}
	});
	function c(m) {
		var k = m || window.event,
		j = [].slice.call(arguments, 1),
		n = 0,
		l = true,
		h = 0,
		f = 0;
		m = e.event.fix(k);
		m.type = "mousewheel";
		if (k.wheelDelta) {
			n = k.wheelDelta / 120
		}
		if (k.detail) {
			n = -k.detail / 3
		}
		f = n;
		if (k.axis !== undefined && k.axis === k.HORIZONTAL_AXIS) {
			f = 0;
			h = -1 * n
		}
		if (k.wheelDeltaY !== undefined) {
			f = k.wheelDeltaY / 120
		}
		if (k.wheelDeltaX !== undefined) {
			h = -1 * k.wheelDeltaX / 120
		}
		j.unshift(m, n, h, f);
		return (e.event.dispatch || e.event.handle).apply(this, j)
	}
})(jQuery);
(function (a) {
	a.fn.hoverIntent = function (m, l) {
		var n = {
			sensitivity : 7,
			interval : 100,
			timeout : 0
		};
		n = a.extend(n, l ? {
				over : m,
				out : l
			}
				 : m);
		var p,
		o,
		j,
		e;
		var h = function (f) {
			p = f.pageX;
			o = f.pageY
		};
		var c = function (q, f) {
			f.hoverIntent_t = clearTimeout(f.hoverIntent_t);
			if ((Math.abs(j - p) + Math.abs(e - o)) < n.sensitivity) {
				a(f).unbind("mousemove", h);
				f.hoverIntent_s = 1;
				return n.over.apply(f, [q])
			} else {
				j = p;
				e = o;
				f.hoverIntent_t = setTimeout(function () {
						c(q, f)
					}, n.interval)
			}
		};
		var k = function (q, f) {
			f.hoverIntent_t = clearTimeout(f.hoverIntent_t);
			f.hoverIntent_s = 0;
			return n.out.apply(f, [q])
		};
		var b = function (r) {
			var q = jQuery.extend({}, r);
			var f = this;
			if (f.hoverIntent_t) {
				f.hoverIntent_t = clearTimeout(f.hoverIntent_t)
			}
			if (r.type == "mouseenter") {
				j = q.pageX;
				e = q.pageY;
				a(f).bind("mousemove", h);
				if (f.hoverIntent_s != 1) {
					f.hoverIntent_t = setTimeout(function () {
							c(q, f)
						}, n.interval)
				}
			} else {
				a(f).unbind("mousemove", h);
				if (f.hoverIntent_s == 1) {
					f.hoverIntent_t = setTimeout(function () {
							k(q, f)
						}, n.timeout)
				}
			}
		};
		return this.bind("mouseenter", b).bind("mouseleave", b)
	}
})(jQuery);
(function () {
	var a = jQuery.event.special,
	c = "D" + (+new Date()),
	b = "D" + (+new Date() + 1);
	a.scrollstart = {
		setup : function () {
			var f,
			e = function (k) {
				var h = this,
				j = arguments;
				if (f) {
					clearTimeout(f)
				} else {
					k.type = "scrollstart";
					jQuery.event.handle.apply(h, j)
				}
				f = setTimeout(function () {
						f = null
					}, a.scrollstop.latency)
			};
			jQuery(this).bind("scroll", e).data(c, e)
		},
		teardown : function () {
			jQuery(this).unbind("scroll", jQuery(this).data(c))
		}
	};
	a.scrollstop = {
		latency : 300,
		setup : function () {
			var f,
			e = function (k) {
				var h = this,
				j = arguments;
				if (f) {
					clearTimeout(f)
				}
				f = setTimeout(function () {
						f = null;
						k.type = "scrollstop";
						jQuery.event.handle.apply(h, j)
					}, a.scrollstop.latency)
			};
			jQuery(this).bind("scroll", e).data(b, e)
		},
		teardown : function () {
			jQuery(this).unbind("scroll", jQuery(this).data(b))
		}
	}
})();
(function (b) {
	var a = b([]),
	e;
	b.event.special.resize = {
		setup : function () {
			var f = b(this);
			a = a.add(f);
			f.data("resize", {
				w : f.width(),
				h : f.height()
			});
			if (a.length === 1) {
				c()
			}
		},
		teardown : function () {
			var f = b(this);
			a = a.not(f);
			f.removeData("resize");
			if (!a.length) {
				clearTimeout(e)
			}
		}
	};
	function c() {
		a.each(function () {
			var j = b(this),
			h = j.width(),
			f = j.height(),
			k = j.data("resize");
			if (h !== k.w || f !== k.h) {
				k.w = h;
				k.h = f;
				j.triggerHandler("resize")
			}
		});
		e = setTimeout(c, 250)
	}
})(jQuery);
(function (c) {
	var b = location.hash,
	f;
	c.event.special.hashchange = {
		setup : function () {
			if ("onhashchange" in window) {
				return false
			}
			e()
		},
		teardown : function () {
			if ("onhashchange" in window) {
				return false
			}
			a()
		},
		add : function (h) {
			var j = h.handler;
			h.handler = function (k) {
				k.fragment = location.hash.replace(/^#/, "");
				j.apply(this, arguments)
			}
		}
	};
	function e() {
		a();
		var h = location.hash;
		if (h !== b) {
			c(window).trigger("hashchange");
			b = h
		}
		f = setTimeout(e, 100)
	}
	function a() {
		clearTimeout(f)
	}
})(jQuery);
(function (c) {
	var a = c([]);
	c.event.special.clickoutside = {
		setup : function () {
			a = a.add(this);
			if (a.length === 1) {
				c(document).bind("click", b)
			}
		},
		teardown : function () {
			a = a.not(this);
			if (a.length === 0) {
				c(document).unbind("click", b)
			}
		},
		add : function (e) {
			var f = e.handler;
			e.handler = function (j, h) {
				j.target = h;
				f.apply(this, arguments)
			}
		}
	};
	function b(e) {
		c(a).each(function () {
			var f = c(this);
			if (this !== e.target && !f.has(e.target).length) {
				f.triggerHandler("clickoutside", [e.target])
			}
		})
	}
})(jQuery);
(function (a) {
	var b = window.Zving;
	a.fn.extend({
		getComponent : function () {
			return b.ComponentManager.getById(this[0].compId || this[0].id)
		},
		delayFocus : function (c, e) {
			return typeof c === "number" ? this.each(function () {
				var f = this;
				setTimeout(function () {
					a(f).focus();
					if (e) {
						e.call(f)
					}
				}, c)
			}) : this.focus.apply(this, arguments)
		},
		enableSelection : function () {
			return this.attr("unselectable", "off").css("MozUserSelect", "").off("selectstart.ui")
		},
		disableSelection : function () {
			return this.attr("unselectable", "on").css("MozUserSelect", "none").on("selectstart.ui", function () {
				return false
			})
		},
		disable : function (e) {
			var c = this;
			if (e) {
				setTimeout(function () {
					c.enable()
				}, e * 1000)
			}
			return this.each(function () {
				var j = this;
				if (j.tagName.toLowerCase() === "form") {
					var k = j.elements;
					for (var h = 0; h < k.length; h++) {
						a(k[h]).disable()
					}
				} else {
					if (j.getAttribute("ztype")) {
						var f = j.getAttribute("ztype");
						if (/select/i.test(f)) {
							b.Selector.setDisabled(j, true)
						} else {
							if (/date|time/i.test(f)) {
								b.DateTime.setDisabled(j, true)
							} else {
								if (j.compId) {
									a(j).getComponent().disable()
								}
							}
						}
					} else {
						a(j).addClass("disabled");
						j.disabled = true;
						j.setAttribute("disabled", "true")
					}
				}
			})
		},
		enable : function () {
			return this.each(function () {
				var f = this;
				if (f.tagName.toLowerCase() === "form") {
					var h = f.elements;
					for (var e = 0; e < h.length; e++) {
						a(h[e]).enable()
					}
				} else {
					if (f.getAttribute("ztype")) {
						var c = f.getAttribute("ztype");
						if (/select/i.test(c)) {
							b.Selector.setDisabled(f, false)
						} else {
							if (/date|time/i.test(c)) {
								b.DateTime.setDisabled(f, false)
							} else {
								if (f.compId) {
									a(f).getComponent().enable()
								}
							}
						}
					} else {
						a(f).removeClass("disabled");
						f.setAttribute("disabled", "");
						f.disabled = false
					}
				}
			})
		},
		scrollParent : function () {
			var c;
			if ((a.browser.msie && (/(static|relative)/).test(this.css("position"))) || (/absolute/).test(this.css("position"))) {
				c = this.parents().filter(function () {
						return (/(relative|absolute|fixed)/).test(a.curCSS(this, "position", 1)) && (/(auto|scroll)/).test(a.curCSS(this, "overflow", 1) + a.curCSS(this, "overflow-y", 1) + a.curCSS(this, "overflow-x", 1))
					}).eq(0)
			} else {
				c = this.parents().filter(function () {
						return (/(auto|scroll)/).test(a.curCSS(this, "overflow", 1) + a.curCSS(this, "overflow-y", 1) + a.curCSS(this, "overflow-x", 1))
					}).eq(0)
			}
			return (/fixed/).test(this.css("position")) || !c.length ? a(document) : c
		},
		zIndex : function (h) {
			if (h !== undefined) {
				return this.css("zIndex", h)
			}
			if (this.length) {
				var e = a(this[0]),
				c,
				f;
				while (e.length && e[0] !== document) {
					c = e.css("position");
					if (c == "absolute" || c == "relative" || c == "fixed") {
						f = parseInt(e.css("zIndex"), 10);
						if (!isNaN(f) && f !== 0) {
							return f
						}
					}
					e = e.getParent()
				}
			}
			return 0
		},
		mask : function (o) {
			this.unmask();
			var l = a.extend({
					opacity : 0.5,
					msg : "",
					z : 10000,
					maskDivClass : "",
					bgcolor : "#ccc"
				}, o);
			var f = a(document.body);
			var k = {
				top : 0,
				left : 0
			};
			if (this[0] && this[0] !== window.document) {
				f = this;
				k = f.position()
			}
			var e = a('<div class="maskdivgen">&nbsp;</div>');
			e.appendTo(f);
			var n = f.outerWidth();
			if (!n) {
				n = f.width()
			}
			var c = f.outerHeight();
			if (!c) {
				c = f.height()
			}
			e.css({
				position : "absolute",
				top : k.top,
				left : k.left,
				"z-index" : l.z,
				width : n,
				height : c,
				"background-color" : l.bgcolor,
				opacity : 0
			});
			if (l.maskDivClass) {
				e.addClass(l.maskDivClass)
			}
			if (l.msg) {
				var m = a('<div style="position:absolute;border:#6593cf 1px solid; padding:2px;background:#ccca"><div style="line-height:24px;border:#a3bad9 1px solid;background:white;padding:2px 10px 2px 10px">' + l.msg + "</div></div>");
				m.appendTo(e);
				var j = (e.width() - m.width());
				var h = (e.height() - m.height());
				m.css({
					cursor : "wait",
					top : (h / 2 - 2),
					left : (j / 2 - 2)
				})
			}
			e.fadeIn("fast", function () {
				a(this).fadeTo("slow", l.opacity)
			});
			return e
		},
		unmask : function () {
			var c = a(document.body);
			if (this[0] && this[0] !== window.document) {
				c = a(this[0])
			}
			c.find("> div.maskdivgen").fadeOut("slow", 0, function () {
				a(this).remove()
			})
		},
		offsetEx : function () {
			var e = a(this).offset();
			var c = b.Dom.getFrameOffset();
			e.left += c.left;
			e.top += c.top;
			return e
		}
	})
})(jQuery);
/*! jQuery UI - v1.8.20 - 2012-04-30
 * https://github.com/jquery/jquery-ui
 * Includes: jquery.ui.core.js
 * Copyright (c) 2012 AUTHORS.txt; Licensed MIT, GPL */
(function (f, e) {
	function j(a, o) {
		var n = a.nodeName.toLowerCase();
		if ("area" === n) {
			var m = a.parentNode,
			l = m.name,
			k;
			return !a.href || !l || m.nodeName.toLowerCase() !== "map" ? !1 : (k = f("img[usemap=#" + l + "]")[0], !!k && h(k))
		}
		return (/input|select|textarea|button|object/.test(n) ? !a.disabled : "a" == n ? a.href || o : o) && h(a)
	}
	function h(a) {
		return !f(a).parents().andSelf().filter(function () {
			return f.curCSS(this, "visibility") === "hidden" || f.expr.filters.hidden(this)
		}).length
	}
	f.ui = f.ui || {};
	if (f.ui.version) {
		return
	}
	f.extend(f.ui, {
		version : "1.8.20",
		keyCode : {
			ALT : 18,
			BACKSPACE : 8,
			CAPS_LOCK : 20,
			COMMA : 188,
			COMMAND : 91,
			COMMAND_LEFT : 91,
			COMMAND_RIGHT : 93,
			CONTROL : 17,
			DELETE : 46,
			DOWN : 40,
			END : 35,
			ENTER : 13,
			ESCAPE : 27,
			HOME : 36,
			INSERT : 45,
			LEFT : 37,
			MENU : 93,
			NUMPAD_ADD : 107,
			NUMPAD_DECIMAL : 110,
			NUMPAD_DIVIDE : 111,
			NUMPAD_ENTER : 108,
			NUMPAD_MULTIPLY : 106,
			NUMPAD_SUBTRACT : 109,
			PAGE_DOWN : 34,
			PAGE_UP : 33,
			PERIOD : 190,
			RIGHT : 39,
			SHIFT : 16,
			SPACE : 32,
			TAB : 9,
			UP : 38,
			WINDOWS : 91
		}
	}),
	f.fn.extend({
		propAttr : f.fn.prop || f.fn.attr,
		_focus : f.fn.focus,
		focus : function (a, k) {
			return typeof a == "number" ? this.each(function () {
				var b = this;
				setTimeout(function () {
					f(b).focus(),
					k && k.call(b)
				}, a)
			}) : this._focus.apply(this, arguments)
		},
		scrollParent : function () {
			var a;
			return f.browser.msie && /(static|relative)/.test(this.css("position")) || /absolute/.test(this.css("position")) ? a = this.parents().filter(function () {
					return /(relative|absolute|fixed)/.test(f.curCSS(this, "position", 1)) && /(auto|scroll)/.test(f.curCSS(this, "overflow", 1) + f.curCSS(this, "overflow-y", 1) + f.curCSS(this, "overflow-x", 1))
				}).eq(0) : a = this.parents().filter(function () {
					return /(auto|scroll)/.test(f.curCSS(this, "overflow", 1) + f.curCSS(this, "overflow-y", 1) + f.curCSS(this, "overflow-x", 1))
				}).eq(0),
			/fixed/.test(this.css("position")) || !a.length ? f(document) : a
		},
		zIndex : function (l) {
			if (l !== e) {
				return this.css("zIndex", l)
			}
			if (this.length) {
				var k = f(this[0]),
				b,
				a;
				while (k.length && k[0] !== document) {
					b = k.css("position");
					if (b === "absolute" || b === "relative" || b === "fixed") {
						a = parseInt(k.css("zIndex"), 10);
						if (!isNaN(a) && a !== 0) {
							return a
						}
					}
					k = k.parent()
				}
			}
			return 0
		},
		disableSelection : function () {
			return this.bind((f.support.selectstart ? "selectstart" : "mousedown") + ".ui-disableSelection", function (b) {
				b.preventDefault()
			})
		},
		enableSelection : function () {
			return this.unbind(".ui-disableSelection")
		}
	}),
	f.each(["Width", "Height"], function (n, m) {
		function a(o, r, q, p) {
			return f.each(l, function () {
				r -= parseFloat(f.curCSS(o, "padding" + this, !0)) || 0,
				q && (r -= parseFloat(f.curCSS(o, "border" + this + "Width", !0)) || 0),
				p && (r -= parseFloat(f.curCSS(o, "margin" + this, !0)) || 0)
			}),
			r
		}
		var l = m === "Width" ? ["Left", "Right"] : ["Top", "Bottom"],
		k = m.toLowerCase(),
		b = {
			innerWidth : f.fn.innerWidth,
			innerHeight : f.fn.innerHeight,
			outerWidth : f.fn.outerWidth,
			outerHeight : f.fn.outerHeight
		};
		f.fn["inner" + m] = function (o) {
			return o === e ? b["inner" + m].call(this) : this.each(function () {
				f(this).css(k, a(this, o) + "px")
			})
		},
		f.fn["outer" + m] = function (o, p) {
			return typeof o != "number" ? b["outer" + m].call(this, o) : this.each(function () {
				f(this).css(k, a(this, o, !0, p) + "px")
			})
		}
	}),
	f.extend(f.expr[":"], {
		data : function (a, l, k) {
			return !!f.data(a, k[3])
		},
		focusable : function (a) {
			return j(a, !isNaN(f.attr(a, "tabindex")))
		},
		tabbable : function (a) {
			var k = f.attr(a, "tabindex"),
			c = isNaN(k);
			return (c || k >= 0) && j(a, !c)
		}
	}),
	f(function () {
		var a = document.body,
		k = a.appendChild(k = document.createElement("div"));
		k.offsetHeight,
		f.extend(k.style, {
			minHeight : "100px",
			height : "auto",
			padding : 0,
			borderWidth : 0
		}),
		f.support.minHeight = k.offsetHeight === 100,
		f.support.selectstart = "onselectstart" in k,
		a.removeChild(k).style.display = "none"
	}),
	f.extend(f.ui, {
		plugin : {
			add : function (a, n, m) {
				var l = f.ui[a].prototype;
				for (var k in m) {
					l.plugins[k] = l.plugins[k] || [],
					l.plugins[k].push([n, m[k]])
				}
			},
			call : function (l, k, o) {
				var n = l.plugins[k];
				if (!n || !l.element[0].parentNode) {
					return
				}
				for (var m = 0; m < n.length; m++) {
					l.options[n[m][0]] && n[m][1].apply(l.element, o)
				}
			}
		},
		contains : function (k, c) {
			return document.compareDocumentPosition ? k.compareDocumentPosition(c) & 16 : k !== c && k.contains(c)
		},
		hasScroll : function (a, m) {
			if (f(a).css("overflow") === "hidden") {
				return !1
			}
			var l = m && m === "left" ? "scrollLeft" : "scrollTop",
			k = !1;
			return a[l] > 0 ? !0 : (a[l] = 1, k = a[l] > 0, a[l] = 0, k)
		},
		isOverAxis : function (l, k, m) {
			return l > k && l < k + m
		},
		isOver : function (a, o, n, m, l, k) {
			return f.ui.isOverAxis(a, n, l) && f.ui.isOverAxis(o, m, k)
		}
	})
})(jQuery);
/*! jQuery UI - v1.8.20 - 2012-04-30
 * https://github.com/jquery/jquery-ui
 * Includes: jquery.ui.widget.js
 * Copyright (c) 2012 AUTHORS.txt; Licensed MIT, GPL */
(function (f, e) {
	if (f.cleanData) {
		var j = f.cleanData;
		f.cleanData = function (a) {
			for (var l = 0, k; (k = a[l]) != null; l++) {
				try {
					f(k).triggerHandler("remove")
				} catch (c) {}

			}
			j(a)
		}
	} else {
		var h = f.fn.remove;
		f.fn.remove = function (a, k) {
			return this.each(function () {
				return k || (!a || f.filter(a, [this]).length) && f("*", this).add([this]).each(function () {
					try {
						f(this).triggerHandler("remove")
					} catch (c) {}

				}),
				h.call(f(this), a, k)
			})
		}
	}
	f.widget = function (a, o, n) {
		var m = a.split(".")[0],
		l;
		a = a.split(".")[1],
		l = m + "-" + a,
		n || (n = o, o = f.Widget),
		f.expr[":"][l] = function (b) {
			return !!f.data(b, a)
		},
		f[m] = f[m] || {},
		f[m][a] = function (p, c) {
			arguments.length && this._createWidget(p, c)
		};
		var k = new o;
		k.options = f.extend(!0, {}, k.options),
		f[m][a].prototype = f.extend(!0, k, {
				namespace : m,
				widgetName : a,
				widgetEventPrefix : f[m][a].prototype.widgetEventPrefix || a,
				widgetBaseClass : l
			}, n),
		f.widget.bridge(a, f[m][a])
	},
	f.widget.bridge = function (b, a) {
		f.fn[b] = function (m) {
			var l = typeof m == "string",
			k = Array.prototype.slice.call(arguments, 1),
			c = this;
			return m = !l && k.length ? f.extend.apply(null, [!0, m].concat(k)) : m,
			l && m.charAt(0) === "_" ? c : (l ? this.each(function () {
					var o = f.data(this, b),
					n = o && f.isFunction(o[m]) ? o[m].apply(o, k) : o;
					if (n !== o && n !== e) {
						return c = n,
						!1
					}
				}) : this.each(function () {
					var n = f.data(this, b);
					n ? n.option(m || {})._init() : f.data(this, b, new a(m, this))
				}), c)
		}
	},
	f.Widget = function (k, c) {
		arguments.length && this._createWidget(k, c)
	},
	f.Widget.prototype = {
		widgetName : "widget",
		widgetEventPrefix : "",
		options : {
			disabled : !1
		},
		_createWidget : function (a, l) {
			f.data(l, this.widgetName, this),
			this.element = f(l),
			this.options = f.extend(!0, {}, this.options, this._getCreateOptions(), a);
			var k = this;
			this.element.bind("remove." + this.widgetName, function () {
				k.destroy()
			}),
			this._create(),
			this._trigger("create"),
			this._init()
		},
		_getCreateOptions : function () {
			return f.metadata && f.metadata.get(this.element[0])[this.widgetName]
		},
		_create : function () {},
		_init : function () {},
		destroy : function () {
			this.element.unbind("." + this.widgetName).removeData(this.widgetName),
			this.widget().unbind("." + this.widgetName).removeAttr("aria-disabled").removeClass(this.widgetBaseClass + "-disabled ui-state-disabled")
		},
		widget : function () {
			return this.element
		},
		option : function (k, b) {
			var a = k;
			if (arguments.length === 0) {
				return f.extend({}, this.options)
			}
			if (typeof k == "string") {
				if (b === e) {
					return this.options[k]
				}
				a = {},
				a[k] = b
			}
			return this._setOptions(a),
			this
		},
		_setOptions : function (a) {
			var k = this;
			return f.each(a, function (l, c) {
				k._setOption(l, c)
			}),
			this
		},
		_setOption : function (k, c) {
			return this.options[k] = c,
			k === "disabled" && this.widget()[c ? "addClass" : "removeClass"](this.widgetBaseClass + "-disabled ui-state-disabled").attr("aria-disabled", c),
			this
		},
		enable : function () {
			return this._setOption("disabled", !1)
		},
		disable : function () {
			return this._setOption("disabled", !0)
		},
		_trigger : function (a, o, n) {
			var m,
			l,
			k = this.options[a];
			n = n || {},
			o = f.Event(o),
			o.type = (a === this.widgetEventPrefix ? a : this.widgetEventPrefix + a).toLowerCase(),
			o.target = this.element[0],
			l = o.originalEvent;
			if (l) {
				for (m in l) {
					m in o || (o[m] = l[m])
				}
			}
			return this.element.trigger(o, n),
			!(f.isFunction(k) && k.call(this.element[0], o, n) === !1 || o.isDefaultPrevented())
		}
	}
})(jQuery);
/*! jQuery UI - v1.8.20 - 2012-04-30
 * https://github.com/jquery/jquery-ui
 * Includes: jquery.ui.mouse.js
 * Copyright (c) 2012 AUTHORS.txt; Licensed MIT, GPL */
(function (f, e) {
	var h = !1;
	f(document).mouseup(function (b) {
		h = !1
	}),
	f.widget("ui.mouse", {
		options : {
			cancel : ":input,option",
			distance : 1,
			delay : 0
		},
		_mouseInit : function () {
			var a = this;
			this.element.bind("mousedown." + this.widgetName, function (b) {
				return a._mouseDown(b)
			}).bind("click." + this.widgetName, function (b) {
				if (!0 === f.data(b.target, a.widgetName + ".preventClickEvent")) {
					return f.removeData(b.target, a.widgetName + ".preventClickEvent"),
					b.stopImmediatePropagation(),
					!1
				}
			}),
			this.started = !1
		},
		_mouseDestroy : function () {
			this.element.unbind("." + this.widgetName),
			f(document).unbind("mousemove." + this.widgetName, this._mouseMoveDelegate).unbind("mouseup." + this.widgetName, this._mouseUpDelegate)
		},
		_mouseDown : function (a) {
			if (h) {
				return
			}
			this._mouseStarted && this._mouseUp(a),
			this._mouseDownEvent = a;
			var k = this,
			j = a.which == 1,
			c = typeof this.options.cancel == "string" && a.target.nodeName ? f(a.target).closest(this.options.cancel).length : !1;
			if (!j || c || !this._mouseCapture(a)) {
				return !0
			}
			this.mouseDelayMet = !this.options.delay,
			this.mouseDelayMet || (this._mouseDelayTimer = setTimeout(function () {
						k.mouseDelayMet = !0
					}, this.options.delay));
			if (this._mouseDistanceMet(a) && this._mouseDelayMet(a)) {
				this._mouseStarted = this._mouseStart(a) !== !1;
				if (!this._mouseStarted) {
					return a.preventDefault(),
					!0
				}
			}
			return !0 === f.data(a.target, this.widgetName + ".preventClickEvent") && f.removeData(a.target, this.widgetName + ".preventClickEvent"),
			this._mouseMoveDelegate = function (b) {
				return k._mouseMove(b)
			},
			this._mouseUpDelegate = function (b) {
				return k._mouseUp(b)
			},
			f(document).bind("mousemove." + this.widgetName, this._mouseMoveDelegate).bind("mouseup." + this.widgetName, this._mouseUpDelegate),
			a.preventDefault(),
			h = !0,
			!0
		},
		_mouseMove : function (a) {
			return !f.browser.msie || document.documentMode >= 9 || !!a.button ? this._mouseStarted ? (this._mouseDrag(a), a.preventDefault()) : (this._mouseDistanceMet(a) && this._mouseDelayMet(a) && (this._mouseStarted = this._mouseStart(this._mouseDownEvent, a) !== !1, this._mouseStarted ? this._mouseDrag(a) : this._mouseUp(a)), !this._mouseStarted) : this._mouseUp(a)
		},
		_mouseUp : function (a) {
			return f(document).unbind("mousemove." + this.widgetName, this._mouseMoveDelegate).unbind("mouseup." + this.widgetName, this._mouseUpDelegate),
			this._mouseStarted && (this._mouseStarted = !1, a.target == this._mouseDownEvent.target && f.data(a.target, this.widgetName + ".preventClickEvent", !0), this._mouseStop(a)),
			!1
		},
		_mouseDistanceMet : function (b) {
			return Math.max(Math.abs(this._mouseDownEvent.pageX - b.pageX), Math.abs(this._mouseDownEvent.pageY - b.pageY)) >= this.options.distance
		},
		_mouseDelayMet : function (b) {
			return this.mouseDelayMet
		},
		_mouseStart : function (b) {},
		_mouseDrag : function (b) {},
		_mouseStop : function (b) {},
		_mouseCapture : function (b) {
			return !0
		}
	})
})(jQuery);
/*! jQuery UI - v1.8.20 - 2012-04-30
 * https://github.com/jquery/jquery-ui
 * Includes: jquery.ui.position.js
 * Copyright (c) 2012 AUTHORS.txt; Licensed MIT, GPL */
(function (k, j) {
	k.ui = k.ui || {};
	var q = /left|center|right/,
	p = /top|center|bottom/,
	o = "center",
	n = {},
	m = k.fn.position,
	l = k.fn.offset;
	k.fn.position = function (c) {
		if (!c || !c.of) {
			return m.apply(this, arguments)
		}
		c = k.extend({}, c);
		var u = k(c.of),
		t = u[0],
		r = (c.collision || "flip").split(" "),
		f = c.offset ? c.offset.split(" ") : [0, 0],
		e,
		a,
		v;
		return t.nodeType === 9 ? (e = u.width(), a = u.height(), v = {
				top : 0,
				left : 0
			}) : t.setTimeout ? (e = u.width(), a = u.height(), v = {
				top : u.scrollTop(),
				left : u.scrollLeft()
			}) : t.preventDefault ? (c.at = "left top", e = a = 0, v = {
				top : c.of.pageY,
				left : c.of.pageX
			}) : (e = u.outerWidth(), a = u.outerHeight(), v = u.offset()),
		k.each(["my", "at"], function () {
			var b = (c[this] || "").split(" ");
			b.length === 1 && (b = q.test(b[0]) ? b.concat([o]) : p.test(b[0]) ? [o].concat(b) : [o, o]),
			b[0] = q.test(b[0]) ? b[0] : o,
			b[1] = p.test(b[1]) ? b[1] : o,
			c[this] = b
		}),
		r.length === 1 && (r[1] = r[0]),
		f[0] = parseInt(f[0], 10) || 0,
		f.length === 1 && (f[1] = f[0]),
		f[1] = parseInt(f[1], 10) || 0,
		c.at[0] === "right" ? v.left += e : c.at[0] === o && (v.left += e / 2),
		c.at[1] === "bottom" ? v.top += a : c.at[1] === o && (v.top += a / 2),
		v.left += f[0],
		v.top += f[1],
		this.each(function () {
			var E = k(this),
			D = E.outerWidth(),
			C = E.outerHeight(),
			B = parseInt(k.curCSS(this, "marginLeft", !0)) || 0,
			A = parseInt(k.curCSS(this, "marginTop", !0)) || 0,
			y = D + B + (parseInt(k.curCSS(this, "marginRight", !0)) || 0),
			x = C + A + (parseInt(k.curCSS(this, "marginBottom", !0)) || 0),
			w = k.extend({}, v),
			b;
			c.my[0] === "right" ? w.left -= D : c.my[0] === o && (w.left -= D / 2),
			c.my[1] === "bottom" ? w.top -= C : c.my[1] === o && (w.top -= C / 2),
			n.fractions || (w.left = Math.round(w.left), w.top = Math.round(w.top)),
			b = {
				left : w.left - B,
				top : w.top - A
			},
			k.each(["left", "top"], function (F, h) {
				k.ui.position[r[F]] && k.ui.position[r[F]][h](w, {
					targetWidth : e,
					targetHeight : a,
					elemWidth : D,
					elemHeight : C,
					collisionPosition : b,
					collisionWidth : y,
					collisionHeight : x,
					offset : f,
					my : c.my,
					at : c.at
				})
			}),
			k.fn.bgiframe && E.bgiframe(),
			E.offset(k.extend(w, {
					using : c.using
				}))
		})
	},
	k.ui.position = {
		fit : {
			left : function (a, r) {
				var h = k(window),
				f = r.collisionPosition.left + r.collisionWidth - h.width() - h.scrollLeft();
				a.left = f > 0 ? a.left - f : Math.max(a.left - r.collisionPosition.left, a.left)
			},
			top : function (a, r) {
				var h = k(window),
				f = r.collisionPosition.top + r.collisionHeight - h.height() - h.scrollTop();
				a.top = f > 0 ? a.top - f : Math.max(a.top - r.collisionPosition.top, a.top)
			}
		},
		flip : {
			left : function (a, w) {
				if (w.at[0] === o) {
					return
				}
				var v = k(window),
				u = w.collisionPosition.left + w.collisionWidth - v.width() - v.scrollLeft(),
				t = w.my[0] === "left" ? -w.elemWidth : w.my[0] === "right" ? w.elemWidth : 0,
				r = w.at[0] === "left" ? w.targetWidth : -w.targetWidth,
				e = -2 * w.offset[0];
				a.left += w.collisionPosition.left < 0 ? t + r + e : u > 0 ? t + r + e : 0
			},
			top : function (a, w) {
				if (w.at[1] === o) {
					return
				}
				var v = k(window),
				u = w.collisionPosition.top + w.collisionHeight - v.height() - v.scrollTop(),
				t = w.my[1] === "top" ? -w.elemHeight : w.my[1] === "bottom" ? w.elemHeight : 0,
				r = w.at[1] === "top" ? w.targetHeight : -w.targetHeight,
				e = -2 * w.offset[1];
				a.top += w.collisionPosition.top < 0 ? t + r + e : u > 0 ? t + r + e : 0
			}
		}
	},
	k.offset.setOffset || (k.offset.setOffset = function (a, x) {
		/static/.test(k.curCSS(a, "position")) && (a.style.position = "relative");
		var w = k(a),
		v = w.offset(),
		u = parseInt(k.curCSS(a, "top", !0), 10) || 0,
		t = parseInt(k.curCSS(a, "left", !0), 10) || 0,
		r = {
			top : x.top - v.top + u,
			left : x.left - v.left + t
		};
		"using" in x ? x.using.call(a, r) : w.css(r)
	}, k.fn.offset = function (a) {
		var e = this[0];
		return !e || !e.ownerDocument ? null : a ? this.each(function () {
			k.offset.setOffset(this, a)
		}) : l.call(this)
	}),
	function () {
		var a = document.getElementsByTagName("body")[0],
		x = document.createElement("div"),
		w,
		v,
		u,
		t,
		r;
		w = document.createElement(a ? "div" : "body"),
		u = {
			visibility : "hidden",
			width : 0,
			height : 0,
			border : 0,
			margin : 0,
			background : "none"
		},
		a && k.extend(u, {
			position : "absolute",
			left : "-1000px",
			top : "-1000px"
		});
		for (var f in u) {
			w.style[f] = u[f]
		}
		w.appendChild(x),
		v = a || document.documentElement,
		v.insertBefore(w, v.firstChild),
		x.style.cssText = "position: absolute; left: 10.7432222px; top: 10.432325px; height: 30px; width: 201px;",
		t = k(x).offset(function (e, c) {
				return c
			}).offset(),
		w.innerHTML = "",
		v.removeChild(w),
		r = t.top + t.left + (a ? 2000 : 0),
		n.fractions = r > 21 && r < 22
	}
	()
})(jQuery);
/*! jQuery UI - v1.8.20 - 2012-04-30
 * https://github.com/jquery/jquery-ui
 * Includes: jquery.ui.draggable.js
 * Copyright (c) 2012 AUTHORS.txt; Licensed MIT, GPL */
(function (e, c) {
	e.widget("ui.draggable", e.ui.mouse, {
		widgetEventPrefix : "drag",
		options : {
			addClasses : !0,
			appendTo : "parent",
			axis : !1,
			connectToSortable : !1,
			containment : !1,
			cursor : "auto",
			cursorAt : !1,
			grid : !1,
			handle : !1,
			helper : "original",
			iframeFix : !1,
			opacity : !1,
			refreshPositions : !1,
			revert : !1,
			revertDuration : 500,
			scope : "default",
			scroll : !0,
			scrollSensitivity : 20,
			scrollSpeed : 20,
			snap : !1,
			snapMode : "both",
			snapTolerance : 20,
			stack : !1,
			zIndex : !1
		},
		_create : function () {
			this.options.helper == "original" && !/^(?:r|a|f)/.test(this.element.css("position")) && (this.element[0].style.position = "relative"),
			this.options.addClasses && this.element.addClass("ui-draggable"),
			this.options.disabled && this.element.addClass("ui-draggable-disabled"),
			this._mouseInit()
		},
		destroy : function () {
			if (!this.element.data("draggable")) {
				return
			}
			return this.element.removeData("draggable").unbind(".draggable").removeClass("ui-draggable ui-draggable-dragging ui-draggable-disabled"),
			this._mouseDestroy(),
			this
		},
		_mouseCapture : function (a) {
			var f = this.options;
			return this.helper || f.disabled || e(a.target).is(".ui-resizable-handle") ? !1 : (this.handle = this._getHandle(a), this.handle ? (f.iframeFix && e(f.iframeFix === !0 ? "iframe" : f.iframeFix).each(function () {
						e('<div class="ui-draggable-iframeFix" style="background: #fff;"></div>').css({
							width : this.offsetWidth + "px",
							height : this.offsetHeight + "px",
							position : "absolute",
							opacity : "0.001",
							zIndex : 1000
						}).css(e(this).offset()).appendTo("body")
					}), !0) : !1)
		},
		_mouseStart : function (a) {
			var f = this.options;
			return this.helper = this._createHelper(a),
			this._cacheHelperProportions(),
			e.ui.ddmanager && (e.ui.ddmanager.current = this),
			this._cacheMargins(),
			this.cssPosition = this.helper.css("position"),
			this.scrollParent = this.helper.scrollParent(),
			this.offset = this.positionAbs = this.element.offset(),
			this.offset = {
				top : this.offset.top - this.margins.top,
				left : this.offset.left - this.margins.left
			},
			e.extend(this.offset, {
				click : {
					left : a.pageX - this.offset.left,
					top : a.pageY - this.offset.top
				},
				parent : this._getParentOffset(),
				relative : this._getRelativeOffset()
			}),
			this.originalPosition = this.position = this._generatePosition(a),
			this.originalPageX = a.pageX,
			this.originalPageY = a.pageY,
			f.cursorAt && this._adjustOffsetFromHelper(f.cursorAt),
			f.containment && this._setContainment(),
			this._trigger("start", a) === !1 ? (this._clear(), !1) : (this._cacheHelperProportions(), e.ui.ddmanager && !f.dropBehaviour && e.ui.ddmanager.prepareOffsets(this, a), this.helper.addClass("ui-draggable-dragging"), this._mouseDrag(a, !0), e.ui.ddmanager && e.ui.ddmanager.dragStart(this, a), !0)
		},
		_mouseDrag : function (a, h) {
			this.position = this._generatePosition(a),
			this.positionAbs = this._convertPositionTo("absolute");
			if (!h) {
				var f = this._uiHash();
				if (this._trigger("drag", a, f) === !1) {
					return this._mouseUp({}),
					!1
				}
				this.position = f.position
			}
			if (!this.options.axis || this.options.axis != "y") {
				this.helper[0].style.left = this.position.left + "px"
			}
			if (!this.options.axis || this.options.axis != "x") {
				this.helper[0].style.top = this.position.top + "px"
			}
			return e.ui.ddmanager && e.ui.ddmanager.drag(this, a),
			!1
		},
		_mouseStop : function (a) {
			var l = !1;
			e.ui.ddmanager && !this.options.dropBehaviour && (l = e.ui.ddmanager.drop(this, a)),
			this.dropped && (l = this.dropped, this.dropped = !1);
			var k = this.element[0],
			j = !1;
			while (k && (k = k.parentNode)) {
				k == document && (j = !0)
			}
			if (!j && this.options.helper === "original") {
				return !1
			}
			if (this.options.revert == "invalid" && !l || this.options.revert == "valid" && l || this.options.revert === !0 || e.isFunction(this.options.revert) && this.options.revert.call(this.element, l)) {
				var h = this;
				e(this.helper).animate(this.originalPosition, parseInt(this.options.revertDuration, 10), function () {
					h._trigger("stop", a) !== !1 && h._clear()
				})
			} else {
				this._trigger("stop", a) !== !1 && this._clear()
			}
			return !1
		},
		_mouseUp : function (a) {
			return this.options.iframeFix === !0 && e("div.ui-draggable-iframeFix").each(function () {
				this.parentNode.removeChild(this)
			}),
			e.ui.ddmanager && e.ui.ddmanager.dragStop(this, a),
			e.ui.mouse.prototype._mouseUp.call(this, a)
		},
		cancel : function () {
			return this.helper.is(".ui-draggable-dragging") ? this._mouseUp({}) : this._clear(),
			this
		},
		_getHandle : function (a) {
			var f = !this.options.handle || !e(this.options.handle, this.element).length ? !0 : !1;
			return e(this.options.handle, this.element).find("*").andSelf().each(function () {
				this == a.target && (f = !0)
			}),
			f
		},
		_createHelper : function (a) {
			var h = this.options,
			f = e.isFunction(h.helper) ? e(h.helper.apply(this.element[0], [a])) : h.helper == "clone" ? this.element.clone().removeAttr("id") : this.element;
			return f.parents("body").length || f.appendTo(h.appendTo == "parent" ? this.element[0].parentNode : h.appendTo),
			f[0] != this.element[0] && !/(fixed|absolute)/.test(f.css("position")) && f.css("position", "absolute"),
			f
		},
		_adjustOffsetFromHelper : function (a) {
			typeof a == "string" && (a = a.split(" ")),
			e.isArray(a) && (a = {
					left : +a[0],
					top : +a[1] || 0
				}),
			"left" in a && (this.offset.click.left = a.left + this.margins.left),
			"right" in a && (this.offset.click.left = this.helperProportions.width - a.right + this.margins.left),
			"top" in a && (this.offset.click.top = a.top + this.margins.top),
			"bottom" in a && (this.offset.click.top = this.helperProportions.height - a.bottom + this.margins.top)
		},
		_getParentOffset : function () {
			this.offsetParent = this.helper.offsetParent();
			var a = this.offsetParent.offset();
			this.cssPosition == "absolute" && this.scrollParent[0] != document && e.ui.contains(this.scrollParent[0], this.offsetParent[0]) && (a.left += this.scrollParent.scrollLeft(), a.top += this.scrollParent.scrollTop());
			if (this.offsetParent[0] == document.body || this.offsetParent[0].tagName && this.offsetParent[0].tagName.toLowerCase() == "html" && e.browser.msie) {
				a = {
					top : 0,
					left : 0
				}
			}
			return {
				top : a.top + (parseInt(this.offsetParent.css("borderTopWidth"), 10) || 0),
				left : a.left + (parseInt(this.offsetParent.css("borderLeftWidth"), 10) || 0)
			}
		},
		_getRelativeOffset : function () {
			if (this.cssPosition == "relative") {
				var b = this.element.position();
				return {
					top : b.top - (parseInt(this.helper.css("top"), 10) || 0) + this.scrollParent.scrollTop(),
					left : b.left - (parseInt(this.helper.css("left"), 10) || 0) + this.scrollParent.scrollLeft()
				}
			}
			return {
				top : 0,
				left : 0
			}
		},
		_cacheMargins : function () {
			this.margins = {
				left : parseInt(this.element.css("marginLeft"), 10) || 0,
				top : parseInt(this.element.css("marginTop"), 10) || 0,
				right : parseInt(this.element.css("marginRight"), 10) || 0,
				bottom : parseInt(this.element.css("marginBottom"), 10) || 0
			}
		},
		_cacheHelperProportions : function () {
			this.helperProportions = {
				width : this.helper.outerWidth(),
				height : this.helper.outerHeight()
			}
		},
		_setContainment : function () {
			var a = this.options;
			a.containment == "parent" && (a.containment = this.helper[0].parentNode);
			if (a.containment == "document" || a.containment == "window") {
				this.containment = [a.containment == "document" ? 0 : e(window).scrollLeft() - this.offset.relative.left - this.offset.parent.left, a.containment == "document" ? 0 : e(window).scrollTop() - this.offset.relative.top - this.offset.parent.top, (a.containment == "document" ? 0 : e(window).scrollLeft()) + e(a.containment == "document" ? document : window).width() - this.helperProportions.width - this.margins.left, (a.containment == "document" ? 0 : e(window).scrollTop()) + (e(a.containment == "document" ? document : window).height() || document.body.parentNode.scrollHeight) - this.helperProportions.height - this.margins.top]
			}
			if (!/^(document|window|parent)$/.test(a.containment) && a.containment.constructor != Array) {
				var l = e(a.containment),
				k = l[0];
				if (!k) {
					return
				}
				var j = l.offset(),
				h = e(k).css("overflow") != "hidden";
				this.containment = [(parseInt(e(k).css("borderLeftWidth"), 10) || 0) + (parseInt(e(k).css("paddingLeft"), 10) || 0), (parseInt(e(k).css("borderTopWidth"), 10) || 0) + (parseInt(e(k).css("paddingTop"), 10) || 0), (h ? Math.max(k.scrollWidth, k.offsetWidth) : k.offsetWidth) - (parseInt(e(k).css("borderLeftWidth"), 10) || 0) - (parseInt(e(k).css("paddingRight"), 10) || 0) - this.helperProportions.width - this.margins.left - this.margins.right, (h ? Math.max(k.scrollHeight, k.offsetHeight) : k.offsetHeight) - (parseInt(e(k).css("borderTopWidth"), 10) || 0) - (parseInt(e(k).css("paddingBottom"), 10) || 0) - this.helperProportions.height - this.margins.top - this.margins.bottom],
				this.relative_container = l
			} else {
				a.containment.constructor == Array && (this.containment = a.containment)
			}
		},
		_convertPositionTo : function (a, m) {
			m || (m = this.position);
			var l = a == "absolute" ? 1 : -1,
			k = this.options,
			j = this.cssPosition == "absolute" && (this.scrollParent[0] == document || !e.ui.contains(this.scrollParent[0], this.offsetParent[0])) ? this.offsetParent : this.scrollParent,
			h = /(html|body)/i.test(j[0].tagName);
			return {
				top : m.top + this.offset.relative.top * l + this.offset.parent.top * l - (e.browser.safari && e.browser.version < 526 && this.cssPosition == "fixed" ? 0 : (this.cssPosition == "fixed" ? -this.scrollParent.scrollTop() : h ? 0 : j.scrollTop()) * l),
				left : m.left + this.offset.relative.left * l + this.offset.parent.left * l - (e.browser.safari && e.browser.version < 526 && this.cssPosition == "fixed" ? 0 : (this.cssPosition == "fixed" ? -this.scrollParent.scrollLeft() : h ? 0 : j.scrollLeft()) * l)
			}
		},
		_generatePosition : function (u) {
			var t = this.options,
			r = this.cssPosition == "absolute" && (this.scrollParent[0] == document || !e.ui.contains(this.scrollParent[0], this.offsetParent[0])) ? this.offsetParent : this.scrollParent,
			q = /(html|body)/i.test(r[0].tagName),
			p = u.pageX,
			o = u.pageY;
			if (this.originalPosition) {
				var n;
				if (this.containment) {
					if (this.relative_container) {
						var m = this.relative_container.offset();
						n = [this.containment[0] + m.left, this.containment[1] + m.top, this.containment[2] + m.left, this.containment[3] + m.top]
					} else {
						n = this.containment
					}
					u.pageX - this.offset.click.left < n[0] && (p = n[0] + this.offset.click.left),
					u.pageY - this.offset.click.top < n[1] && (o = n[1] + this.offset.click.top),
					u.pageX - this.offset.click.left > n[2] && (p = n[2] + this.offset.click.left),
					u.pageY - this.offset.click.top > n[3] && (o = n[3] + this.offset.click.top)
				}
				if (t.grid) {
					var l = t.grid[1] ? this.originalPageY + Math.round((o - this.originalPageY) / t.grid[1]) * t.grid[1] : this.originalPageY;
					o = n ? l - this.offset.click.top < n[1] || l - this.offset.click.top > n[3] ? l - this.offset.click.top < n[1] ? l + t.grid[1] : l - t.grid[1] : l : l;
					var a = t.grid[0] ? this.originalPageX + Math.round((p - this.originalPageX) / t.grid[0]) * t.grid[0] : this.originalPageX;
					p = n ? a - this.offset.click.left < n[0] || a - this.offset.click.left > n[2] ? a - this.offset.click.left < n[0] ? a + t.grid[0] : a - t.grid[0] : a : a
				}
			}
			return {
				top : o - this.offset.click.top - this.offset.relative.top - this.offset.parent.top + (e.browser.safari && e.browser.version < 526 && this.cssPosition == "fixed" ? 0 : this.cssPosition == "fixed" ? -this.scrollParent.scrollTop() : q ? 0 : r.scrollTop()),
				left : p - this.offset.click.left - this.offset.relative.left - this.offset.parent.left + (e.browser.safari && e.browser.version < 526 && this.cssPosition == "fixed" ? 0 : this.cssPosition == "fixed" ? -this.scrollParent.scrollLeft() : q ? 0 : r.scrollLeft())
			}
		},
		_clear : function () {
			this.helper.removeClass("ui-draggable-dragging"),
			this.helper[0] != this.element[0] && !this.cancelHelperRemoval && this.helper.remove(),
			this.helper = null,
			this.cancelHelperRemoval = !1
		},
		_trigger : function (a, h, f) {
			return f = f || this._uiHash(),
			e.ui.plugin.call(this, a, [h, f]),
			a == "drag" && (this.positionAbs = this._convertPositionTo("absolute")),
			e.Widget.prototype._trigger.call(this, a, h, f)
		},
		plugins : {},
		_uiHash : function (b) {
			return {
				helper : this.helper,
				position : this.position,
				originalPosition : this.originalPosition,
				offset : this.positionAbs
			}
		}
	}),
	e.extend(e.ui.draggable, {
		version : "1.8.20"
	}),
	e.ui.plugin.add("draggable", "connectToSortable", {
		start : function (a, l) {
			var k = e(this).data("draggable"),
			j = k.options,
			h = e.extend({}, l, {
					item : k.element
				});
			k.sortables = [],
			e(j.connectToSortable).each(function () {
				var b = e.data(this, "sortable");
				b && !b.options.disabled && (k.sortables.push({
						instance : b,
						shouldRevert : b.options.revert
					}), b.refreshPositions(), b._trigger("activate", a, h))
			})
		},
		stop : function (a, j) {
			var h = e(this).data("draggable"),
			f = e.extend({}, j, {
					item : h.element
				});
			e.each(h.sortables, function () {
				this.instance.isOver ? (this.instance.isOver = 0, h.cancelHelperRemoval = !0, this.instance.cancelHelperRemoval = !1, this.shouldRevert && (this.instance.options.revert = !0), this.instance._mouseStop(a), this.instance.options.helper = this.instance.options._helper, h.options.helper == "original" && this.instance.currentItem.css({
						top : "auto",
						left : "auto"
					})) : (this.instance.cancelHelperRemoval = !1, this.instance._trigger("deactivate", a, f))
			})
		},
		drag : function (a, l) {
			var k = e(this).data("draggable"),
			j = this,
			h = function (v) {
				var u = this.offset.click.top,
				t = this.offset.click.left,
				r = this.positionAbs.top,
				q = this.positionAbs.left,
				p = v.height,
				o = v.width,
				n = v.top,
				m = v.left;
				return e.ui.isOver(r + u, q + t, n, m, p, o)
			};
			e.each(k.sortables, function (b) {
				this.instance.positionAbs = k.positionAbs,
				this.instance.helperProportions = k.helperProportions,
				this.instance.offset.click = k.offset.click,
				this.instance._intersectsWith(this.instance.containerCache) ? (this.instance.isOver || (this.instance.isOver = 1, this.instance.currentItem = e(j).clone().removeAttr("id").appendTo(this.instance.element).data("sortable-item", !0), this.instance.options._helper = this.instance.options.helper, this.instance.options.helper = function () {
						return l.helper[0]
					}, a.target = this.instance.currentItem[0], this.instance._mouseCapture(a, !0), this.instance._mouseStart(a, !0, !0), this.instance.offset.click.top = k.offset.click.top, this.instance.offset.click.left = k.offset.click.left, this.instance.offset.parent.left -= k.offset.parent.left - this.instance.offset.parent.left, this.instance.offset.parent.top -= k.offset.parent.top - this.instance.offset.parent.top, k._trigger("toSortable", a), k.dropped = this.instance.element, k.currentItem = k.element, this.instance.fromOutside = k), this.instance.currentItem && this.instance._mouseDrag(a)) : this.instance.isOver && (this.instance.isOver = 0, this.instance.cancelHelperRemoval = !0, this.instance.options.revert = !1, this.instance._trigger("out", a, this.instance._uiHash(this.instance)), this.instance._mouseStop(a, !0), this.instance.options.helper = this.instance.options._helper, this.instance.currentItem.remove(), this.instance.placeholder && this.instance.placeholder.remove(), k._trigger("fromSortable", a), k.dropped = !1)
			})
		}
	}),
	e.ui.plugin.add("draggable", "cursor", {
		start : function (a, j) {
			var h = e("body"),
			f = e(this).data("draggable").options;
			h.css("cursor") && (f._cursor = h.css("cursor")),
			h.css("cursor", f.cursor)
		},
		stop : function (a, h) {
			var f = e(this).data("draggable").options;
			f._cursor && e("body").css("cursor", f._cursor)
		}
	}),
	e.ui.plugin.add("draggable", "opacity", {
		start : function (a, j) {
			var h = e(j.helper),
			f = e(this).data("draggable").options;
			h.css("opacity") && (f._opacity = h.css("opacity")),
			h.css("opacity", f.opacity)
		},
		stop : function (a, h) {
			var f = e(this).data("draggable").options;
			f._opacity && e(h.helper).css("opacity", f._opacity)
		}
	}),
	e.ui.plugin.add("draggable", "scroll", {
		start : function (a, h) {
			var f = e(this).data("draggable");
			f.scrollParent[0] != document && f.scrollParent[0].tagName != "HTML" && (f.overflowOffset = f.scrollParent.offset())
		},
		drag : function (a, l) {
			var k = e(this).data("draggable"),
			j = k.options,
			h = !1;
			if (k.scrollParent[0] != document && k.scrollParent[0].tagName != "HTML") {
				if (!j.axis || j.axis != "x") {
					k.overflowOffset.top + k.scrollParent[0].offsetHeight - a.pageY < j.scrollSensitivity ? k.scrollParent[0].scrollTop = h = k.scrollParent[0].scrollTop + j.scrollSpeed : a.pageY - k.overflowOffset.top < j.scrollSensitivity && (k.scrollParent[0].scrollTop = h = k.scrollParent[0].scrollTop - j.scrollSpeed)
				}
				if (!j.axis || j.axis != "y") {
					k.overflowOffset.left + k.scrollParent[0].offsetWidth - a.pageX < j.scrollSensitivity ? k.scrollParent[0].scrollLeft = h = k.scrollParent[0].scrollLeft + j.scrollSpeed : a.pageX - k.overflowOffset.left < j.scrollSensitivity && (k.scrollParent[0].scrollLeft = h = k.scrollParent[0].scrollLeft - j.scrollSpeed)
				}
			} else {
				if (!j.axis || j.axis != "x") {
					a.pageY - e(document).scrollTop() < j.scrollSensitivity ? h = e(document).scrollTop(e(document).scrollTop() - j.scrollSpeed) : e(window).height() - (a.pageY - e(document).scrollTop()) < j.scrollSensitivity && (h = e(document).scrollTop(e(document).scrollTop() + j.scrollSpeed))
				}
				if (!j.axis || j.axis != "y") {
					a.pageX - e(document).scrollLeft() < j.scrollSensitivity ? h = e(document).scrollLeft(e(document).scrollLeft() - j.scrollSpeed) : e(window).width() - (a.pageX - e(document).scrollLeft()) < j.scrollSensitivity && (h = e(document).scrollLeft(e(document).scrollLeft() + j.scrollSpeed))
				}
			}
			h !== !1 && e.ui.ddmanager && !j.dropBehaviour && e.ui.ddmanager.prepareOffsets(k, a)
		}
	}),
	e.ui.plugin.add("draggable", "snap", {
		start : function (a, j) {
			var h = e(this).data("draggable"),
			f = h.options;
			h.snapElements = [],
			e(f.snap.constructor != String ? f.snap.items || ":data(draggable)" : f.snap).each(function () {
				var k = e(this),
				l = k.offset();
				this != h.element[0] && h.snapElements.push({
					item : this,
					width : k.outerWidth(),
					height : k.outerHeight(),
					top : l.top,
					left : l.left
				})
			})
		},
		drag : function (M, L) {
			var K = e(this).data("draggable"),
			J = K.options,
			I = J.snapTolerance,
			H = L.offset.left,
			G = H + K.helperProportions.width,
			F = L.offset.top,
			E = F + K.helperProportions.height;
			for (var D = K.snapElements.length - 1; D >= 0; D--) {
				var C = K.snapElements[D].left,
				B = C + K.snapElements[D].width,
				A = K.snapElements[D].top,
				y = A + K.snapElements[D].height;
				if (!(C - I < H && H < B + I && A - I < F && F < y + I || C - I < H && H < B + I && A - I < E && E < y + I || C - I < G && G < B + I && A - I < F && F < y + I || C - I < G && G < B + I && A - I < E && E < y + I)) {
					K.snapElements[D].snapping && K.options.snap.release && K.options.snap.release.call(K.element, M, e.extend(K._uiHash(), {
							snapItem : K.snapElements[D].item
						})),
					K.snapElements[D].snapping = !1;
					continue
				}
				if (J.snapMode != "inner") {
					var x = Math.abs(A - E) <= I,
					w = Math.abs(y - F) <= I,
					v = Math.abs(C - G) <= I,
					u = Math.abs(B - H) <= I;
					x && (L.position.top = K._convertPositionTo("relative", {
								top : A - K.helperProportions.height,
								left : 0
							}).top - K.margins.top),
					w && (L.position.top = K._convertPositionTo("relative", {
								top : y,
								left : 0
							}).top - K.margins.top),
					v && (L.position.left = K._convertPositionTo("relative", {
								top : 0,
								left : C - K.helperProportions.width
							}).left - K.margins.left),
					u && (L.position.left = K._convertPositionTo("relative", {
								top : 0,
								left : B
							}).left - K.margins.left)
				}
				var a = x || w || v || u;
				if (J.snapMode != "outer") {
					var x = Math.abs(A - F) <= I,
					w = Math.abs(y - E) <= I,
					v = Math.abs(C - H) <= I,
					u = Math.abs(B - G) <= I;
					x && (L.position.top = K._convertPositionTo("relative", {
								top : A,
								left : 0
							}).top - K.margins.top),
					w && (L.position.top = K._convertPositionTo("relative", {
								top : y - K.helperProportions.height,
								left : 0
							}).top - K.margins.top),
					v && (L.position.left = K._convertPositionTo("relative", {
								top : 0,
								left : C
							}).left - K.margins.left),
					u && (L.position.left = K._convertPositionTo("relative", {
								top : 0,
								left : B - K.helperProportions.width
							}).left - K.margins.left)
				}
				!K.snapElements[D].snapping && (x || w || v || u || a) && K.options.snap.snap && K.options.snap.snap.call(K.element, M, e.extend(K._uiHash(), {
						snapItem : K.snapElements[D].item
					})),
				K.snapElements[D].snapping = x || w || v || u || a
			}
		}
	}),
	e.ui.plugin.add("draggable", "stack", {
		start : function (a, l) {
			var k = e(this).data("draggable").options,
			j = e.makeArray(e(k.stack)).sort(function (f, m) {
					return (parseInt(e(f).css("zIndex"), 10) || 0) - (parseInt(e(m).css("zIndex"), 10) || 0)
				});
			if (!j.length) {
				return
			}
			var h = parseInt(j[0].style.zIndex) || 0;
			e(j).each(function (b) {
				this.style.zIndex = h + b
			}),
			this[0].style.zIndex = h + j.length
		}
	}),
	e.ui.plugin.add("draggable", "zIndex", {
		start : function (a, j) {
			var h = e(j.helper),
			f = e(this).data("draggable").options;
			h.css("zIndex") && (f._zIndex = h.css("zIndex")),
			h.css("zIndex", f.zIndex)
		},
		stop : function (a, h) {
			var f = e(this).data("draggable").options;
			f._zIndex && e(h.helper).css("zIndex", f._zIndex)
		}
	})
})(jQuery);
/*! jQuery UI - v1.8.20 - 2012-04-30
 * https://github.com/jquery/jquery-ui
 * Includes: jquery.ui.droppable.js
 * Copyright (c) 2012 AUTHORS.txt; Licensed MIT, GPL */
(function (e, c) {
	e.widget("ui.droppable", {
		widgetEventPrefix : "drop",
		options : {
			accept : "*",
			activeClass : !1,
			addClasses : !0,
			greedy : !1,
			hoverClass : !1,
			scope : "default",
			tolerance : "intersect"
		},
		_create : function () {
			var a = this.options,
			f = a.accept;
			this.isover = 0,
			this.isout = 1,
			this.accept = e.isFunction(f) ? f : function (b) {
				return b.is(f)
			},
			this.proportions = {
				width : this.element[0].offsetWidth,
				height : this.element[0].offsetHeight
			},
			e.ui.ddmanager.droppables[a.scope] = e.ui.ddmanager.droppables[a.scope] || [],
			e.ui.ddmanager.droppables[a.scope].push(this),
			a.addClasses && this.element.addClass("ui-droppable")
		},
		destroy : function () {
			var a = e.ui.ddmanager.droppables[this.options.scope];
			for (var f = 0; f < a.length; f++) {
				a[f] == this && a.splice(f, 1)
			}
			return this.element.removeClass("ui-droppable ui-droppable-disabled").removeData("droppable").unbind(".droppable"),
			this
		},
		_setOption : function (a, f) {
			a == "accept" && (this.accept = e.isFunction(f) ? f : function (b) {
				return b.is(f)
			}),
			e.Widget.prototype._setOption.apply(this, arguments)
		},
		_activate : function (a) {
			var f = e.ui.ddmanager.current;
			this.options.activeClass && this.element.addClass(this.options.activeClass),
			f && this._trigger("activate", a, this.ui(f))
		},
		_deactivate : function (a) {
			var f = e.ui.ddmanager.current;
			this.options.activeClass && this.element.removeClass(this.options.activeClass),
			f && this._trigger("deactivate", a, this.ui(f))
		},
		_over : function (a) {
			var f = e.ui.ddmanager.current;
			if (!f || (f.currentItem || f.element)[0] == this.element[0]) {
				return
			}
			this.accept.call(this.element[0], f.currentItem || f.element) && (this.options.hoverClass && this.element.addClass(this.options.hoverClass), this._trigger("over", a, this.ui(f)))
		},
		_out : function (a) {
			var f = e.ui.ddmanager.current;
			if (!f || (f.currentItem || f.element)[0] == this.element[0]) {
				return
			}
			this.accept.call(this.element[0], f.currentItem || f.element) && (this.options.hoverClass && this.element.removeClass(this.options.hoverClass), this._trigger("out", a, this.ui(f)))
		},
		_drop : function (a, j) {
			var h = j || e.ui.ddmanager.current;
			if (!h || (h.currentItem || h.element)[0] == this.element[0]) {
				return !1
			}
			var f = !1;
			return this.element.find(":data(droppable)").not(".ui-draggable-dragging").each(function () {
				var k = e.data(this, "droppable");
				if (k.options.greedy && !k.options.disabled && k.options.scope == h.options.scope && k.accept.call(k.element[0], h.currentItem || h.element) && e.ui.intersect(h, e.extend(k, {
							offset : k.element.offset()
						}), k.options.tolerance)) {
					return f = !0,
					!1
				}
			}),
			f ? !1 : this.accept.call(this.element[0], h.currentItem || h.element) ? (this.options.activeClass && this.element.removeClass(this.options.activeClass), this.options.hoverClass && this.element.removeClass(this.options.hoverClass), this._trigger("drop", a, this.ui(h)), this.element) : !1
		},
		ui : function (b) {
			return {
				draggable : b.currentItem || b.element,
				helper : b.helper,
				position : b.position,
				offset : b.positionAbs
			}
		}
	}),
	e.extend(e.ui.droppable, {
		version : "1.8.20"
	}),
	e.ui.intersect = function (D, C, B) {
		if (!C.offset) {
			return !1
		}
		var A = (D.positionAbs || D.position.absolute).left,
		y = A + D.helperProportions.width,
		x = (D.positionAbs || D.position.absolute).top,
		w = x + D.helperProportions.height,
		v = C.offset.left,
		u = v + C.proportions.width,
		t = C.offset.top,
		r = t + C.proportions.height;
		switch (B) {
		case "fit":
			return v <= A && y <= u && t <= x && w <= r;
		case "intersect":
			return v < A + D.helperProportions.width / 2 && y - D.helperProportions.width / 2 < u && t < x + D.helperProportions.height / 2 && w - D.helperProportions.height / 2 < r;
		case "pointer":
			var q = (D.positionAbs || D.position.absolute).left + (D.clickOffset || D.offset.click).left,
			p = (D.positionAbs || D.position.absolute).top + (D.clickOffset || D.offset.click).top,
			a = e.ui.isOver(p, q, t, v, C.proportions.height, C.proportions.width);
			return a;
		case "touch":
			return (x >= t && x <= r || w >= t && w <= r || x < t && w > r) && (A >= v && A <= u || y >= v && y <= u || A < v && y > u);
		default:
			return !1
		}
	},
	e.ui.ddmanager = {
		current : null,
		droppables : {
			"default" : []
		},
		prepareOffsets : function (a, o) {
			var n = e.ui.ddmanager.droppables[a.options.scope] || [],
			m = o ? o.type : null,
			l = (a.currentItem || a.element).find(":data(droppable)").andSelf();
			g : for (var k = 0; k < n.length; k++) {
				if (n[k].options.disabled || a && !n[k].accept.call(n[k].element[0], a.currentItem || a.element)) {
					continue
				}
				for (var j = 0; j < l.length; j++) {
					if (l[j] == n[k].element[0]) {
						n[k].proportions.height = 0;
						continue g
					}
				}
				n[k].visible = n[k].element.css("display") != "none";
				if (!n[k].visible) {
					continue
				}
				m == "mousedown" && n[k]._activate.call(n[k], o),
				n[k].offset = n[k].element.offset(),
				n[k].proportions = {
					width : n[k].element[0].offsetWidth,
					height : n[k].element[0].offsetHeight
				}
			}
		},
		drop : function (a, h) {
			var f = !1;
			return e.each(e.ui.ddmanager.droppables[a.options.scope] || [], function () {
				if (!this.options) {
					return
				}
				!this.options.disabled && this.visible && e.ui.intersect(a, this, this.options.tolerance) && (f = this._drop.call(this, h) || f),
				!this.options.disabled && this.visible && this.accept.call(this.element[0], a.currentItem || a.element) && (this.isout = 1, this.isover = 0, this._deactivate.call(this, h))
			}),
			f
		},
		dragStart : function (a, f) {
			a.element.parents(":not(body,html)").bind("scroll.droppable", function () {
				a.options.refreshPositions || e.ui.ddmanager.prepareOffsets(a, f)
			})
		},
		drag : function (a, f) {
			a.options.refreshPositions && e.ui.ddmanager.prepareOffsets(a, f),
			e.each(e.ui.ddmanager.droppables[a.options.scope] || [], function () {
				if (this.options.disabled || this.greedyChild || !this.visible) {
					return
				}
				var k = e.ui.intersect(a, this, this.options.tolerance),
				j = !k && this.isover == 1 ? "isout" : k && this.isover == 0 ? "isover" : null;
				if (!j) {
					return
				}
				var h;
				if (this.options.greedy) {
					var b = this.element.parents(":data(droppable):eq(0)");
					b.length && (h = e.data(b[0], "droppable"), h.greedyChild = j == "isover" ? 1 : 0)
				}
				h && j == "isover" && (h.isover = 0, h.isout = 1, h._out.call(h, f)),
				this[j] = 1,
				this[j == "isout" ? "isover" : "isout"] = 0,
				this[j == "isover" ? "_over" : "_out"].call(this, f),
				h && j == "isout" && (h.isout = 0, h.isover = 1, h._over.call(h, f))
			})
		},
		dragStop : function (a, f) {
			a.element.parents(":not(body,html)").unbind("scroll.droppable"),
			a.options.refreshPositions || e.ui.ddmanager.prepareOffsets(a, f)
		}
	}
})(jQuery);
/*! jQuery UI - v1.8.20 - 2012-04-30
 * https://github.com/jquery/jquery-ui
 * Includes: jquery.ui.resizable.js
 * Copyright (c) 2012 AUTHORS.txt; Licensed MIT, GPL */
(function (f, e) {
	f.widget("ui.resizable", f.ui.mouse, {
		widgetEventPrefix : "resize",
		options : {
			alsoResize : !1,
			animate : !1,
			animateDuration : "slow",
			animateEasing : "swing",
			aspectRatio : !1,
			autoHide : !1,
			containment : !1,
			ghost : !1,
			grid : !1,
			handles : "e,s,se",
			helper : !1,
			maxHeight : null,
			maxWidth : null,
			minHeight : 10,
			minWidth : 10,
			zIndex : 1000
		},
		_create : function () {
			var a = this,
			p = this.options;
			this.element.addClass("ui-resizable"),
			f.extend(this, {
				_aspectRatio : !!p.aspectRatio,
				aspectRatio : p.aspectRatio,
				originalElement : this.element,
				_proportionallyResizeElements : [],
				_helper : p.helper || p.ghost || p.animate ? p.helper || "ui-resizable-helper" : null
			}),
			this.element[0].nodeName.match(/canvas|textarea|input|select|button|img/i) && (this.element.wrap(f('<div class="ui-wrapper" style="overflow: hidden;"></div>').css({
						position : this.element.css("position"),
						width : this.element.outerWidth(),
						height : this.element.outerHeight(),
						top : this.element.css("top"),
						left : this.element.css("left")
					})), this.element = this.element.parent().data("resizable", this.element.data("resizable")), this.elementIsWrapper = !0, this.element.css({
					marginLeft : this.originalElement.css("marginLeft"),
					marginTop : this.originalElement.css("marginTop"),
					marginRight : this.originalElement.css("marginRight"),
					marginBottom : this.originalElement.css("marginBottom")
				}), this.originalElement.css({
					marginLeft : 0,
					marginTop : 0,
					marginRight : 0,
					marginBottom : 0
				}), this.originalResizeStyle = this.originalElement.css("resize"), this.originalElement.css("resize", "none"), this._proportionallyResizeElements.push(this.originalElement.css({
						position : "static",
						zoom : 1,
						display : "block"
					})), this.originalElement.css({
					margin : this.originalElement.css("margin")
				}), this._proportionallyResize()),
			this.handles = p.handles || (f(".ui-resizable-handle", this.element).length ? {
					n : ".ui-resizable-n",
					e : ".ui-resizable-e",
					s : ".ui-resizable-s",
					w : ".ui-resizable-w",
					se : ".ui-resizable-se",
					sw : ".ui-resizable-sw",
					ne : ".ui-resizable-ne",
					nw : ".ui-resizable-nw"
				}
					 : "e,s,se");
			if (this.handles.constructor == String) {
				this.handles == "all" && (this.handles = "n,e,s,w,se,sw,ne,nw");
				var o = this.handles.split(",");
				this.handles = {};
				for (var n = 0; n < o.length; n++) {
					var m = f.trim(o[n]),
					l = "ui-resizable-" + m,
					k = f('<div class="ui-resizable-handle ' + l + '"></div>');
					k.css({
						zIndex : p.zIndex
					}),
					"se" == m && k.addClass("ui-icon ui-icon-gripsmall-diagonal-se"),
					this.handles[m] = ".ui-resizable-" + m,
					this.element.append(k)
				}
			}
			this._renderAxis = function (q) {
				q = q || this.element;
				for (var v in this.handles) {
					this.handles[v].constructor == String && (this.handles[v] = f(this.handles[v], this.element).show());
					if (this.elementIsWrapper && this.originalElement[0].nodeName.match(/textarea|input|select|button/i)) {
						var u = f(this.handles[v], this.element),
						t = 0;
						t = /sw|ne|nw|se|n|s/.test(v) ? u.outerHeight() : u.outerWidth();
						var r = ["padding", /ne|nw|n/.test(v) ? "Top" : /se|sw|s/.test(v) ? "Bottom" : /^e$/.test(v) ? "Right" : "Left"].join("");
						q.css(r, t),
						this._proportionallyResize()
					}
					if (!f(this.handles[v]).length) {
						continue
					}
				}
			},
			this._renderAxis(this.element),
			this._handles = f(".ui-resizable-handle", this.element).disableSelection(),
			this._handles.mouseover(function () {
				if (!a.resizing) {
					if (this.className) {
						var b = this.className.match(/ui-resizable-(se|sw|ne|nw|n|e|s|w)/i)
					}
					a.axis = b && b[1] ? b[1] : "se"
				}
			}),
			p.autoHide && (this._handles.hide(), f(this.element).addClass("ui-resizable-autohide").hover(function () {
					if (p.disabled) {
						return
					}
					f(this).removeClass("ui-resizable-autohide"),
					a._handles.show()
				}, function () {
					if (p.disabled) {
						return
					}
					a.resizing || (f(this).addClass("ui-resizable-autohide"), a._handles.hide())
				})),
			this._mouseInit()
		},
		destroy : function () {
			this._mouseDestroy();
			var a = function (c) {
				f(c).removeClass("ui-resizable ui-resizable-disabled ui-resizable-resizing").removeData("resizable").unbind(".resizable").find(".ui-resizable-handle").remove()
			};
			if (this.elementIsWrapper) {
				a(this.element);
				var k = this.element;
				k.after(this.originalElement.css({
						position : k.css("position"),
						width : k.outerWidth(),
						height : k.outerHeight(),
						top : k.css("top"),
						left : k.css("left")
					})).remove()
			}
			return this.originalElement.css("resize", this.originalResizeStyle),
			a(this.originalElement),
			this
		},
		_mouseCapture : function (a) {
			var l = !1;
			for (var k in this.handles) {
				f(this.handles[k])[0] == a.target && (l = !0)
			}
			return !this.options.disabled && l
		},
		_mouseStart : function (a) {
			var o = this.options,
			n = this.element.position(),
			m = this.element;
			this.resizing = !0,
			this.documentScroll = {
				top : f(document).scrollTop(),
				left : f(document).scrollLeft()
			},
			(m.is(".ui-draggable") || /absolute/.test(m.css("position"))) && m.css({
				position : "absolute",
				top : n.top,
				left : n.left
			}),
			this._renderProxy();
			var l = j(this.helper.css("left")),
			k = j(this.helper.css("top"));
			o.containment && (l += f(o.containment).scrollLeft() || 0, k += f(o.containment).scrollTop() || 0),
			this.offset = this.helper.offset(),
			this.position = {
				left : l,
				top : k
			},
			this.size = this._helper ? {
				width : m.outerWidth(),
				height : m.outerHeight()
			}
			 : {
				width : m.width(),
				height : m.height()
			},
			this.originalSize = this._helper ? {
				width : m.outerWidth(),
				height : m.outerHeight()
			}
			 : {
				width : m.width(),
				height : m.height()
			},
			this.originalPosition = {
				left : l,
				top : k
			},
			this.sizeDiff = {
				width : m.outerWidth() - m.width(),
				height : m.outerHeight() - m.height()
			},
			this.originalMousePosition = {
				left : a.pageX,
				top : a.pageY
			},
			this.aspectRatio = typeof o.aspectRatio == "number" ? o.aspectRatio : this.originalSize.width / this.originalSize.height || 1;
			var c = f(".ui-resizable-" + this.axis).css("cursor");
			return f("body").css("cursor", c == "auto" ? this.axis + "-resize" : c),
			m.addClass("ui-resizable-resizing"),
			this._propagate("start", a),
			!0
		},
		_mouseDrag : function (B) {
			var A = this.helper,
			y = this.options,
			x = {},
			w = this,
			v = this.originalMousePosition,
			u = this.axis,
			t = B.pageX - v.left || 0,
			r = B.pageY - v.top || 0,
			q = this._change[u];
			if (!q) {
				return !1
			}
			var p = q.apply(this, [B, t, r]),
			o = f.browser.msie && f.browser.version < 7,
			a = this.sizeDiff;
			this._updateVirtualBoundaries(B.shiftKey);
			if (this._aspectRatio || B.shiftKey) {
				p = this._updateRatio(p, B)
			}
			return p = this._respectSize(p, B),
			this._propagate("resize", B),
			A.css({
				top : this.position.top + "px",
				left : this.position.left + "px",
				width : this.size.width + "px",
				height : this.size.height + "px"
			}),
			!this._helper && this._proportionallyResizeElements.length && this._proportionallyResize(),
			this._updateCache(p),
			this._trigger("resize", B, this.ui()),
			!1
		},
		_mouseStop : function (u) {
			this.resizing = !1;
			var t = this.options,
			r = this;
			if (this._helper) {
				var q = this._proportionallyResizeElements,
				p = q.length && /textarea/i.test(q[0].nodeName),
				o = p && f.ui.hasScroll(q[0], "left") ? 0 : r.sizeDiff.height,
				n = p ? 0 : r.sizeDiff.width,
				m = {
					width : r.helper.width() - n,
					height : r.helper.height() - o
				},
				l = parseInt(r.element.css("left"), 10) + (r.position.left - r.originalPosition.left) || null,
				a = parseInt(r.element.css("top"), 10) + (r.position.top - r.originalPosition.top) || null;
				t.animate || this.element.css(f.extend(m, {
						top : a,
						left : l
					})),
				r.helper.height(r.size.height),
				r.helper.width(r.size.width),
				this._helper && !t.animate && this._proportionallyResize()
			}
			return f("body").css("cursor", "auto"),
			this.element.removeClass("ui-resizable-resizing"),
			this._propagate("stop", u),
			this._helper && this.helper.remove(),
			!1
		},
		_updateVirtualBoundaries : function (l) {
			var k = this.options,
			q,
			p,
			o,
			n,
			m;
			m = {
				minWidth : h(k.minWidth) ? k.minWidth : 0,
				maxWidth : h(k.maxWidth) ? k.maxWidth : Infinity,
				minHeight : h(k.minHeight) ? k.minHeight : 0,
				maxHeight : h(k.maxHeight) ? k.maxHeight : Infinity
			};
			if (this._aspectRatio || l) {
				q = m.minHeight * this.aspectRatio,
				o = m.minWidth / this.aspectRatio,
				p = m.maxHeight * this.aspectRatio,
				n = m.maxWidth / this.aspectRatio,
				q > m.minWidth && (m.minWidth = q),
				o > m.minHeight && (m.minHeight = o),
				p < m.maxWidth && (m.maxWidth = p),
				n < m.maxHeight && (m.maxHeight = n)
			}
			this._vBoundaries = m
		},
		_updateCache : function (k) {
			var c = this.options;
			this.offset = this.helper.offset(),
			h(k.left) && (this.position.left = k.left),
			h(k.top) && (this.position.top = k.top),
			h(k.height) && (this.size.height = k.height),
			h(k.width) && (this.size.width = k.width)
		},
		_updateRatio : function (l, k) {
			var p = this.options,
			o = this.position,
			n = this.size,
			m = this.axis;
			return h(l.height) ? l.width = l.height * this.aspectRatio : h(l.width) && (l.height = l.width / this.aspectRatio),
			m == "sw" && (l.left = o.left + (n.width - l.width), l.top = null),
			m == "nw" && (l.top = o.top + (n.height - l.height), l.left = o.left + (n.width - l.width)),
			l
		},
		_respectSize : function (G, F) {
			var E = this.helper,
			D = this._vBoundaries,
			C = this._aspectRatio || F.shiftKey,
			B = this.axis,
			A = h(G.width) && D.maxWidth && D.maxWidth < G.width,
			y = h(G.height) && D.maxHeight && D.maxHeight < G.height,
			x = h(G.width) && D.minWidth && D.minWidth > G.width,
			w = h(G.height) && D.minHeight && D.minHeight > G.height;
			x && (G.width = D.minWidth),
			w && (G.height = D.minHeight),
			A && (G.width = D.maxWidth),
			y && (G.height = D.maxHeight);
			var v = this.originalPosition.left + this.originalSize.width,
			u = this.position.top + this.size.height,
			t = /sw|nw|w/.test(B),
			r = /nw|ne|n/.test(B);
			x && t && (G.left = v - D.minWidth),
			A && t && (G.left = v - D.maxWidth),
			w && r && (G.top = u - D.minHeight),
			y && r && (G.top = u - D.maxHeight);
			var q = !G.width && !G.height;
			return q && !G.left && G.top ? G.top = null : q && !G.top && G.left && (G.left = null),
			G
		},
		_proportionallyResize : function () {
			var a = this.options;
			if (!this._proportionallyResizeElements.length) {
				return
			}
			var o = this.helper || this.element;
			for (var n = 0; n < this._proportionallyResizeElements.length; n++) {
				var m = this._proportionallyResizeElements[n];
				if (!this.borderDif) {
					var l = [m.css("borderTopWidth"), m.css("borderRightWidth"), m.css("borderBottomWidth"), m.css("borderLeftWidth")],
					k = [m.css("paddingTop"), m.css("paddingRight"), m.css("paddingBottom"), m.css("paddingLeft")];
					this.borderDif = f.map(l, function (q, p) {
							var t = parseInt(q, 10) || 0,
							r = parseInt(k[p], 10) || 0;
							return t + r
						})
				}
				if (!f.browser.msie || !f(o).is(":hidden") && !f(o).parents(":hidden").length) {
					m.css({
						height : o.height() - this.borderDif[0] - this.borderDif[2] || 0,
						width : o.width() - this.borderDif[1] - this.borderDif[3] || 0
					})
				} else {
					continue
				}
			}
		},
		_renderProxy : function () {
			var a = this.element,
			n = this.options;
			this.elementOffset = a.offset();
			if (this._helper) {
				this.helper = this.helper || f('<div style="overflow:hidden;"></div>');
				var m = f.browser.msie && f.browser.version < 7,
				l = m ? 1 : 0,
				k = m ? 2 : -1;
				this.helper.addClass(this._helper).css({
					width : this.element.outerWidth() + k,
					height : this.element.outerHeight() + k,
					position : "absolute",
					left : this.elementOffset.left - l + "px",
					top : this.elementOffset.top - l + "px",
					zIndex : ++n.zIndex
				}),
				this.helper.appendTo("body").disableSelection()
			} else {
				this.helper = this.element
			}
		},
		_change : {
			e : function (l, k, m) {
				return {
					width : this.originalSize.width + k
				}
			},
			w : function (l, k, p) {
				var o = this.options,
				n = this.originalSize,
				m = this.originalPosition;
				return {
					left : m.left + k,
					width : n.width - k
				}
			},
			n : function (l, k, p) {
				var o = this.options,
				n = this.originalSize,
				m = this.originalPosition;
				return {
					top : m.top + p,
					height : n.height - p
				}
			},
			s : function (l, k, m) {
				return {
					height : this.originalSize.height + m
				}
			},
			se : function (a, l, k) {
				return f.extend(this._change.s.apply(this, arguments), this._change.e.apply(this, [a, l, k]))
			},
			sw : function (a, l, k) {
				return f.extend(this._change.s.apply(this, arguments), this._change.w.apply(this, [a, l, k]))
			},
			ne : function (a, l, k) {
				return f.extend(this._change.n.apply(this, arguments), this._change.e.apply(this, [a, l, k]))
			},
			nw : function (a, l, k) {
				return f.extend(this._change.n.apply(this, arguments), this._change.w.apply(this, [a, l, k]))
			}
		},
		_propagate : function (a, k) {
			f.ui.plugin.call(this, a, [k, this.ui()]),
			a != "resize" && this._trigger(a, k, this.ui())
		},
		plugins : {},
		ui : function () {
			return {
				originalElement : this.originalElement,
				element : this.element,
				helper : this.helper,
				position : this.position,
				size : this.size,
				originalSize : this.originalSize,
				originalPosition : this.originalPosition
			}
		}
	}),
	f.extend(f.ui.resizable, {
		version : "1.8.20"
	}),
	f.ui.plugin.add("resizable", "alsoResize", {
		start : function (a, n) {
			var m = f(this).data("resizable"),
			l = m.options,
			k = function (c) {
				f(c).each(function () {
					var o = f(this);
					o.data("resizable-alsoresize", {
						width : parseInt(o.width(), 10),
						height : parseInt(o.height(), 10),
						left : parseInt(o.css("left"), 10),
						top : parseInt(o.css("top"), 10)
					})
				})
			};
			typeof l.alsoResize == "object" && !l.alsoResize.parentNode ? l.alsoResize.length ? (l.alsoResize = l.alsoResize[0], k(l.alsoResize)) : f.each(l.alsoResize, function (b) {
				k(b)
			}) : k(l.alsoResize)
		},
		resize : function (a, q) {
			var p = f(this).data("resizable"),
			o = p.options,
			n = p.originalSize,
			m = p.originalPosition,
			l = {
				height : p.size.height - n.height || 0,
				width : p.size.width - n.width || 0,
				top : p.position.top - m.top || 0,
				left : p.position.left - m.left || 0
			},
			k = function (c, r) {
				f(c).each(function () {
					var t = f(this),
					w = f(this).data("resizable-alsoresize"),
					v = {},
					u = r && r.length ? r : t.parents(q.originalElement[0]).length ? ["width", "height"] : ["width", "height", "top", "left"];
					f.each(u, function (y, x) {
						var A = (w[x] || 0) + (l[x] || 0);
						A && A >= 0 && (v[x] = A || null)
					}),
					t.css(v)
				})
			};
			typeof o.alsoResize == "object" && !o.alsoResize.nodeType ? f.each(o.alsoResize, function (r, c) {
				k(r, c)
			}) : k(o.alsoResize)
		},
		stop : function (a, k) {
			f(this).removeData("resizable-alsoresize")
		}
	}),
	f.ui.plugin.add("resizable", "animate", {
		stop : function (w, v) {
			var u = f(this).data("resizable"),
			t = u.options,
			r = u._proportionallyResizeElements,
			q = r.length && /textarea/i.test(r[0].nodeName),
			p = q && f.ui.hasScroll(r[0], "left") ? 0 : u.sizeDiff.height,
			o = q ? 0 : u.sizeDiff.width,
			n = {
				width : u.size.width - o,
				height : u.size.height - p
			},
			m = parseInt(u.element.css("left"), 10) + (u.position.left - u.originalPosition.left) || null,
			a = parseInt(u.element.css("top"), 10) + (u.position.top - u.originalPosition.top) || null;
			u.element.animate(f.extend(n, a && m ? {
					top : a,
					left : m
				}
					 : {}), {
				duration : t.animateDuration,
				easing : t.animateEasing,
				step : function () {
					var b = {
						width : parseInt(u.element.css("width"), 10),
						height : parseInt(u.element.css("height"), 10),
						top : parseInt(u.element.css("top"), 10),
						left : parseInt(u.element.css("left"), 10)
					};
					r && r.length && f(r[0]).css({
						width : b.width,
						height : b.height
					}),
					u._updateCache(b),
					u._propagate("resize", w)
				}
			})
		}
	}),
	f.ui.plugin.add("resizable", "containment", {
		start : function (D, C) {
			var B = f(this).data("resizable"),
			A = B.options,
			y = B.element,
			x = A.containment,
			w = x instanceof f ? x.get(0) : /parent/.test(x) ? y.parent().get(0) : x;
			if (!w) {
				return
			}
			B.containerElement = f(w);
			if (/document/.test(x) || x == document) {
				B.containerOffset = {
					left : 0,
					top : 0
				},
				B.containerPosition = {
					left : 0,
					top : 0
				},
				B.parentData = {
					element : f(document),
					left : 0,
					top : 0,
					width : f(document).width(),
					height : f(document).height() || document.body.parentNode.scrollHeight
				}
			} else {
				var v = f(w),
				u = [];
				f(["Top", "Right", "Left", "Bottom"]).each(function (l, k) {
					u[l] = j(v.css("padding" + k))
				}),
				B.containerOffset = v.offset(),
				B.containerPosition = v.position(),
				B.containerSize = {
					height : v.innerHeight() - u[3],
					width : v.innerWidth() - u[1]
				};
				var t = B.containerOffset,
				r = B.containerSize.height,
				q = B.containerSize.width,
				c = f.ui.hasScroll(w, "left") ? w.scrollWidth : q,
				a = f.ui.hasScroll(w) ? w.scrollHeight : r;
				B.parentData = {
					element : w,
					left : t.left,
					top : t.top,
					width : c,
					height : a
				}
			}
		},
		resize : function (F, E) {
			var D = f(this).data("resizable"),
			C = D.options,
			B = D.containerSize,
			A = D.containerOffset,
			y = D.size,
			x = D.position,
			w = D._aspectRatio || F.shiftKey,
			v = {
				top : 0,
				left : 0
			},
			u = D.containerElement;
			u[0] != document && /static/.test(u.css("position")) && (v = A),
			x.left < (D._helper ? A.left : 0) && (D.size.width = D.size.width + (D._helper ? D.position.left - A.left : D.position.left - v.left), w && (D.size.height = D.size.width / D.aspectRatio), D.position.left = C.helper ? A.left : 0),
			x.top < (D._helper ? A.top : 0) && (D.size.height = D.size.height + (D._helper ? D.position.top - A.top : D.position.top), w && (D.size.width = D.size.height * D.aspectRatio), D.position.top = D._helper ? A.top : 0),
			D.offset.left = D.parentData.left + D.position.left,
			D.offset.top = D.parentData.top + D.position.top;
			var t = Math.abs((D._helper ? D.offset.left - v.left : D.offset.left - v.left) + D.sizeDiff.width),
			r = Math.abs((D._helper ? D.offset.top - v.top : D.offset.top - A.top) + D.sizeDiff.height),
			q = D.containerElement.get(0) == D.element.parent().get(0),
			a = /relative|absolute/.test(D.containerElement.css("position"));
			q && a && (t -= D.parentData.left),
			t + D.size.width >= D.parentData.width && (D.size.width = D.parentData.width - t, w && (D.size.height = D.size.width / D.aspectRatio)),
			r + D.size.height >= D.parentData.height && (D.size.height = D.parentData.height - r, w && (D.size.width = D.size.height * D.aspectRatio))
		},
		stop : function (y, x) {
			var w = f(this).data("resizable"),
			v = w.options,
			u = w.position,
			t = w.containerOffset,
			r = w.containerPosition,
			q = w.containerElement,
			p = f(w.helper),
			o = p.offset(),
			n = p.outerWidth() - w.sizeDiff.width,
			a = p.outerHeight() - w.sizeDiff.height;
			w._helper && !v.animate && /relative/.test(q.css("position")) && f(this).css({
				left : o.left - r.left - t.left,
				width : n,
				height : a
			}),
			w._helper && !v.animate && /static/.test(q.css("position")) && f(this).css({
				left : o.left - r.left - t.left,
				width : n,
				height : a
			})
		}
	}),
	f.ui.plugin.add("resizable", "ghost", {
		start : function (a, n) {
			var m = f(this).data("resizable"),
			l = m.options,
			k = m.size;
			m.ghost = m.originalElement.clone(),
			m.ghost.css({
				opacity : 0.25,
				display : "block",
				position : "relative",
				height : k.height,
				width : k.width,
				margin : 0,
				left : 0,
				top : 0
			}).addClass("ui-resizable-ghost").addClass(typeof l.ghost == "string" ? l.ghost : ""),
			m.ghost.appendTo(m.helper)
		},
		resize : function (a, m) {
			var l = f(this).data("resizable"),
			k = l.options;
			l.ghost && l.ghost.css({
				position : "relative",
				height : l.size.height,
				width : l.size.width
			})
		},
		stop : function (a, m) {
			var l = f(this).data("resizable"),
			k = l.options;
			l.ghost && l.helper && l.helper.get(0).removeChild(l.ghost.get(0))
		}
	}),
	f.ui.plugin.add("resizable", "grid", {
		resize : function (w, v) {
			var u = f(this).data("resizable"),
			t = u.options,
			r = u.size,
			q = u.originalSize,
			p = u.originalPosition,
			o = u.axis,
			n = t._aspectRatio || w.shiftKey;
			t.grid = typeof t.grid == "number" ? [t.grid, t.grid] : t.grid;
			var m = Math.round((r.width - q.width) / (t.grid[0] || 1)) * (t.grid[0] || 1),
			a = Math.round((r.height - q.height) / (t.grid[1] || 1)) * (t.grid[1] || 1);
			/^(se|s|e)$/.test(o) ? (u.size.width = q.width + m, u.size.height = q.height + a) : /^(ne)$/.test(o) ? (u.size.width = q.width + m, u.size.height = q.height + a, u.position.top = p.top - a) : /^(sw)$/.test(o) ? (u.size.width = q.width + m, u.size.height = q.height + a, u.position.left = p.left - m) : (u.size.width = q.width + m, u.size.height = q.height + a, u.position.top = p.top - a, u.position.left = p.left - m)
		}
	});
	var j = function (b) {
		return parseInt(b, 10) || 0
	},
	h = function (b) {
		return !isNaN(parseInt(b, 10))
	}
})(jQuery);
/*! jQuery UI - v1.8.20 - 2012-04-30
 * https://github.com/jquery/jquery-ui
 * Includes: jquery.ui.selectable.js
 * Copyright (c) 2012 AUTHORS.txt; Licensed MIT, GPL */
(function (e, c) {
	e.widget("ui.selectable", e.ui.mouse, {
		options : {
			appendTo : "body",
			autoRefresh : !0,
			distance : 0,
			filter : "*",
			tolerance : "touch"
		},
		_create : function () {
			var a = this;
			this.element.addClass("ui-selectable"),
			this.dragged = !1;
			var f;
			this.refresh = function () {
				f = e(a.options.filter, a.element[0]),
				f.addClass("ui-selectee"),
				f.each(function () {
					var h = e(this),
					j = h.offset();
					e.data(this, "selectable-item", {
						element : this,
						$element : h,
						left : j.left,
						top : j.top,
						right : j.left + h.outerWidth(),
						bottom : j.top + h.outerHeight(),
						startselected : !1,
						selected : h.hasClass("ui-selected"),
						selecting : h.hasClass("ui-selecting"),
						unselecting : h.hasClass("ui-unselecting")
					})
				})
			},
			this.refresh(),
			this.selectees = f.addClass("ui-selectee"),
			this._mouseInit(),
			this.helper = e("<div class='ui-selectable-helper'></div>")
		},
		destroy : function () {
			return this.selectees.removeClass("ui-selectee").removeData("selectable-item"),
			this.element.removeClass("ui-selectable ui-selectable-disabled").removeData("selectable").unbind(".selectable"),
			this._mouseDestroy(),
			this
		},
		_mouseStart : function (a) {
			var h = this;
			this.opos = [a.pageX, a.pageY];
			if (this.options.disabled) {
				return
			}
			var f = this.options;
			this.selectees = e(f.filter, this.element[0]),
			this._trigger("start", a),
			e(f.appendTo).append(this.helper),
			this.helper.css({
				left : a.clientX,
				top : a.clientY,
				width : 0,
				height : 0
			}),
			f.autoRefresh && this.refresh(),
			this.selectees.filter(".ui-selected").each(function () {
				var b = e.data(this, "selectable-item");
				b.startselected = !0,
				!a.metaKey && !a.ctrlKey && (b.$element.removeClass("ui-selected"), b.selected = !1, b.$element.addClass("ui-unselecting"), b.unselecting = !0, h._trigger("unselecting", a, {
						unselecting : b.element
					}))
			}),
			e(a.target).parents().andSelf().each(function () {
				var j = e.data(this, "selectable-item");
				if (j) {
					var b = !a.metaKey && !a.ctrlKey || !j.$element.hasClass("ui-selected");
					return j.$element.removeClass(b ? "ui-unselecting" : "ui-selected").addClass(b ? "ui-selecting" : "ui-unselecting"),
					j.unselecting = !b,
					j.selecting = b,
					j.selected = b,
					b ? h._trigger("selecting", a, {
						selecting : j.element
					}) : h._trigger("unselecting", a, {
						unselecting : j.element
					}),
					!1
				}
			})
		},
		_mouseDrag : function (a) {
			var p = this;
			this.dragged = !0;
			if (this.options.disabled) {
				return
			}
			var o = this.options,
			n = this.opos[0],
			m = this.opos[1],
			l = a.pageX,
			k = a.pageY;
			if (n > l) {
				var j = l;
				l = n,
				n = j
			}
			if (m > k) {
				var j = k;
				k = m,
				m = j
			}
			return this.helper.css({
				left : n,
				top : m,
				width : l - n,
				height : k - m
			}),
			this.selectees.each(function () {
				var f = e.data(this, "selectable-item");
				if (!f || f.element == p.element[0]) {
					return
				}
				var b = !1;
				o.tolerance == "touch" ? b = !(f.left > l || f.right < n || f.top > k || f.bottom < m) : o.tolerance == "fit" && (b = f.left > n && f.right < l && f.top > m && f.bottom < k),
				b ? (f.selected && (f.$element.removeClass("ui-selected"), f.selected = !1), f.unselecting && (f.$element.removeClass("ui-unselecting"), f.unselecting = !1), f.selecting || (f.$element.addClass("ui-selecting"), f.selecting = !0, p._trigger("selecting", a, {
							selecting : f.element
						}))) : (f.selecting && ((a.metaKey || a.ctrlKey) && f.startselected ? (f.$element.removeClass("ui-selecting"), f.selecting = !1, f.$element.addClass("ui-selected"), f.selected = !0) : (f.$element.removeClass("ui-selecting"), f.selecting = !1, f.startselected && (f.$element.addClass("ui-unselecting"), f.unselecting = !0), p._trigger("unselecting", a, {
								unselecting : f.element
							}))), f.selected && !a.metaKey && !a.ctrlKey && !f.startselected && (f.$element.removeClass("ui-selected"), f.selected = !1, f.$element.addClass("ui-unselecting"), f.unselecting = !0, p._trigger("unselecting", a, {
							unselecting : f.element
						})))
			}),
			!1
		},
		_mouseStop : function (a) {
			var h = this;
			this.dragged = !1;
			var f = this.options;
			return e(".ui-unselecting", this.element[0]).each(function () {
				var b = e.data(this, "selectable-item");
				b.$element.removeClass("ui-unselecting"),
				b.unselecting = !1,
				b.startselected = !1,
				h._trigger("unselected", a, {
					unselected : b.element
				})
			}),
			e(".ui-selecting", this.element[0]).each(function () {
				var b = e.data(this, "selectable-item");
				b.$element.removeClass("ui-selecting").addClass("ui-selected"),
				b.selecting = !1,
				b.selected = !0,
				b.startselected = !0,
				h._trigger("selected", a, {
					selected : b.element
				})
			}),
			this._trigger("stop", a),
			this.helper.remove(),
			!1
		}
	}),
	e.extend(e.ui.selectable, {
		version : "1.8.20"
	})
})(jQuery);
/*! jQuery UI - v1.8.20 - 2012-04-30
 * https://github.com/jquery/jquery-ui
 * Includes: jquery.ui.sortable.js
 * Copyright (c) 2012 AUTHORS.txt; Licensed MIT, GPL */
(function (e, c) {
	e.widget("ui.sortable", e.ui.mouse, {
		widgetEventPrefix : "sort",
		ready : !1,
		options : {
			appendTo : "parent",
			axis : !1,
			connectWith : !1,
			containment : !1,
			cursor : "auto",
			cursorAt : !1,
			dropOnEmpty : !0,
			forcePlaceholderSize : !1,
			forceHelperSize : !1,
			grid : !1,
			handle : !1,
			helper : "original",
			items : "> *",
			opacity : !1,
			placeholder : !1,
			revert : !1,
			scroll : !0,
			scrollSensitivity : 20,
			scrollSpeed : 20,
			scope : "default",
			tolerance : "intersect",
			zIndex : 1000
		},
		_create : function () {
			var b = this.options;
			this.containerCache = {},
			this.element.addClass("ui-sortable"),
			this.refresh(),
			this.floating = this.items.length ? b.axis === "x" || /left|right/.test(this.items[0].item.css("float")) || /inline|table-cell/.test(this.items[0].item.css("display")) : !1,
			this.offset = this.element.offset(),
			this._mouseInit(),
			this.ready = !0
		},
		destroy : function () {
			e.Widget.prototype.destroy.call(this),
			this.element.removeClass("ui-sortable ui-sortable-disabled"),
			this._mouseDestroy();
			for (var a = this.items.length - 1; a >= 0; a--) {
				this.items[a].item.removeData(this.widgetName + "-item")
			}
			return this
		},
		_setOption : function (a, f) {
			a === "disabled" ? (this.options[a] = f, this.widget()[f ? "addClass" : "removeClass"]("ui-sortable-disabled")) : e.Widget.prototype._setOption.apply(this, arguments)
		},
		_mouseCapture : function (a, o) {
			var n = this;
			if (this.reverting) {
				return !1
			}
			if (this.options.disabled || this.options.type == "static") {
				return !1
			}
			this._refreshItems(a);
			var m = null,
			l = this,
			k = e(a.target).parents().each(function () {
					if (e.data(this, n.widgetName + "-item") == l) {
						return m = e(this),
						!1
					}
				});
			e.data(a.target, n.widgetName + "-item") == l && (m = e(a.target));
			if (!m) {
				return !1
			}
			if (this.options.handle && !o) {
				var j = !1;
				e(this.options.handle, m).find("*").andSelf().each(function () {
					this == a.target && (j = !0)
				});
				if (!j) {
					return !1
				}
			}
			return this.currentItem = m,
			this._removeCurrentsFromItems(),
			!0
		},
		_mouseStart : function (a, m, l) {
			var k = this.options,
			j = this;
			this.currentContainer = this,
			this.refreshPositions(),
			this.helper = this._createHelper(a),
			this._cacheHelperProportions(),
			this._cacheMargins(),
			this.scrollParent = this.helper.scrollParent(),
			this.offset = this.currentItem.offset(),
			this.offset = {
				top : this.offset.top - this.margins.top,
				left : this.offset.left - this.margins.left
			},
			this.helper.css("position", "absolute"),
			this.cssPosition = this.helper.css("position"),
			e.extend(this.offset, {
				click : {
					left : a.pageX - this.offset.left,
					top : a.pageY - this.offset.top
				},
				parent : this._getParentOffset(),
				relative : this._getRelativeOffset()
			}),
			this.originalPosition = this._generatePosition(a),
			this.originalPageX = a.pageX,
			this.originalPageY = a.pageY,
			k.cursorAt && this._adjustOffsetFromHelper(k.cursorAt),
			this.domPosition = {
				prev : this.currentItem.prev()[0],
				parent : this.currentItem.parent()[0]
			},
			this.helper[0] != this.currentItem[0] && this.currentItem.hide(),
			this._createPlaceholder(),
			k.containment && this._setContainment(),
			k.cursor && (e("body").css("cursor") && (this._storedCursor = e("body").css("cursor")), e("body").css("cursor", k.cursor)),
			k.opacity && (this.helper.css("opacity") && (this._storedOpacity = this.helper.css("opacity")), this.helper.css("opacity", k.opacity)),
			k.zIndex && (this.helper.css("zIndex") && (this._storedZIndex = this.helper.css("zIndex")), this.helper.css("zIndex", k.zIndex)),
			this.scrollParent[0] != document && this.scrollParent[0].tagName != "HTML" && (this.overflowOffset = this.scrollParent.offset()),
			this._trigger("start", a, this._uiHash()),
			this._preserveHelperProportions || this._cacheHelperProportions();
			if (!l) {
				for (var h = this.containers.length - 1; h >= 0; h--) {
					this.containers[h]._trigger("activate", a, j._uiHash(this))
				}
			}
			return e.ui.ddmanager && (e.ui.ddmanager.current = this),
			e.ui.ddmanager && !k.dropBehaviour && e.ui.ddmanager.prepareOffsets(this, a),
			this.dragging = !0,
			this.helper.addClass("ui-sortable-helper"),
			this._mouseDrag(a),
			!0
		},
		_mouseDrag : function (a) {
			this.position = this._generatePosition(a),
			this.positionAbs = this._convertPositionTo("absolute"),
			this.lastPositionAbs || (this.lastPositionAbs = this.positionAbs);
			if (this.options.scroll) {
				var o = this.options,
				n = !1;
				this.scrollParent[0] != document && this.scrollParent[0].tagName != "HTML" ? (this.overflowOffset.top + this.scrollParent[0].offsetHeight - a.pageY < o.scrollSensitivity ? this.scrollParent[0].scrollTop = n = this.scrollParent[0].scrollTop + o.scrollSpeed : a.pageY - this.overflowOffset.top < o.scrollSensitivity && (this.scrollParent[0].scrollTop = n = this.scrollParent[0].scrollTop - o.scrollSpeed), this.overflowOffset.left + this.scrollParent[0].offsetWidth - a.pageX < o.scrollSensitivity ? this.scrollParent[0].scrollLeft = n = this.scrollParent[0].scrollLeft + o.scrollSpeed : a.pageX - this.overflowOffset.left < o.scrollSensitivity && (this.scrollParent[0].scrollLeft = n = this.scrollParent[0].scrollLeft - o.scrollSpeed)) : (a.pageY - e(document).scrollTop() < o.scrollSensitivity ? n = e(document).scrollTop(e(document).scrollTop() - o.scrollSpeed) : e(window).height() - (a.pageY - e(document).scrollTop()) < o.scrollSensitivity && (n = e(document).scrollTop(e(document).scrollTop() + o.scrollSpeed)), a.pageX - e(document).scrollLeft() < o.scrollSensitivity ? n = e(document).scrollLeft(e(document).scrollLeft() - o.scrollSpeed) : e(window).width() - (a.pageX - e(document).scrollLeft()) < o.scrollSensitivity && (n = e(document).scrollLeft(e(document).scrollLeft() + o.scrollSpeed))),
				n !== !1 && e.ui.ddmanager && !o.dropBehaviour && e.ui.ddmanager.prepareOffsets(this, a)
			}
			this.positionAbs = this._convertPositionTo("absolute");
			if (!this.options.axis || this.options.axis != "y") {
				this.helper[0].style.left = this.position.left + "px"
			}
			if (!this.options.axis || this.options.axis != "x") {
				this.helper[0].style.top = this.position.top + "px"
			}
			for (var m = this.items.length - 1; m >= 0; m--) {
				var l = this.items[m],
				k = l.item[0],
				j = this._intersectsWithPointer(l);
				if (!j) {
					continue
				}
				if (k != this.currentItem[0] && this.placeholder[j == 1 ? "next" : "prev"]()[0] != k && !e.ui.contains(this.placeholder[0], k) && (this.options.type == "semi-dynamic" ? !e.ui.contains(this.element[0], k) : !0)) {
					this.direction = j == 1 ? "down" : "up";
					if (this.options.tolerance == "pointer" || this._intersectsWithSides(l)) {
						this._rearrange(a, l)
					} else {
						break
					}
					this._trigger("change", a, this._uiHash());
					break
				}
			}
			return this._contactContainers(a),
			e.ui.ddmanager && e.ui.ddmanager.drag(this, a),
			this._trigger("sort", a, this._uiHash()),
			this.lastPositionAbs = this.positionAbs,
			!1
		},
		_mouseStop : function (a, j) {
			if (!a) {
				return
			}
			e.ui.ddmanager && !this.options.dropBehaviour && e.ui.ddmanager.drop(this, a);
			if (this.options.revert) {
				var h = this,
				f = h.placeholder.offset();
				h.reverting = !0,
				e(this.helper).animate({
					left : f.left - this.offset.parent.left - h.margins.left + (this.offsetParent[0] == document.body ? 0 : this.offsetParent[0].scrollLeft),
					top : f.top - this.offset.parent.top - h.margins.top + (this.offsetParent[0] == document.body ? 0 : this.offsetParent[0].scrollTop)
				}, parseInt(this.options.revert, 10) || 500, function () {
					h._clear(a)
				})
			} else {
				this._clear(a, j)
			}
			return !1
		},
		cancel : function () {
			var a = this;
			if (this.dragging) {
				this._mouseUp({
					target : null
				}),
				this.options.helper == "original" ? this.currentItem.css(this._storedCSS).removeClass("ui-sortable-helper") : this.currentItem.show();
				for (var f = this.containers.length - 1; f >= 0; f--) {
					this.containers[f]._trigger("deactivate", null, a._uiHash(this)),
					this.containers[f].containerCache.over && (this.containers[f]._trigger("out", null, a._uiHash(this)), this.containers[f].containerCache.over = 0)
				}
			}
			return this.placeholder && (this.placeholder[0].parentNode && this.placeholder[0].parentNode.removeChild(this.placeholder[0]), this.options.helper != "original" && this.helper && this.helper[0].parentNode && this.helper.remove(), e.extend(this, {
					helper : null,
					dragging : !1,
					reverting : !1,
					_noFinalSort : null
				}), this.domPosition.prev ? e(this.domPosition.prev).after(this.currentItem) : e(this.domPosition.parent).prepend(this.currentItem)),
			this
		},
		serialize : function (a) {
			var h = this._getItemsAsjQuery(a && a.connected),
			f = [];
			return a = a || {},
			e(h).each(function () {
				var b = (e(a.item || this).attr(a.attribute || "id") || "").match(a.expression || /(.+)[-=_](.+)/);
				b && f.push((a.key || b[1] + "[]") + "=" + (a.key && a.expression ? b[1] : b[2]))
			}),
			!f.length && a.key && f.push(a.key + "="),
			f.join("&")
		},
		toArray : function (a) {
			var h = this._getItemsAsjQuery(a && a.connected),
			f = [];
			return a = a || {},
			h.each(function () {
				f.push(e(a.item || this).attr(a.attribute || "id") || "")
			}),
			f
		},
		_intersectsWith : function (y) {
			var x = this.positionAbs.left,
			w = x + this.helperProportions.width,
			v = this.positionAbs.top,
			u = v + this.helperProportions.height,
			t = y.left,
			r = t + y.width,
			q = y.top,
			p = q + y.height,
			o = this.offset.click.top,
			n = this.offset.click.left,
			m = v + o > q && v + o < p && x + n > t && x + n < r;
			return this.options.tolerance == "pointer" || this.options.forcePointerForContainers || this.options.tolerance != "pointer" && this.helperProportions[this.floating ? "width" : "height"] > y[this.floating ? "width" : "height"] ? m : t < x + this.helperProportions.width / 2 && w - this.helperProportions.width / 2 < r && q < v + this.helperProportions.height / 2 && u - this.helperProportions.height / 2 < p
		},
		_intersectsWithPointer : function (a) {
			var m = this.options.axis === "x" || e.ui.isOverAxis(this.positionAbs.top + this.offset.click.top, a.top, a.height),
			l = this.options.axis === "y" || e.ui.isOverAxis(this.positionAbs.left + this.offset.click.left, a.left, a.width),
			k = m && l,
			j = this._getDragVerticalDirection(),
			h = this._getDragHorizontalDirection();
			return k ? this.floating ? h && h == "right" || j == "down" ? 2 : 1 : j && (j == "down" ? 2 : 1) : !1
		},
		_intersectsWithSides : function (a) {
			var l = e.ui.isOverAxis(this.positionAbs.top + this.offset.click.top, a.top + a.height / 2, a.height),
			k = e.ui.isOverAxis(this.positionAbs.left + this.offset.click.left, a.left + a.width / 2, a.width),
			j = this._getDragVerticalDirection(),
			h = this._getDragHorizontalDirection();
			return this.floating && h ? h == "right" && k || h == "left" && !k : j && (j == "down" && l || j == "up" && !l)
		},
		_getDragVerticalDirection : function () {
			var b = this.positionAbs.top - this.lastPositionAbs.top;
			return b != 0 && (b > 0 ? "down" : "up")
		},
		_getDragHorizontalDirection : function () {
			var b = this.positionAbs.left - this.lastPositionAbs.left;
			return b != 0 && (b > 0 ? "right" : "left")
		},
		refresh : function (b) {
			return this._refreshItems(b),
			this.refreshPositions(),
			this
		},
		_connectWith : function () {
			var b = this.options;
			return b.connectWith.constructor == String ? [b.connectWith] : b.connectWith
		},
		_getItemsAsjQuery : function (r) {
			var q = this,
			p = [],
			o = [],
			n = this._connectWith();
			if (n && r) {
				for (var m = n.length - 1; m >= 0; m--) {
					var l = e(n[m]);
					for (var k = l.length - 1; k >= 0; k--) {
						var a = e.data(l[k], this.widgetName);
						a && a != this && !a.options.disabled && o.push([e.isFunction(a.options.items) ? a.options.items.call(a.element) : e(a.options.items, a.element).not(".ui-sortable-helper").not(".ui-sortable-placeholder"), a])
					}
				}
			}
			o.push([e.isFunction(this.options.items) ? this.options.items.call(this.element, null, {
						options : this.options,
						item : this.currentItem
					}) : e(this.options.items, this.element).not(".ui-sortable-helper").not(".ui-sortable-placeholder"), this]);
			for (var m = o.length - 1; m >= 0; m--) {
				o[m][0].each(function () {
					p.push(this)
				})
			}
			return e(p)
		},
		_removeCurrentsFromItems : function () {
			var h = this.currentItem.find(":data(" + this.widgetName + "-item)");
			for (var f = 0; f < this.items.length; f++) {
				for (var j = 0; j < h.length; j++) {
					h[j] == this.items[f].item[0] && this.items.splice(f, 1)
				}
			}
		},
		_refreshItems : function (B) {
			this.items = [],
			this.containers = [this];
			var A = this.items,
			y = this,
			x = [[e.isFunction(this.options.items) ? this.options.items.call(this.element[0], B, {
						item : this.currentItem
					}) : e(this.options.items, this.element), this]],
			w = this._connectWith();
			if (w && this.ready) {
				for (var v = w.length - 1; v >= 0; v--) {
					var u = e(w[v]);
					for (var t = u.length - 1; t >= 0; t--) {
						var r = e.data(u[t], this.widgetName);
						r && r != this && !r.options.disabled && (x.push([e.isFunction(r.options.items) ? r.options.items.call(r.element[0], B, {
										item : this.currentItem
									}) : e(r.options.items, r.element), r]), this.containers.push(r))
					}
				}
			}
			for (var v = x.length - 1; v >= 0; v--) {
				var q = x[v][1],
				p = x[v][0];
				for (var t = 0, o = p.length; t < o; t++) {
					var a = e(p[t]);
					a.data(this.widgetName + "-item", q),
					A.push({
						item : a,
						instance : q,
						width : 0,
						height : 0,
						left : 0,
						top : 0
					})
				}
			}
		},
		refreshPositions : function (a) {
			this.offsetParent && this.helper && (this.offset.parent = this._getParentOffset());
			for (var l = this.items.length - 1; l >= 0; l--) {
				var k = this.items[l];
				if (k.instance != this.currentContainer && this.currentContainer && k.item[0] != this.currentItem[0]) {
					continue
				}
				var j = this.options.toleranceElement ? e(this.options.toleranceElement, k.item) : k.item;
				a || (k.width = j.outerWidth(), k.height = j.outerHeight());
				var h = j.offset();
				k.left = h.left,
				k.top = h.top
			}
			if (this.options.custom && this.options.custom.refreshContainers) {
				this.options.custom.refreshContainers.call(this)
			} else {
				for (var l = this.containers.length - 1; l >= 0; l--) {
					var h = this.containers[l].element.offset();
					this.containers[l].containerCache.left = h.left,
					this.containers[l].containerCache.top = h.top,
					this.containers[l].containerCache.width = this.containers[l].element.outerWidth(),
					this.containers[l].containerCache.height = this.containers[l].element.outerHeight()
				}
			}
			return this
		},
		_createPlaceholder : function (a) {
			var j = a || this,
			h = j.options;
			if (!h.placeholder || h.placeholder.constructor == String) {
				var f = h.placeholder;
				h.placeholder = {
					element : function () {
						var k = e(document.createElement(j.currentItem[0].nodeName)).addClass(f || j.currentItem[0].className + " ui-sortable-placeholder").removeClass("ui-sortable-helper")[0];
						return f || (k.style.visibility = "hidden"),
						k
					},
					update : function (l, k) {
						if (f && !h.forcePlaceholderSize) {
							return
						}
						k.height() || k.height(j.currentItem.innerHeight() - parseInt(j.currentItem.css("paddingTop") || 0, 10) - parseInt(j.currentItem.css("paddingBottom") || 0, 10)),
						k.width() || k.width(j.currentItem.innerWidth() - parseInt(j.currentItem.css("paddingLeft") || 0, 10) - parseInt(j.currentItem.css("paddingRight") || 0, 10))
					}
				}
			}
			j.placeholder = e(h.placeholder.element.call(j.element, j.currentItem)),
			j.currentItem.after(j.placeholder),
			h.placeholder.update(j, j.placeholder)
		},
		_contactContainers : function (r) {
			var q = null,
			p = null;
			for (var o = this.containers.length - 1; o >= 0; o--) {
				if (e.ui.contains(this.currentItem[0], this.containers[o].element[0])) {
					continue
				}
				if (this._intersectsWith(this.containers[o].containerCache)) {
					if (q && e.ui.contains(this.containers[o].element[0], q.element[0])) {
						continue
					}
					q = this.containers[o],
					p = o
				} else {
					this.containers[o].containerCache.over && (this.containers[o]._trigger("out", r, this._uiHash(this)), this.containers[o].containerCache.over = 0)
				}
			}
			if (!q) {
				return
			}
			if (this.containers.length === 1) {
				this.containers[p]._trigger("over", r, this._uiHash(this)),
				this.containers[p].containerCache.over = 1
			} else {
				if (this.currentContainer != this.containers[p]) {
					var n = 10000,
					m = null,
					l = this.positionAbs[this.containers[p].floating ? "left" : "top"];
					for (var k = this.items.length - 1; k >= 0; k--) {
						if (!e.ui.contains(this.containers[p].element[0], this.items[k].item[0])) {
							continue
						}
						var a = this.items[k][this.containers[p].floating ? "left" : "top"];
						Math.abs(a - l) < n && (n = Math.abs(a - l), m = this.items[k])
					}
					if (!m && !this.options.dropOnEmpty) {
						return
					}
					this.currentContainer = this.containers[p],
					m ? this._rearrange(r, m, null, !0) : this._rearrange(r, null, this.containers[p].element, !0),
					this._trigger("change", r, this._uiHash()),
					this.containers[p]._trigger("change", r, this._uiHash(this)),
					this.options.placeholder.update(this.currentContainer, this.placeholder),
					this.containers[p]._trigger("over", r, this._uiHash(this)),
					this.containers[p].containerCache.over = 1
				}
			}
		},
		_createHelper : function (a) {
			var h = this.options,
			f = e.isFunction(h.helper) ? e(h.helper.apply(this.element[0], [a, this.currentItem])) : h.helper == "clone" ? this.currentItem.clone() : this.currentItem;
			return f.parents("body").length || e(h.appendTo != "parent" ? h.appendTo : this.currentItem[0].parentNode)[0].appendChild(f[0]),
			f[0] == this.currentItem[0] && (this._storedCSS = {
					width : this.currentItem[0].style.width,
					height : this.currentItem[0].style.height,
					position : this.currentItem.css("position"),
					top : this.currentItem.css("top"),
					left : this.currentItem.css("left")
				}),
			(f[0].style.width == "" || h.forceHelperSize) && f.width(this.currentItem.width()),
			(f[0].style.height == "" || h.forceHelperSize) && f.height(this.currentItem.height()),
			f
		},
		_adjustOffsetFromHelper : function (a) {
			typeof a == "string" && (a = a.split(" ")),
			e.isArray(a) && (a = {
					left : +a[0],
					top : +a[1] || 0
				}),
			"left" in a && (this.offset.click.left = a.left + this.margins.left),
			"right" in a && (this.offset.click.left = this.helperProportions.width - a.right + this.margins.left),
			"top" in a && (this.offset.click.top = a.top + this.margins.top),
			"bottom" in a && (this.offset.click.top = this.helperProportions.height - a.bottom + this.margins.top)
		},
		_getParentOffset : function () {
			this.offsetParent = this.helper.offsetParent();
			var a = this.offsetParent.offset();
			this.cssPosition == "absolute" && this.scrollParent[0] != document && e.ui.contains(this.scrollParent[0], this.offsetParent[0]) && (a.left += this.scrollParent.scrollLeft(), a.top += this.scrollParent.scrollTop());
			if (this.offsetParent[0] == document.body || this.offsetParent[0].tagName && this.offsetParent[0].tagName.toLowerCase() == "html" && e.browser.msie) {
				a = {
					top : 0,
					left : 0
				}
			}
			return {
				top : a.top + (parseInt(this.offsetParent.css("borderTopWidth"), 10) || 0),
				left : a.left + (parseInt(this.offsetParent.css("borderLeftWidth"), 10) || 0)
			}
		},
		_getRelativeOffset : function () {
			if (this.cssPosition == "relative") {
				var b = this.currentItem.position();
				return {
					top : b.top - (parseInt(this.helper.css("top"), 10) || 0) + this.scrollParent.scrollTop(),
					left : b.left - (parseInt(this.helper.css("left"), 10) || 0) + this.scrollParent.scrollLeft()
				}
			}
			return {
				top : 0,
				left : 0
			}
		},
		_cacheMargins : function () {
			this.margins = {
				left : parseInt(this.currentItem.css("marginLeft"), 10) || 0,
				top : parseInt(this.currentItem.css("marginTop"), 10) || 0
			}
		},
		_cacheHelperProportions : function () {
			this.helperProportions = {
				width : this.helper.outerWidth(),
				height : this.helper.outerHeight()
			}
		},
		_setContainment : function () {
			var a = this.options;
			a.containment == "parent" && (a.containment = this.helper[0].parentNode);
			if (a.containment == "document" || a.containment == "window") {
				this.containment = [0 - this.offset.relative.left - this.offset.parent.left, 0 - this.offset.relative.top - this.offset.parent.top, e(a.containment == "document" ? document : window).width() - this.helperProportions.width - this.margins.left, (e(a.containment == "document" ? document : window).height() || document.body.parentNode.scrollHeight) - this.helperProportions.height - this.margins.top]
			}
			if (!/^(document|window|parent)$/.test(a.containment)) {
				var j = e(a.containment)[0],
				h = e(a.containment).offset(),
				f = e(j).css("overflow") != "hidden";
				this.containment = [h.left + (parseInt(e(j).css("borderLeftWidth"), 10) || 0) + (parseInt(e(j).css("paddingLeft"), 10) || 0) - this.margins.left, h.top + (parseInt(e(j).css("borderTopWidth"), 10) || 0) + (parseInt(e(j).css("paddingTop"), 10) || 0) - this.margins.top, h.left + (f ? Math.max(j.scrollWidth, j.offsetWidth) : j.offsetWidth) - (parseInt(e(j).css("borderLeftWidth"), 10) || 0) - (parseInt(e(j).css("paddingRight"), 10) || 0) - this.helperProportions.width - this.margins.left, h.top + (f ? Math.max(j.scrollHeight, j.offsetHeight) : j.offsetHeight) - (parseInt(e(j).css("borderTopWidth"), 10) || 0) - (parseInt(e(j).css("paddingBottom"), 10) || 0) - this.helperProportions.height - this.margins.top]
			}
		},
		_convertPositionTo : function (a, m) {
			m || (m = this.position);
			var l = a == "absolute" ? 1 : -1,
			k = this.options,
			j = this.cssPosition == "absolute" && (this.scrollParent[0] == document || !e.ui.contains(this.scrollParent[0], this.offsetParent[0])) ? this.offsetParent : this.scrollParent,
			h = /(html|body)/i.test(j[0].tagName);
			return {
				top : m.top + this.offset.relative.top * l + this.offset.parent.top * l - (e.browser.safari && this.cssPosition == "fixed" ? 0 : (this.cssPosition == "fixed" ? -this.scrollParent.scrollTop() : h ? 0 : j.scrollTop()) * l),
				left : m.left + this.offset.relative.left * l + this.offset.parent.left * l - (e.browser.safari && this.cssPosition == "fixed" ? 0 : (this.cssPosition == "fixed" ? -this.scrollParent.scrollLeft() : h ? 0 : j.scrollLeft()) * l)
			}
		},
		_generatePosition : function (a) {
			var p = this.options,
			o = this.cssPosition == "absolute" && (this.scrollParent[0] == document || !e.ui.contains(this.scrollParent[0], this.offsetParent[0])) ? this.offsetParent : this.scrollParent,
			n = /(html|body)/i.test(o[0].tagName);
			this.cssPosition == "relative" && (this.scrollParent[0] == document || this.scrollParent[0] == this.offsetParent[0]) && (this.offset.relative = this._getRelativeOffset());
			var m = a.pageX,
			l = a.pageY;
			if (this.originalPosition) {
				this.containment && (a.pageX - this.offset.click.left < this.containment[0] && (m = this.containment[0] + this.offset.click.left), a.pageY - this.offset.click.top < this.containment[1] && (l = this.containment[1] + this.offset.click.top), a.pageX - this.offset.click.left > this.containment[2] && (m = this.containment[2] + this.offset.click.left), a.pageY - this.offset.click.top > this.containment[3] && (l = this.containment[3] + this.offset.click.top));
				if (p.grid) {
					var k = this.originalPageY + Math.round((l - this.originalPageY) / p.grid[1]) * p.grid[1];
					l = this.containment ? k - this.offset.click.top < this.containment[1] || k - this.offset.click.top > this.containment[3] ? k - this.offset.click.top < this.containment[1] ? k + p.grid[1] : k - p.grid[1] : k : k;
					var j = this.originalPageX + Math.round((m - this.originalPageX) / p.grid[0]) * p.grid[0];
					m = this.containment ? j - this.offset.click.left < this.containment[0] || j - this.offset.click.left > this.containment[2] ? j - this.offset.click.left < this.containment[0] ? j + p.grid[0] : j - p.grid[0] : j : j
				}
			}
			return {
				top : l - this.offset.click.top - this.offset.relative.top - this.offset.parent.top + (e.browser.safari && this.cssPosition == "fixed" ? 0 : this.cssPosition == "fixed" ? -this.scrollParent.scrollTop() : n ? 0 : o.scrollTop()),
				left : m - this.offset.click.left - this.offset.relative.left - this.offset.parent.left + (e.browser.safari && this.cssPosition == "fixed" ? 0 : this.cssPosition == "fixed" ? -this.scrollParent.scrollLeft() : n ? 0 : o.scrollLeft())
			}
		},
		_rearrange : function (j, h, n, m) {
			n ? n[0].appendChild(this.placeholder[0]) : h.item[0].parentNode.insertBefore(this.placeholder[0], this.direction == "down" ? h.item[0] : h.item[0].nextSibling),
			this.counter = this.counter ? ++this.counter : 1;
			var l = this,
			k = this.counter;
			window.setTimeout(function () {
				k == l.counter && l.refreshPositions(!m)
			}, 0)
		},
		_clear : function (a, l) {
			this.reverting = !1;
			var k = [],
			j = this;
			!this._noFinalSort && this.currentItem.parent().length && this.placeholder.before(this.currentItem),
			this._noFinalSort = null;
			if (this.helper[0] == this.currentItem[0]) {
				for (var h in this._storedCSS) {
					if (this._storedCSS[h] == "auto" || this._storedCSS[h] == "static") {
						this._storedCSS[h] = ""
					}
				}
				this.currentItem.css(this._storedCSS).removeClass("ui-sortable-helper")
			} else {
				this.currentItem.show()
			}
			this.fromOutside && !l && k.push(function (b) {
				this._trigger("receive", b, this._uiHash(this.fromOutside))
			}),
			(this.fromOutside || this.domPosition.prev != this.currentItem.prev().not(".ui-sortable-helper")[0] || this.domPosition.parent != this.currentItem.parent()[0]) && !l && k.push(function (b) {
				this._trigger("update", b, this._uiHash())
			});
			if (!e.ui.contains(this.element[0], this.currentItem[0])) {
				l || k.push(function (b) {
					this._trigger("remove", b, this._uiHash())
				});
				for (var h = this.containers.length - 1; h >= 0; h--) {
					e.ui.contains(this.containers[h].element[0], this.currentItem[0]) && !l && (k.push(function (b) {
							return function (f) {
								b._trigger("receive", f, this._uiHash(this))
							}
						}
							.call(this, this.containers[h])), k.push(function (b) {
							return function (f) {
								b._trigger("update", f, this._uiHash(this))
							}
						}
							.call(this, this.containers[h])))
				}
			}
			for (var h = this.containers.length - 1; h >= 0; h--) {
				l || k.push(function (b) {
					return function (f) {
						b._trigger("deactivate", f, this._uiHash(this))
					}
				}
					.call(this, this.containers[h])),
				this.containers[h].containerCache.over && (k.push(function (b) {
						return function (f) {
							b._trigger("out", f, this._uiHash(this))
						}
					}
						.call(this, this.containers[h])), this.containers[h].containerCache.over = 0)
			}
			this._storedCursor && e("body").css("cursor", this._storedCursor),
			this._storedOpacity && this.helper.css("opacity", this._storedOpacity),
			this._storedZIndex && this.helper.css("zIndex", this._storedZIndex == "auto" ? "" : this._storedZIndex),
			this.dragging = !1;
			if (this.cancelHelperRemoval) {
				if (!l) {
					this._trigger("beforeStop", a, this._uiHash());
					for (var h = 0; h < k.length; h++) {
						k[h].call(this, a)
					}
					this._trigger("stop", a, this._uiHash())
				}
				return !1
			}
			l || this._trigger("beforeStop", a, this._uiHash()),
			this.placeholder[0].parentNode.removeChild(this.placeholder[0]),
			this.helper[0] != this.currentItem[0] && this.helper.remove(),
			this.helper = null;
			if (!l) {
				for (var h = 0; h < k.length; h++) {
					k[h].call(this, a)
				}
				this._trigger("stop", a, this._uiHash())
			}
			return this.fromOutside = !1,
			!0
		},
		_trigger : function () {
			e.Widget.prototype._trigger.apply(this, arguments) === !1 && this.cancel()
		},
		_uiHash : function (a) {
			var f = a || this;
			return {
				helper : f.helper,
				placeholder : f.placeholder || e([]),
				position : f.position,
				originalPosition : f.originalPosition,
				offset : f.positionAbs,
				item : f.currentItem,
				sender : a ? a.element : null
			}
		}
	}),
	e.extend(e.ui.sortable, {
		version : "1.8.20"
	})
})(jQuery);
(function () {
	var c = window.Zving,
	b = c.isIE,
	a = {};
	a.wait = function () {
		var f = c.getDom("_WaitBGDiv");
		if (!f) {
			f = document.createElement("div");
			f.id = "_WaitBGDiv";
			f.style.display = "none";
			document.body.appendChild(f);
			f.style.cssText = "background-color:#333;position:absolute;left:0px;top:0px;opacity:0.03;filter:alpha(opacity=3);width:100%;height:100%;z-index:991"
		}
		var e = Math.max(document.documentElement.scrollHeight, document.body.scrollHeight);
		f.style.height = e + "px";
		f.style.display = "block"
	};
	a.endWait = function () {
		var e = c.getDom("_WaitBGDiv");
		if (e) {
			e.hide()
		}
	};
	a._Sort = function (f, e) {
		var j = f[1];
		var h = e[1];
		if (typeof(j) === "number") {
			if (typeof(h) === "number") {
				if (j > h) {
					return 1
				} else {
					if (j === h) {
						return 0
					} else {
						return -1
					}
				}
			}
			return -1
		} else {
			if (typeof(h) === "number") {
				return 1
			} else {
				return 0
			}
		}
	};
	a.loadComplete = false;
	a.loadFunctions = [];
	a.load = function (e) {
		a.loadComplete = true;
		a.loadFunctions.sort(a._Sort);
		var f;
		for (f = 0; f < a.loadFunctions.length; f++) {
			a.loadFunctions[f][0](e)
		}
		a.loadFunctions.length = 0
	};
	a.onLoad = function (f, e) {
		a.loadFunctions.push([f, e])
	};
	$(window).on("load", a.load);
	$(window).on("unload", function () {
		a.loadFunctions.length = 0;
		$(window).off("load", a.load)
	});
	a.readyFunctions = [];
	a.ready = function () {
		if (window._OnReady) {
			try {
				window._OnReady()
			} catch (f) {}

		}
		a.readyFunctions.sort(a._Sort);
		var e;
		for (e = 0; e < a.readyFunctions.length; e++) {
			a.readyFunctions[e][0]()
		}
		a.readyFunctions.length = 0
	};
	a.onReady = function (f, e) {
		a.readyFunctions.push([f, e])
	};
	a.isReady = false;
	a.bindReady = function (e) {
		if (a.isReady) {
			return
		}
		a.isReady = true;
		a.ready.call(window);
		if (document.removeEventListener) {
			document.removeEventListener("DOMContentLoaded", a.bindReady, false)
		} else {
			if (document.attachEvent) {
				document.detachEvent("onreadystatechange", a.bindReady);
				if (window == window.top) {
					clearInterval(a._Timer);
					a._Timer = null
				}
			}
		}
	};
	if (document.addEventListener) {
		document.addEventListener("DOMContentLoaded", a.bindReady, false)
	} else {
		if (document.attachEvent) {
			document.attachEvent("onreadystatechange", function () {
				if ((/loaded|complete/).test(document.readyState)) {
					a.bindReady()
				}
			});
			if (window == window.top) {
				a._Timer = setInterval(function () {
						try {
							if (!a.isReady) {
								document.documentElement.doScroll("left")
							}
						} catch (f) {
							return
						}
						a.bindReady()
					}, 16)
			}
		}
	}
	a.getScrollLeft = function (f) {
		f = f || window;
		var e = f.document;
		return f.pageXOffset || e.documentElement.scrollLeft || e.body.scrollLeft
	};
	a.getScrollTop = function (f) {
		f = f || window;
		var e = f.document;
		return f.pageYOffset || e.documentElement.scrollTop || e.body.scrollTop
	};
	a.scrollTo = function (f) {
		f = c.getDom(f);
		var e = f.x ? f.x : f.offsetLeft,
		h = f.y ? f.y : f.offsetTop;
		window.scrollTo(e, h)
	};
	c.Page = a
})();
(function () {
	var j = window.Zving,
	b = j.EventManager,
	e = j.getDom,
	a = {};
	j.idSeed = {
		_seed : 0
	};
	j.id = function (m) {
		var l = j.idSeed;
		if (!m) {
			return "id" + (++l._seed)
		}
		if (m.id) {
			return m.id
		}
		if (m.URL && m.readyState) {
			m.id = "z-document";
			return m.id
		} else {
			if (m.location && m.navigator) {
				m.id = "z-window";
				return m.id
			} else {
				if (m.nodeName) {
					if (l[m.nodeName] === undefined) {
						l[m.nodeName] = 0
					}
					if (!m.getAttribute("_id")) {
						m._id = m.nodeName.toLowerCase() + (++l[m.nodeName]);
						m.setAttribute("_id", m._id);
						return m._id
					} else {
						return m.getAttribute("_id")
					}
				}
			}
		}
		return j.Console.error(location.pathname + " ## Node.js -- id ::  参数错误，试图获取非DOM元素的id " + m)
	};
	var h = (function () {
		var l = {};
		l["float"] = j.isIE ? "styleFloat" : "cssFloat";
		function m(n) {
			return n.charAt(1).toUpperCase()
		}
		return function (n) {
			return l[n] ? l[n] : (l[n] = n.toLowerCase().replace(/-./g, m))
		}
	})();
	var f = function (o, q) {
		if (/px$/.test(q) || !q) {
			return parseInt(q, 10) || 0
		}
		var n = o.style.right,
		m = o.runtimeStyle.right;
		var l;
		var p = o.currentStyle;
		if (!p && !j.isIE) {
			p = j.rootWin.getComputedStyle(o, null)
		}
		o.runtimeStyle.right = p.right;
		o.style.right = q;
		l = o.style.pixelRight || 0;
		o.style.right = n;
		o.runtimeStyle.right = m;
		return l
	};
	a.getByTag = function (q, o) {
		q = e(q);
		q = q || document;
		o = o.toUpperCase();
		var p = q.getElementsByTagName(o);
		var m = [];
		var l = p.length;
		for (var n = 0; n < l; n++) {
			m.push(p[n])
		}
		return m
	};
	a.getAttr = function (n, l, m) {
		n = e(n);
		if ("href" != l && n[l] !== undefined) {
			return n[l]
		} else {
			return n.getAttribute(l, m || (n.tagName == "A" && l.toLowerCase() == "href" && 2) || null)
		}
	};
	a.setAttr = function (m, l, n) {
		m = e(m);
		if (l && typeof l === "object") {
			for (var o in l) {
				if (l.hasOwnProperty(o)) {
					a.setAttr(m, o, l[o])
				}
			}
		} else {
			if (m.setAttribute) {
				m.setAttribute(l, n)
			}
		}
		return m
	};
	a.attr = function (m, l, n) {
		if (typeof(n) === "undefined") {
			return a.getAttr(m, l)
		} else {
			return a.setAttr(m, l, n)
		}
	};
	a.setOpacity = function (l, m) {
		if ((m = Number(m)) < 1) {
			m *= 100
		}
		if (j.isIE) {
			l.style.filter = "alpha(opacity=" + m + ")";
			if (j.isIE6 && m > 98) {
				l.style.filter = ""
			}
		} else {
			l.style.opacity = m / 100
		}
		return l
	};
	a.getOpacity = function (l) {
		var m;
		if (j.isIE) {
			m = l.currentStyle.filter.indexOf("opacity=") >= 0 ? (parseFloat(l.currentStyle.filter.match(/opacity=([^)]*)/)[1])) + "" : "100"
		} else {
			m = 100 * l.ownerDocument.defaultView.getComputedStyle(l, null).opacity
		}
		return m
	};
	a.setStyle = function (m, l, n) {
		m = e(m);
		if (l && typeof l === "object") {
			for (var p in l) {
				if (l.hasOwnProperty(p)) {
					a.setStyle(m, p, l[p])
				}
			}
		} else {
			l = l.camelize();
			if (l == "float") {
				l = j.isIE ? "styleFloat" : "cssFloat"
			} else {
				if (l == "opacity") {
					return a.setOpacity(m, n)
				}
			}
			try {
				m.style[l] = n
			} catch (o) {
				console.log(m, "设置样式" + l + "为" + n + "失败")
			}
		}
		return m
	};
	a.getStyle = function (n, m) {
		n = e(n);
		m = j.String.camelize(m);
		if (m == "float") {
			m = j.isIE ? "styleFloat" : "cssFloat"
		} else {
			if (m == "opacity") {
				return a.getOpacity(n)
			}
		}
		var l = n.style[m];
		return (!l || l === "auto") ? null : l
	};
	a.getCurrentStyle = function (o, l) {
		o = e(o);
		l = j.String.camelize(l);
		if (l == "float") {
			l = j.isIE ? "styleFloat" : "cssFloat"
		} else {
			if (l == "opacity") {
				return a.getOpacity(o)
			}
		}
		if (o.currentStyle) {
			var p = o.currentStyle[l];
			if (p === "auto" && (l === "width" || l === "height")) {
				var n = o.getBoundingClientRect();
				if (l === "width") {
					return n.right - n.left + "px"
				} else {
					return n.bottom - n.top + "px"
				}
			}
			return p
		} else {
			var m = o.ownerDocument.defaultView.getComputedStyle(o, null);
			if (!m) {
				m = j.rootWin.getComputedStyle(o, null)
			}
			if (m) {
				return m[l]
			} else {
				return o.style[l]
			}
		}
	};
	a.css = function (m, l, n) {
		if (typeof(n) === "undefined") {
			return a.getCurrentStyle(m, l)
		} else {
			return a.setStyle(m, l, n)
		}
	};
	a.focusEx = function (m) {
		m = e(m);
		try {
			m.focus()
		} catch (l) {}

	};
	a.on = a.addListener = function (p, l, o, n, m) {
		if (typeof(l) !== "string" || typeof(o) !== "function") {
			throw "Node.js -- Node.on :: 传入addListener方法的参数错误"
		}
		p = e(p);
		p.ownerDocument.parentWindow.Zving.EventManager.on(p, l, o, n || p, m);
		return p
	};
	a.un = a.removeListener = function (o, l, n, m) {
		if (typeof(l) !== "string" || typeof(n) !== "function") {
			throw "Node.js -- Node.un :: 传入removeListener方法的参数错误"
		}
		o = e(o);
		o.ownerDocument.parentWindow.Zving.EventManager.removeListener(o, l, n, m || o);
		return o
	};
	a.fire = function (n, m) {
		n = e(n);
		if (n.fireEvent) {
			return n.fireEvent(m)
		} else {
			var l;
			evtName = evtName.substring(2);
			var o = n.ownerDocument || document;
			if (/mouse|click/i.test(evtName)) {
				l = o.createEvent("MouseEvents");
				l.initMouseEvent(evtName, true, true, o.defaultView, 1, 0, 0, 0, 0, false, false, false, false, 0, null)
			} else {
				l = o.createEvent("Events");
				l.initEvent(evtName, true, true)
			}
			return this.dispatchEvent(l)
		}
	};
	a.hover = function (m, n, l) {
		m = e(m);
		a.on(m, "onmouseenter", n);
		a.on(m, "onmouseleave", l || n);
		return m
	};
	a.isVisible = function (m) {
		m = e(m);
		var l = m.currentStyle;
		if (!l && !j.isIE) {
			l = j.rootWin.getComputedStyle(m, null);
			if (!l) {
				return false
			}
		}
		if (l.visibility === "hidden" || l.display === "none") {
			return false
		} else {
			if (m.offsetWidth === 0 && m.offsetHeight === 0 && m.tagName !== "TR") {
				return false
			}
		}
		return true
	};
	a.hide = function (m) {
		m = e(m);
		if (m.tagName.toLowerCase() === "input" && m.type === "button") {
			if (m.parentNode && m.parentNode.getAttribute("ztype") === "zInputBtnWrapper") {
				m.parentNode.style.display = "none"
			}
		}
		var l = m.currentStyle;
		if (!l && !j.isIE) {
			l = j.rootWin.getComputedStyle(m, null)
		}
		if (l && l.display !== "none") {
			m._style_display = l.display
		}
		m.style.display = "none";
		return m
	};
	a.show = function (o, p) {
		o = e(o);
		var n = j.isIE6 ? {
			li : "list-item",
			thead : "table-header-group",
			tfoot : "table-footer-group",
			button : "inline-block",
			textarea : "inline-block",
			input : "inline-block",
			select : "inline-block"
		}
		 : {
			li : "list-item",
			table : "table",
			tr : "table-row",
			thead : "table-header-group",
			tbody : "table-row-group",
			tfoot : "table-footer-group",
			col : "table-column",
			colgroup : "table-column-group",
			td : "table-cell",
			caption : "table-caption",
			button : "inline-block",
			textarea : "inline-block",
			input : "inline-block",
			select : "inline-block"
		};
		if (o.tagName.toLowerCase() === "input" && o.type === "button") {
			if (o.parentNode && o.parentNode.getAttribute("ztype") === "zInputBtnWrapper") {
				o.parentNode.style.display = ""
			}
		}
		var l,
		m = o.tagName.toLowerCase();
		if (p) {
			l = p
		} else {
			if (o._style_display) {
				l = o._style_display
			} else {
				if (n[m]) {
					l = n[m]
				} else {
					if (/^(body|div|dl|dt|dd|fieldset|form|h\d|ol|p|ul|center|hr|menu|pre)$/i.test(m)) {
						l = "block"
					} else {
						l = ""
					}
				}
			}
		}
		o.style.display = l;
		return o
	};
	a.toggle = function (l, m) {
		l = e(l);
		if (a.isVisible(l)) {
			a.hide(l)
		} else {
			a.show(l, m)
		}
		return l
	};
	a.unselectable = function (l) {
		l = e(l);
		l.unselectable = "on";
		l.onselectstart = function (m) {
			m = m || window.event;
			m.stopPropagation();
			if (preventDefault) {
				m.preventDefault()
			}
			return false
		};
		l.style["-moz-user-select"] = "none";
		l.style["-khtml-user-select"] = "none";
		return l
	};
	a.disable = function (o, l) {
		o = e(o);
		if (o.tagName.toLowerCase() === "form") {
			var p = o.elements;
			for (var n = 0; n < p.length; n++) {
				try {
					a.disable(p[n])
				} catch (m) {
					alert("error Node.disable():" + p[n].outerHTML)
				}
			}
		} else {
			if (o.getAttribute("ztype") && o.getAttribute("ztype").toLowerCase() === "select") {
				j.Selector.setDisabled(o, true)
			} else {
				if (a.hasClass(o, "z-btn")) {
					a.addClass(o, "z-btn-disabled");
					if (o.onclick) {
						o._onclick_ = o.onclick
					}
					o.onclick = null;
					o._disabled = true
				} else {
					a.addClass(o, "disabled");
					o.disabled = "true";
					if (o.getAttribute("ztype") && /date|time/i.test(o.getAttribute("ztype"))) {
						j.DateTime.setDisabled(o, true)
					}
				}
			}
		}
		return o
	};
	a.enable = function (o) {
		o = e(o);
		if (o.tagName.toLowerCase() === "form") {
			var p = o.elements;
			for (var n = 0; n < p.length; n++) {
				var m = p[n];
				try {
					a.enable(p[n])
				} catch (l) {
					alert("enable():" + p[n].outerHTML)
				}
			}
		} else {
			if (o.getAttribute("ztype") && o.getAttribute("ztype").toLowerCase() === "select") {
				j.Selector.setDisabled(o, false)
			} else {
				if (a.hasClass(o, "z-btn-disabled")) {
					a.removeClass(o, "z-btn-disabled");
					if (o._onclick_) {
						o.onclick = o._onclick_
					}
					o.disabled = null
				} else {
					if (a.hasClass(o, "disabled")) {
						a.removeClass(o, "disabled")
					}
					o.disabled = null;
					if (o.getAttribute("ztype") && /date|time/i.test(o.getAttribute("ztype"))) {
						j.DateTime.setDisabled(o, false)
					}
				}
			}
		}
		return o
	};
	a.getData = function (m, l) {
		m = e(m);
		var o = m.ownerDocument.parentWindow.Zving.elCache[j.id(m)];
		if (!o) {
			return
		}
		var n = m.ownerDocument.parentWindow.Zving.elCache[j.id(m)].data;
		return n[l]
	};
	a.setData = function (m, l, n) {
		m = e(m);
		var p = m.ownerDocument.parentWindow.Zving.elCache[j.id(m)];
		if (!p) {
			j.Dom.addToCache(m)
		}
		var o = m.ownerDocument.parentWindow.Zving.elCache[j.id(m)].data;
		o[l] = n;
		return m
	};
	a.removeData = function (m, l) {
		m = e(m);
		var n = m.ownerDocument.parentWindow.Zving.elCache[j.id(m)].data;
		delete n[l];
		return m
	};
	a.data = function (m, l, n) {
		if (typeof(n) === "undefined") {
			return a.getData(m, l)
		} else {
			return a.setData(m, l, n)
		}
	};
	a.getPadding = function (l) {
		el = e(l);
		return [f(el, a.getCurrentStyle(el, "paddingTop")), f(el, a.getCurrentStyle(el, "paddingRight")), f(el, a.getCurrentStyle(el, "paddingBottom")), f(el, a.getCurrentStyle(el, "paddingLeft"))]
	};
	a.getMargin = function (l) {
		el = e(l);
		return [f(el, a.getCurrentStyle(el, "marginTop")), f(el, a.getCurrentStyle(el, "marginRight")), f(el, a.getCurrentStyle(el, "marginBottom")), f(el, a.getCurrentStyle(el, "marginLeft"))]
	};
	a.getOuterW = function (p) {
		p = e(p);
		var q = {
			thin : 2,
			medium : 4,
			thick : 6
		};
		var o = p.currentStyle;
		if (!o && !j.isIE) {
			o = j.rootWin.getComputedStyle(p, null)
		}
		var l = o.borderLeftWidth;
		l = o.borderLeftStyle == "none" ? 0 : (q[l] || parseFloat(l) || 0);
		var m = o.borderRightWidth;
		m = o.borderRightStyle == "none" ? 0 : (q[m] || parseFloat(m) || 0);
		var n = o.paddingLeft;
		n = parseFloat(n) || 0;
		var r = o.paddingRight;
		r = parseFloat(r) || 0;
		return l + m + n + r
	};
	a.getOuterH = function (p) {
		p = e(p);
		var r = {
			thin : 2,
			medium : 4,
			thick : 6
		};
		var o = p.currentStyle;
		if (!o && !j.isIE) {
			o = j.rootWin.getComputedStyle(p, null)
		}
		var n = o.borderTopWidth;
		n = o.borderTopStyle == "none" ? 0 : (r[n] || parseFloat(n) || 0);
		var l = o.borderBottomWidth;
		l = o.borderBottomStyle == "none" ? 0 : (r[l] || parseFloat(l) || 0);
		var q = o.paddingTop;
		q = parseFloat(q) || 0;
		var m = o.paddingBottom;
		m = parseFloat(m) || 0;
		return n + l + q + m
	};
	a.getContentSize = function (l) {
		l = e(l);
		var m = a.getSize(l);
		return {
			width : m.width - a.getOuterW(l),
			height : m.height - a.getOuterH(l)
		}
	};
	a.getDimensions = a.getSize = function (p) {
		p = e(p);
		var r;
		if ((/^(head|meta|script|link|area|param)$/i).test(p.tagName)) {
			r = {
				width : 0,
				height : 0
			}
		} else {
			if (a.getCurrentStyle(p, "display") === "none") {
				var o = p.style;
				var q = o.visibility;
				var t = o.position;
				var m = o.display;
				o.visibility = "hidden";
				o.position = "absolute";
				switch (p.tagName.toLowerCase()) {
				case "table":
					o.display = "table";
					break;
				case "tr":
					o.display = "table-row";
					break;
				case "td":
				case "th":
					o.display = "table-cell";
					break;
				case "button":
				case "textarea":
				case "input":
				case "select":
					o.display = "inline-block";
					break;
				default:
					o.display = "block"
				}
				var l = p.offsetWidth;
				var n = p.offsetHeight;
				o.display = m;
				o.position = t;
				o.visibility = q;
				r = {
					width : l,
					height : n
				}
			} else {
				r = {
					width : p.offsetWidth,
					height : p.offsetHeight
				}
			}
		}
		return r
	};
	a.setSize = function (n, l, m) {
		n = e(n);
		if (typeof m == "undefined" && j.isObject(l)) {
			m = l.height;
			l = l.width
		}
		if (typeof l == "string" && !/^\d+\.?\d*(px)?$/.test(l)) {
			n.style.width = l
		} else {
			l = parseFloat(l)
		}
		if (typeof m == "string" && !/^\d+\.?\d*(px)?$/.test(m)) {
			n.style.height = m
		} else {
			m = parseFloat(m)
		}
		if (!isNaN(l)) {
			n.style.width = (j.isBorderBox ? l : Math.max(+l - a.getOuterW(n), 0)) + "px"
		}
		if (!isNaN(m)) {
			n.style.height = (j.isBorderBox ? m : Math.max(+m - a.getOuterH(n), 0)) + "px"
		}
		return n
	};
	a.setContentSize = function (n, l, m) {
		n = e(n);
		l = parseFloat(l, 10);
		m = parseFloat(m, 10);
		if (isNaN(l) && isNaN(m)) {
			throw "Node.js -- Node.setSize :: 传入setContentSize方法的参数错误"
		}
		if (!isNaN(l)) {
			n.style.width = (j.isBorderBox ? +l + a.getOuterW(n) : l) + "px"
		}
		if (!isNaN(m)) {
			n.style.height = (j.isBorderBox ? +m + a.getOuterH(n) : m) + "px"
		}
		return n
	};
	a.setPosition = function (m, l, n) {
		m = e(m);
		if (typeof n == "undefined" && j.isObject(l)) {
			n = l.y;
			l = l.x
		}
		l = parseFloat(l);
		n = parseFloat(n);
		if (!isNaN(l)) {
			m.style.left = l + "px";
			m.left = l
		}
		if (!isNaN(n)) {
			m.style.top = n + "px";
			m.top = n
		}
		return m
	};
	a.getPosition = a.getOffset = function (o) {
		o = e(o);
		var v = o.ownerDocument;
		if (o.parentNode === null || o.style.display === "none") {
			return false
		}
		var w = null;
		var u = [];
		var r;
		if (o.getBoundingClientRect) {
			r = o.getBoundingClientRect();
			var n = Math.max(v.documentElement.scrollTop, v.body.scrollTop);
			var p = Math.max(v.documentElement.scrollLeft, v.body.scrollLeft);
			var m = r.left + p - v.documentElement.clientLeft - v.body.clientLeft;
			var l = r.top + n - v.documentElement.clientTop - v.body.clientTop;
			return {
				x : m,
				y : l,
				left : m,
				top : l
			}
		} else {
			if (v.getBoxObjectFor) {
				r = v.getBoxObjectFor(o);
				var t = (o.style.borderLeftWidth) ? parseInt(o.style.borderLeftWidth, 10) : 0;
				var q = (o.style.borderTopWidth) ? parseInt(o.style.borderTopWidth, 10) : 0;
				u = [r.x - t, r.y - q]
			}
		}
		if (o.parentNode) {
			w = o.parentNode
		} else {
			w = null
		}
		while (w && w.tagName !== "BODY" && w.tagName !== "HTML") {
			u[0] -= w.scrollLeft;
			u[1] -= w.scrollTop;
			if (w.parentNode) {
				w = w.parentNode
			} else {
				w = null
			}
		}
		return {
			x : u[0],
			y : u[1],
			left : u[0],
			top : u[1]
		}
	};
	a.offset = function (m, l, n) {
		if (typeof l == "undefined") {
			a.getOffset(m)
		} else {
			a.setPosition(m, l, n)
		}
	};
	a.getPositionEx = function (m) {
		m = e(m);
		var n = a.getPosition(m);
		var l = j.Dom.getFrameXY();
		n.x += l.x;
		n.y += l.y;
		return n
	};
	a.getScroll = function (r) {
		d = e(r);
		var v = d.ownerDocument,
		u = v.parentWindow,
		m = v.body,
		o = v.documentElement,
		n,
		q,
		p;
		if (d == v || d == m) {
			if (j.isIE && j.isStrict) {
				n = o.scrollLeft;
				q = o.scrollTop
			} else {
				n = u.pageXOffset;
				q = u.pageYOffset
			}
			p = {
				left : n || (m ? m.scrollLeft : 0),
				top : q || (m ? m.scrollTop : 0)
			}
		} else {
			p = {
				left : d.scrollLeft,
				top : d.scrollTop
			}
		}
		return p
	};
	a.getRect = a.getBox = function (o) {
		el = e(o);
		var q = a.getOffset(el);
		var l = q.x;
		var r = q.y;
		var m = el.offsetWidth;
		var n = el.offsetHeight;
		return {
			width : m,
			height : n,
			left : l,
			top : r,
			bottom : r + n,
			right : l + m
		}
	};
	a.setBox = function (m, l) {
		el = e(m);
		el.setPosition(l);
		el.setSize(l);
		return el
	};
	a.appendTo = function (m, l) {
		el = e(m);
		return l.appendChild(el)
	};
	a.createChild = function (o, m, l, n) {
		var p;
		o = j.g(o);
		p = j.Dom.createNode(m || {
				tag : "div"
			});
		if (l && o.firstChild) {
			p.insertBefore(o, o.firstChild)
		} else {
			o.appendChild(p)
		}
		return n === true ? p : o
	};
	a.createProxy = function (n, m, q, p) {
		el = e(n);
		var o = q ? q.ownerDocument : document;
		var r = j.Dom.createNode({
				className : m || ""
			}, o);
		var l = q ? r.appendTo(q) : el.parentNode.insertBefore(r, el);
		if (p) {
			a.setBox(l, a.getBox(el))
		}
		return l
	};
	a.scrollTo = function (m, l, o, n) {
		m = e(m);
		m.scrollLeft = l;
		m.scrollTop = o;
		return m
	};
	a.getParent = function (m, l) {
		m = e(m);
		if (!l) {
			return m.parentNode
		}
		while (m) {
			if (m.nodeName.toLowerCase() === l.toLowerCase()) {
				return m
			}
			m = m.parentNode
		}
		return null
	};
	a.getParentBy = function (m, l) {
		m = e(m);
		while (m && m.nodeType) {
			if (l(m)) {
				return m
			}
			m = m.parentNode
		}
		return null
	};
	a.getParentByClass = function (m, l) {
		m = e(m);
		while (m && m.nodeType) {
			if (new RegExp("(?:^|\\s)" + l + "(?:\\s|$)").test(m.className)) {
				return m
			}
			m = m.parentNode
		}
		return null
	};
	a.getParentByAttr = function (m, l, n) {
		m = e(m);
		while (m && m.tagName && m.getAttribute) {
			if (m.getAttribute(l) === n) {
				return m
			}
			m = m.parentNode
		}
		return null
	};
	a.getForm = function (l) {
		return a.getParent(l, "form")
	};
	a.nextElement = function (m) {
		m = e(m);
		var l = m.nextSibling;
		while (l && l.nodeType !== 1) {
			l = l.nextSibling
		}
		return l
	};
	a.prevElement = function (m) {
		m = e(m);
		var l = m.previousSibling;
		while (l && l.nodeType !== 1) {
			l = l.previousSibling
		}
		return l
	};
	a.siblings = function (o, n) {
		o = e(o);
		var p = o.parentNode.firstChild,
		m = [];
		var l = function (q) {
			return n ? true : (q.tagName ? false : q.tagName.toLowerCase() == n.toLowerCase())
		};
		while (p) {
			if (o != p && l(p)) {
				m.push(p)
			}
			p = p.nextSibling
		}
		return m
	};
	a.hasClass = a.hasClassName = function (m, l) {
		m = e(m);
		l = l.replace(/\-/, "-");
		return new RegExp("(?:^|\\s)" + l + "(?:\\s|$)").test(m.className)
	};
	a.addClass = function (m, l) {
		m = e(m);
		if (!a.hasClass(m, l)) {
			m.className = m.className ? m.className + " " + l : l
		}
		return m
	};
	a.removeClass = function (n, m) {
		n = e(n);
		m = m.replace(/\-/, "-");
		var l = new RegExp(("(^|\\s)" + m + "(?=\\s|$)"), "i");
		n.className = n.className.replace(l, "").replace(/^\s+|\s+$/g, "");
		return n
	};
	a.replaceClass = function (n, m, l) {
		n = e(n);
		if (a.hasClass(n, m)) {
			n.className = n.className.replace(new RegExp("(^|\\s)" + m + "(?=\\s|$)", "ig"), "$1" + l)
		} else {
			a.addClass(n, l)
		}
		return n
	};
	a.toggleClass = function (l, n, m) {
		l = e(l);
		m = m || "";
		if (a.hasClass(l, n)) {
			a.replaceClass(l, n, m)
		} else {
			a.replaceClass(l, m, n)
		}
		return l
	};
	a.getElementsByClassName = function (p, o, n) {
		p = e(p) || document;
		var m = p.getElementsByTagName(n || "*");
		var q = [];
		for (var l = 0; l < m.length; l++) {
			if (a.hasClass(m[l], o)) {
				q.push(m[l])
			}
		}
		return q
	};
	a.getHtml = function (l) {
		l = e(l);
		return l.innerHTML
	};
	a.setHtml = function (l, m) {
		e(l).innerHTML = m;
		return l
	};
	a.html = function (l, m) {
		if (typeof(m) === "undefined") {
			return a.getHtml(l)
		} else {
			return a.setHtml(l, m)
		}
	};
	a.getValue = function (l) {
		var m = e(l);
		if (!m) {
			alert(location.pathname + ' - $V : 表单元素"' + (l || arguments.callee.caller["arguments"][0]) + '"不存在 ');
			return
		}
		if (!m.type) {
			return m.value
		}
		switch (m.type.toLowerCase()) {
		case "text":
		case "textarea":
			if (m.getAttribute("placeholder") && m.value == m.getAttribute("placeholder")) {
				return ""
			}
			return m.value;
		case "submit":
		case "hidden":
		case "password":
		case "file":
		case "image":
		case "select-one":
			return m.value;
		case "checkbox":
		case "radio":
			if (m.checked) {
				return m.value
			}
			return null;
		default:
			return ""
		}
	};
	a.setValue = function (l, n) {
		var m = e(l);
		if (!n && n !== 0) {
			n = ""
		}
		if (!m) {
			alert(location.pathname + ' - $V : 表单元素"' + (l || arguments.callee.caller["arguments"][0]) + '"不存在 ');
			return
		}
		switch (m.type.toLowerCase()) {
		case "submit":
		case "hidden":
		case "password":
		case "textarea":
		case "button":
		case "file":
		case "image":
		case "select-one":
		case "text":
			m.value = n;
			break;
		case "checkbox":
		case "radio":
			if (m.value === "" + n) {
				m.checked = true
			} else {
				m.checked = false
			}
			break
		}
		return m
	};
	a.val = function (l, m) {
		if (typeof(m) === "undefined") {
			return a.getValue(l)
		} else {
			return a.setValue(l, m)
		}
	};
	a.find = function (m, l) {
		m = e(m);
		return jQuery.find(l, m)
	};
	a.anchorPos = function (H, D, t, q, I, o, u) {
		D = e(D);
		if (D.nodeName) {
			var n = a.getPosition(D),
			v = a.getSize(D);
			D = [n.x, n.y, v.width, v.height]
		}
		H = e(H);
		var p = a.getSize(H),
		m = p.width,
		A = p.height,
		B = D[0],
		y = D[1],
		C = D[2],
		l = D[3],
		F,
		E;
		F = t.charAt(0) === "l" ? B - m : B + C;
		E = t.charAt(1) === "t" ? y - A : y + l;
		if (q) {
			if (q.charAt(0) === "h") {
				F = q.charAt(1) === "l" ? F - m : F + m
			} else {
				E = q.charAt(1) === "u" ? E - A : E + A
			}
		}
		if (I) {
			F += I[0];
			E += I[1]
		}
		if (o) {
			var x = j.Dom.getViewport(),
			G = x.height,
			r = x.width;
			if (F < 0) {
				F = 0;
				if (y + l > E && y < E + A) {
					E = y - A
				}
			}
			if (F + m > r) {
				F = y + l > E && y < E + A ? B - m : r - m
			}
			if (E < 0) {
				E = B + C > F && B < F + m ? y + l : 0
			}
			if (E + A > G) {
				E = B + C > F && B < F + m ? y - A : G - A
			}
		}
		if (u) {
			a.setPosition(H, F, E)
		}
		return {
			x : F,
			y : E
		}
	};
	a.getConstrainToXY = function (m, D, J, p, t) {
		m = e(m);
		D = e(D);
		var r = {
			top : 0,
			left : 0,
			bottom : 0,
			right : 0
		};
		p = p ? j.applyIf(p, r) : r;
		var I,
		M,
		H = 0,
		G = 0;
		if (D.tagName == "BODY" || j.isDocument(D)) {
			var C = j.Dom.getViewport(D.ownerDocument || D);
			I = C.width;
			M = C.height
		} else {
			I = D.clientWidth;
			M = D.clientHeight;
			if (!J) {
				var F = D.getOffset();
				H = F[0];
				G = F[1]
			}
		}
		var E = a.getScroll(D);
		H += p.left + E.left;
		G += p.top + E.top;
		I -= p.right;
		M -= p.bottom;
		var K = H + I,
		l = G + M,
		o = t || (!J ? m.getOffset() : [parseInt(m.getStyle("left"), 10) || 0, parseInt(m.getStyle("top"), 10) || 0]),
		A = o[0],
		u = o[1],
		n = m.getConstrainOffset ? m.getConstrainOffset() : 0,
		B = m.offsetWidth + n,
		L = m.offsetHeight + n;
		var q = false;
		if ((A + B) > K) {
			A = K - B;
			q = true
		}
		if ((u + L) > l) {
			u = l - L;
			q = true
		}
		if (A < H) {
			A = H;
			q = true
		}
		if (u < G) {
			u = G;
			q = true
		}
		return q ? [A, u] : false
	};
	a.getJson = function (C, B, m) {
		B = B || {};
		m = m || function (o) {
			return false
		};
		var w = function (D, E, o) {
			if (typeof B[D] != "undefined") {
				if (Object.prototype.toString.apply(B[D]) != "[object Array]" && !o) {
					B[D] = [B[D]]
				}
				B[D].push(E)
			} else {
				B[D] = o ? [E] : E
			}
		};
		var p = j.g(C);
		if (!p || m(p)) {
			return null
		}
		if (/^(input|textarea|select)$/i.test(p.tagName)) {
			var A = p.name || p.id;
			if (!p.type || !A) {
				return
			}
			switch (p.type) {
			case "text":
			case "textarea":
				if (p.getAttribute("placeholder") && p.value == p.getAttribute("placeholder")) {
					w(A, "")
				} else {
					w(A, p.value)
				}
				break;
			case "hidden":
			case "password":
				w(A, p.value);
				break;
			case "checkbox":
				if (p.name && p.checked) {
					w(A, p.value, true)
				}
				break;
			case "radio":
				if (p.name && p.checked) {
					w(A, p.value)
				}
				break;
			case "select-one":
				if (p.selectedIndex > -1) {
					w(A, p.value)
				}
				break;
			case "select-multiple":
				var l = p.options,
				y = [];
				for (var t = 0; t < l.length; ++t) {
					if (l[t].selected) {
						y.push(l[t].value)
					}
				}
				w(A, y);
				break;
			case "button":
				break
			}
			return B
		}
		if (/^(area|base|basefont|col|frame|hr|img|br|isindex|link|meta|option|param)$/i.test(p.tagName)) {
			return null
		}
		var r,
		x,
		v;
		if (p.getAttribute("ztype") && p.getAttribute("ztype").toLowerCase() == "datalevel") {
			r = p.children;
			x = r.length;
			var q = {};
			for (v = 0; v < x; v++) {
				var n = a.getJson(r[v], q)
			}
			if (q) {
				w(p.getAttribute("name") || p.tagName || p.id, q)
			}
		} else {
			r = p.children;
			x = r.length;
			for (v = 0; v < x; v++) {
				var u = a.getJson(r[v], B)
			}
		}
		return B
	};
	a.update = function (n, m, l, q) {
		ele = j.g(n);
		if (typeof m == "undefined") {
			m = ""
		}
		if (l !== true) {
			ele.innerHTML = m;
			if (typeof q == "function") {
				q()
			}
			return ele
		}
		var o = ele.ownerDocument;
		var p = j.id();
		m += '<span id="' + p + '"></span>';
		Dom.available(p, function () {
			var t = o.head || o.getElementsByTagName("head")[0];
			var B = /(?:<script([^>]*)?>)((\n|\r|.)*?)(?:<\/script>)/ig;
			var x = /\ssrc=([\'\"])(.*?)\1/i;
			var A = /\stype=([\'\"])(.*?)\1/i;
			var u;
			while (u = B.exec(m)) {
				var y = u[1];
				var w = y ? y.match(x) : false;
				if (w && w[2]) {
					var C = o.createElement("script");
					C.src = w[2];
					var v = y.match(A);
					if (v && v[2]) {
						C.type = v[2]
					}
					t.appendChild(C)
				} else {
					if (u[2] && u[2].length > 0) {
						if (window.execScript) {
							window.execScript(u[2])
						} else {
							window["eval"](u[2])
						}
					}
				}
			}
			var r = document.getElementById(p);
			if (r) {
				Dom.removeNode(r)
			}
			if (typeof q == "function") {
				q()
			}
		});
		ele.innerHTML = m.replace(/(?:<script.*?>)((\n|\r|.)*?)(?:<\/script>)/ig, "");
		return ele
	};
	a.getComponent = function (l) {
		return ComponentManager.getById(l.compId || l.id)
	};
	var c = j.Helper.methodize(a);
	j.attachNodeMethod = function (l) {
		l._extendedByZvingJSLib = {};
		return j.extra(l, c)
	};
	j.detachNodeMethod = function (l) {
		if (l._extendedByZvingJSLib) {
			try {
				delete l._extendedByZvingJSLib;
				for (var n in c) {
					delete l[n]
				}
			} catch (m) {}

		}
	};
	var k = function (l) {
		if (!l) {
			return j.Console.error(location.pathname + " ## Node.js -- $ :: 没有为$方法传入参数" + l)
		}
		l = e(l);
		if (!l) {
			return
		}
		if (l.nodeType !== 1) {
			return null
		}
		if (l.ownerDocument.parentWindow != window) {
			return l.ownerDocument.parentWindow.Zving.$(l)
		}
		if (l._extendedByZvingJSLib || (/^object|embed$/i).test(l.nodeName)) {
			return l
		}
		j.Dom.addToCache(l);
		return l
	};
	j.$$ = function (l, m) {
		return jQuery.find(l, m)
	};
	a._unload = function () {
		var l;
		for (l in j.elCache) {
			if (j.elCache.hasOwnProperty(l)) {
				j.detachNodeMethod(l)
			}
		}
	};
	j.$ = k;
	j.Node = a
})();
(function () {
	var e = window.Zving,
	b = e.isIE,
	a = e.isGecko,
	c = {};
	c.lockSelect = function (f) {
		f = f || document.body || document.documentElement;
		if (e.isGecko) {
			f.style.MozUserSelect = "none";
			f.style.MozUserInput = "none"
		} else {
			document.selection.empty();
			f.onselectstart = e.Event.stopEvent
		}
	};
	c.unlockSelect = function (f) {
		f = f || document.body || document.documentElement;
		if (e.isGecko) {
			f.style.MozUserSelect = "";
			f.style.MozUserInput = ""
		} else {
			f.onselectstart = null
		}
	};
	c.copyToClipboard = function (n) {
		if (n === null) {
			return
		}
		if (window.clipboardData) {
			window.clipboardData.setData("Text", n)
		} else {
			if (window.netscape) {
				try {
					netscape.security.PrivilegeManager.enablePrivilege("UniversalXPConnect")
				} catch (j) {
					Dialog.alert("Firefox自动复制功能未启用！<br>请访问<a href='about:config' target='_blank'>about:config</a> 将’signed.applets.codebase_principal_support’设置为’true’");
					return
				}
				var k = Components.classes["@mozilla.org/widget/clipboard;1"].createInstance(Components.interfaces.nsIClipboard);
				if (!k) {
					return
				}
				var h = Components.classes["@mozilla.org/widget/transferable;1"].createInstance(Components.interfaces.nsITransferable);
				if (!h) {
					return
				}
				h.addDataFlavor("text/unicode");
				var m = {};
				var f = {};
				m = Components.classes["@mozilla.org/supports-string;1"].createInstance(Components.interfaces.nsISupportsString);
				var o = n;
				m.data = o;
				h.setTransferData("text/unicode", m, o.length * 2);
				var l = Components.interfaces.nsIClipboard;
				if (!k) {
					return
				}
				k.setData(h, null, l.kGlobalClipboard)
			} else {
				Dialog.alert("该浏览器不支持自动复制功能！");
				return
			}
		}
		Dialog.alert("己复制文字：<br><br><font color='red'>" + n + "</font><br><br>到剪贴板", null, 400, 200)
	};
	c.hasFontList = {};
	c.hasFont = function (h) {
		if (c.hasFontList[h.toLowerCase()] !== undefined) {
			return c.hasFontList[h.toLowerCase()]
		}
		var o = e.getDom("_fontdetect");
		if (!o) {
			o = document.createElement("DIV");
			o.id = "_fontdetect";
			o.style.cssText = "position:absolute; top:-111px; left:0;";
			document.body.appendChild(o)
		}
		o.innerHTML = '<iframe src="about:blank" id="frame_fontdetect" width="1000" height="100"></iframe>';
		var j = e.getDom("frame_fontdetect");
		var q = j.contentWindow.document;
		if (e.isIE) {
			q.open();
			q.write("<html><body></body></html>");
			q.close()
		}
		var l = q.body;
		var p = q.createElement("DIV");
		var v = q.createElement("SPAN");
		var u = "font-size: 60px; margin: 0; padding: 0; border: 0; white-space: nowrap; line-height: 1; font-style: normal; font-weight: normal; text-decoration: none;";
		p.appendChild(v);
		p.style.cssText = u;
		v.style.cssText = u;
		v.innerHTML = "The quick brown fox jumps over the lazy dog";
		l.appendChild(p);
		var t = v.offsetWidth;
		var f = v.offsetHeight;
		var m;
		if (p.currentStyle) {
			m = p.currentStyle.fontFamily
		} else {
			m = q.defaultView.getComputedStyle(p, null).fontFamily
		}
		v.style.fontFamily = h;
		var k = v.offsetWidth;
		var n = v.offsetHeight;
		var r;
		q = p = v = null;
		j = null;
		o.innerHTML = "";
		h = h.toLowerCase();
		if (h == m.toLowerCase() || /^(arial|verdana|sans-serif|serif)$/.test(h)) {
			r = true
		} else {
			if (e.isWindows && /^(tahoma|simsun|宋体|simhei|黑体)$/.test(h)) {
				r = true
			} else {
				if (e.isMac && /^(helvetica|stsong|华文宋体|stheiti|华文黑体)$/.test(h)) {
					r = true
				} else {
					r = (k != t || n != f)
				}
			}
		}
		h = h.toLowerCase();
		c.hasFontList[h.toLowerCase()] = r;
		return r
	};
	if (window != e.rootWin) {
		c.hasFontList = e.rootWin.Zving.Misc.hasFontList;
		c.hasFont = e.rootWin.Zving.Misc.hasFont
	}
	e.Misc = c
})();
(function () {
	var a = window.Zving,
	b = {};
	b.getQueryValue = function (e, f) {
		var h = new RegExp("(^|&|\\?|#)" + a.String.escapeReg(f) + "=([^&#]*)(&|\x24|#)", "");
		var c = ("" + e).match(h);
		if (c) {
			return c[2]
		}
		return null
	};
	a.Url = b
})();
(function () {
	var c = window.Zving,
	b = c.isIE;
	var a = function (f, e) {
		this.init(f, e)
	};
	a.prototype = {
		init : function (o, l) {
			this._obj = typeof(o) === "string" ? document.getElementById(o) : o;
			this._styleWidth = this._styleHeight = this._styleLeft = this._styleTop = 0;
			this._sideRight = this._sideDown = this._sideLeft = this._sideUp = 0;
			this._fixLeft = this._fixTop = 0;
			this._scaleLeft = this._scaleTop = 0;
			this._mxSet = function () {};
			this._mxRightWidth = this._mxDownHeight = this._mxUpHeight = this._mxLeftWidth = 0;
			this._mxScaleWidth = this._mxScaleHeight = 0;
			this._fun = function () {};
			var h = this._obj.currentStyle;
			this._borderX = (parseInt(h.borderLeftWidth, 10) || 0) + (parseInt(h.borderRightWidth, 10) || 0);
			this._borderY = (parseInt(h.borderTopWidth, 10) || 0) + (parseInt(h.borderBottomWidth, 10) || 0);
			var j = this;
			this._fR = function (f) {
				j.Resize(f)
			};
			this._fS = function () {
				j.Stop()
			};
			this.SetOptions(l);
			this.Max = !!this.options.Max;
			this._mxContainer = (typeof(o) === "string" ? document.getElementById(this.options.mxContainer) : this.options.mxContainer) || null;
			this.mxLeft = Math.round(this.options.mxLeft);
			this.mxRight = Math.round(this.options.mxRight);
			this.mxTop = Math.round(this.options.mxTop);
			this.mxBottom = Math.round(this.options.mxBottom);
			this.Min = !!this.options.Min;
			this.minWidth = Math.round(this.options.minWidth);
			this.minHeight = Math.round(this.options.minHeight);
			this.Scale = !!this.options.Scale;
			this.Ratio = Math.max(this.options.Ratio, 0);
			this._wrap = this.options.wrap ? (typeof(this.options.wrap) === "string" ? document.getElementById(this.options.wrap) : this.options.wrap) : this._obj;
			this.onResize = this.options.onResize;
			this.endResize = this.options.endResize;
			this.proxy = this.options.proxy;
			if (this.options.proxy) {
				this._proxy = document.createElement("div");
				this._proxy.style.cssText = this._wrap.style.cssText;
				this._proxy.className = "rProxy";
				this._proxy.style.height = this._wrap.clientHeight + "px";
				this._proxy.style.width = this._wrap.clientWidth + "px";
				this._proxy.style.display = "none";
				this._wrap.parentNode.appendChild(this._proxy)
			} else {
				this._proxy = this._wrap
			}
			if (this.options.attachObj) {
				this.attachObj = [];
				for (var m = 0, e = this.options.attachObj.length; m < e; m++) {
					var k = this.options.attachObj[m];
					k = typeof(k) === "string" ? document.getElementById(k) : k;
					if (k) {
						this.attachObj.push(k)
					}
				}
			}
			if (this._obj.currentStyle.position !== "absolute") {
				this._obj.style.position = "relative";
				this.isStatic = true
			} else {
				this.isStatic = this.options.isStatic
			}
			this.idPrefix = o.id + "_";
			var n = document.createElement("span");
			n.innerHTML = ('<a href="javascript:void(0);" id="@rHandleRightDown" class="rHandle rHandleRightDown" drag="false"> </a><a href="javascript:void(0);" id="@rHandleLeftDown" class="rHandle rHandleLeftDown"> </a><a href="javascript:void(0);" id="@rHandleRightUp" class="rHandle rHandleRightUp"> </a><a href="javascript:void(0);" id="@rHandleLeftUp" class="rHandle rHandleLeftUp"> </a><a href="javascript:void(0);" id="@rHandleRight" class="rHandle rHandleRight"> </a><a href="javascript:void(0);" id="@rHandleDown" class="rHandle rHandleDown"></a><a href="javascript:void(0);" id="@rHandleLeft" class="rHandle rHandleLeft"> </a><a href="javascript:void(0);" id="@rHandleUp" class="rHandle rHandleUp"> </a>').replace(/@/gm, this.idPrefix);
			this._wrap.appendChild(n);
			if (!this.isStatic) {
				this.Set(this.idPrefix + "rHandleLeftDown", "left-down");
				this.Set(this.idPrefix + "rHandleRightUp", "right-up");
				this.Set(this.idPrefix + "rHandleLeftUp", "left-up");
				this.Set(this.idPrefix + "rHandleLeft", "left");
				this.Set(this.idPrefix + "rHandleUp", "up")
			}
			this.Set(this.idPrefix + "rHandleRight", "right");
			this.Set(this.idPrefix + "rHandleRightDown", "right-down");
			this.Set(this.idPrefix + "rHandleDown", "down")
		},
		SetOptions : function (e) {
			this.options = {
				Max : false,
				mxContainer : "",
				mxLeft : 0,
				mxRight : 9999,
				mxTop : 0,
				mxBottom : 9999,
				Min : true,
				minWidth : 50,
				minHeight : 30,
				Scale : false,
				Ratio : 0,
				onResize : null,
				attachObj : [],
				proxy : false,
				wrap : null,
				isStatic : false
			};
			if (e) {
				for (var f in e) {
					this.options[f] = e[f]
				}
			}
		},
		Set : function (j, h) {
			var e;
			j = document.getElementById(j);
			if (!j) {
				return
			}
			switch (h.toLowerCase()) {
			case "up":
				e = this.Up;
				break;
			case "down":
				e = this.Down;
				break;
			case "left":
				e = this.Left;
				break;
			case "right":
				e = this.Right;
				break;
			case "left-up":
				e = this.LeftUp;
				break;
			case "right-up":
				e = this.RightUp;
				break;
			case "left-down":
				e = this.LeftDown;
				break;
			case "right-down":
			default:
				if (this.proxy) {
					this._proxy.style.cursor = "nw-resize"
				}
				e = this.RightDown
			}
			var f = this;
			$(j).on("mousedown.resize", function (k) {
				f.Start(k, e)
			})
		},
		Start : function (m, j) {
			if (m.stopPropagation) {
				m.stopPropagation()
			} else {
				m.cancelBubble = true
			}
			this._fun = j;
			this._styleWidth = this._startWidth = this._obj.clientWidth;
			this._styleHeight = this._startHeight = this._obj.clientHeight;
			this._styleLeft = this._startLeft = this._obj.offsetLeft;
			this._styleTop = this._startTop = this._obj.offsetTop;
			this._sideLeft = m.clientX - this._styleWidth;
			this._sideRight = m.clientX + this._styleWidth;
			this._sideUp = m.clientY - this._styleHeight;
			this._sideDown = m.clientY + this._styleHeight;
			this._fixLeft = this._styleWidth + this._styleLeft;
			this._fixTop = this._styleHeight + this._styleTop;
			if (this.Scale) {
				this.Ratio = Math.max(this.Ratio, 0) || this._styleWidth / this._styleHeight;
				this._scaleLeft = this._styleLeft + this._styleWidth / 2;
				this._scaleTop = this._styleTop + this._styleHeight / 2
			}
			if (this.Max) {
				var l = this.mxLeft,
				k = this.mxRight,
				h = this.mxTop,
				f = this.mxBottom;
				if (!!this._mxContainer) {
					l = Math.max(l, 0);
					h = Math.max(h, 0);
					k = Math.min(k, this._mxContainer.clientWidth);
					f = Math.min(f, this._mxContainer.clientHeight)
				}
				k = Math.max(k, l + (this.Min ? this.minWidth : 0) + this._borderX);
				f = Math.max(f, h + (this.Min ? this.minHeight : 0) + this._borderY);
				this._mxSet = function () {
					this._mxRightWidth = k - this._styleLeft - this._borderX;
					this._mxDownHeight = f - this._styleTop - this._borderY;
					this._mxUpHeight = Math.max(this._fixTop - h, this.Min ? this.minHeight : 0);
					this._mxLeftWidth = Math.max(this._fixLeft - l, this.Min ? this.minWidth : 0)
				};
				this._mxSet();
				if (this.Scale) {
					this._mxScaleWidth = Math.min(this._scaleLeft - l, k - this._scaleLeft - this._borderX) * 2;
					this._mxScaleHeight = Math.min(this._scaleTop - h, f - this._scaleTop - this._borderY) * 2
				}
			}
			if (this.proxy) {
				this._proxy.style.display = "block"
			}
			$(document).on("mousemove.resize", this._fR);
			$(document).on("mouseup.resize", this._fS);
			if (b) {
				$(this._wrap).on("losecapture.resize", this._fS);
				this._wrap.setCapture()
			} else {
				$(window).on("blur.resize", this._fS);
				m.preventDefault()
			}
		},
		Resize : function (k) {
			if (window.getSelection) {
				window.getSelection().removeAllRanges()
			} else {
				document.selection.empty()
			}
			this._fun(k);
			this._proxy.style.width = this._styleWidth + "px";
			this._proxy.style.height = this._styleHeight + "px";
			if (!this.isStatic) {
				this._proxy.style.top = this._styleTop + "px";
				this._proxy.style.left = this._styleLeft + "px"
			}
			if (this.attachObj && this.attachObj.length > 0) {
				for (var j = 0, f = this.attachObj.length; j < f; j++) {
					var h;
					this.attachObj[j].style.width = this._proxy.style.clientWidth + this._styleWidth - this._startWidth + "px";
					this.attachObj[j].style.height = this._proxy.style.clientHeight + this._styleHeight - this._startHeight + "px"
				}
			}
			if (this.onResize) {
				this.onResize()
			}
		},
		Up : function (f) {
			this.RepairY(this._sideDown - f.clientY, this._mxUpHeight);
			this.RepairTop()
		},
		Down : function (f) {
			this.RepairY(f.clientY - this._sideUp, this._mxDownHeight)
		},
		Right : function (f) {
			this.RepairX(f.clientX - this._sideLeft, this._mxRightWidth)
		},
		Left : function (f) {
			this.RepairX(this._sideRight - f.clientX, this._mxLeftWidth);
			this.RepairLeft()
		},
		RightDown : function (f) {
			this.RepairAngle(f.clientX - this._sideLeft, this._mxRightWidth, f.clientY - this._sideUp, this._mxDownHeight)
		},
		RightUp : function (f) {
			this.RepairAngle(f.clientX - this._sideLeft, this._mxRightWidth, this._sideDown - f.clientY, this._mxUpHeight);
			this.RepairTop()
		},
		LeftDown : function (f) {
			this.RepairAngle(this._sideRight - f.clientX, this._mxLeftWidth, f.clientY - this._sideUp, this._mxDownHeight);
			this.RepairLeft()
		},
		LeftUp : function (f) {
			this.RepairAngle(this._sideRight - f.clientX, this._mxLeftWidth, this._sideDown - f.clientY, this._mxUpHeight);
			this.RepairTop();
			this.RepairLeft()
		},
		RepairX : function (h, e) {
			h = this.RepairWidth(h, e);
			if (this.Scale) {
				var j = this.RepairScaleHeight(h);
				if (this.Max && j > this._mxScaleHeight) {
					j = this._mxScaleHeight;
					h = this.RepairScaleWidth(j)
				} else {
					if (this.Min && j < this.minHeight) {
						var f = this.RepairScaleWidth(this.minHeight);
						if (f < e) {
							j = this.minHeight;
							h = f
						}
					}
				}
				this._styleHeight = j;
				this._styleTop = this._scaleTop - j / 2
			}
			this._styleWidth = h
		},
		RepairY : function (j, e) {
			j = this.RepairHeight(j, e);
			if (this.Scale) {
				var h = this.RepairScaleWidth(j);
				if (this.Max && h > this._mxScaleWidth) {
					h = this._mxScaleWidth;
					j = this.RepairScaleHeight(h)
				} else {
					if (this.Min && h < this.minWidth) {
						var f = this.RepairScaleHeight(this.minWidth);
						if (f < e) {
							h = this.minWidth;
							j = f
						}
					}
				}
				this._styleWidth = h;
				this._styleLeft = this._scaleLeft - h / 2
			}
			this._styleHeight = j
		},
		RepairAngle : function (j, f, k, e) {
			j = this.RepairWidth(j, f);
			if (this.Scale) {
				k = this.RepairScaleHeight(j);
				if (this.Max && k > e) {
					k = e;
					j = this.RepairScaleWidth(k)
				} else {
					if (this.Min && k < this.minHeight) {
						var h = this.RepairScaleWidth(this.minHeight);
						if (h < f) {
							k = this.minHeight;
							j = h
						}
					}
				}
			} else {
				k = this.RepairHeight(k, e)
			}
			this._styleWidth = j;
			this._styleHeight = k
		},
		RepairTop : function () {
			this._styleTop = this._fixTop - this._styleHeight
		},
		RepairLeft : function () {
			this._styleLeft = this._fixLeft - this._styleWidth
		},
		RepairHeight : function (f, e) {
			f = Math.min(this.Max ? e : f, f);
			f = Math.max(this.Min ? this.minHeight : f, f, 0);
			return f
		},
		RepairWidth : function (f, e) {
			f = Math.min(this.Max ? e : f, f);
			f = Math.max(this.Min ? this.minWidth : f, f, 0);
			return f
		},
		RepairScaleHeight : function (e) {
			return Math.max(Math.round((e + this._borderX) / this.Ratio - this._borderY), 0)
		},
		RepairScaleWidth : function (e) {
			return Math.max(Math.round((e + this._borderY) * this.Ratio - this._borderX), 0)
		},
		Stop : function () {
			if (this.proxy) {
				this._proxy.style.display = "none"
			}
			$(document).off("mousemove.resize", this._fR);
			$(document).off("mouseup.resize", this._fS);
			if (b) {
				$(this._wrap).off("losecapture.resize", this._fS);
				this._wrap.releaseCapture()
			} else {
				$(window).off("blur.resize", this._fS)
			}
			if (this.endResize && typeof(this.endResize) === "function") {
				this.endResize({
					height : this._styleHeight,
					width : this._styleWidth,
					top : this._styleTop,
					left : this._styleLeft
				})
			}
		}
	};
	c.Resize = a
})();
(function () {
	var k = window.Zving,
	e = k.Node;
	k.currentDragDropManager = null;
	if (window != k.rootWin) {
		k.currentDragDropManager = k.rootWin.Zving.currentDragDropManager
	}
	var c = {
		isDragging : false,
		dragObject : null,
		startDate : null,
		startWindow : window,
		startX : 0,
		startY : 0,
		pageX : 0,
		pageY : 0,
		deltaX : 0,
		deltaY : 0,
		deltaDeltaX : 0,
		deltaDeltaY : 0,
		mouseDownTarget : null,
		startDrag : k.noopFn
	};
	(function () {
		var l = function (p) {
			var o = c.dragObject;
			p = Event.fixEvent(p);
			if (c.isDragging || !o) {
				return
			}
			c.isDragging = true;
			if (this.startWindow != k.rootWin) {
				k.currentDragDropManager = c
			}
			$(document).on("mousemove.drag", m);
			$(document).on("mouseup.drag", n);
			c.startDate = new Date();
			c.deltaX = c.deltaY = c.deltaDeltaX = c.deltaDeltaY = 0;
			c.startX = c.pageX = p.pageX;
			c.startY = c.pageY = p.pageY;
			c.mouseDownTarget = p.target || p.srcElement;
			o.dragstart(p);
			p.preventDefault()
		};
		var n = function (p) {
			var o = c.dragObject;
			if (!c.isDragging || !o) {
				return
			}
			o.dragend(p);
			c.isDragging = false;
			if (this.startWindow != k.rootWin) {
				k.currentDragDropManager = null
			}
			c.dragObject = null;
			$(document).off("mousemove.drag");
			$(document).off("mouseup.drag")
		};
		var m = function (u) {
			var t = c.dragObject;
			u = Event.fixEvent(u);
			if (!c.isDragging || !t) {
				return
			}
			var r = u.view || (u.target || u.srcElement).ownerDocument.parentWindow;
			var o = u.pageX;
			var v = u.pageY;
			if (r != c.startWindow) {
				var p = k.Dom.getFrameOffset(r);
				var q = k.Dom.getFrameOffset(c.startWindow);
				o = o + p.x - q.x;
				v = v + p.y - q.y
			}
			c.deltaDeltaX = o - c.pageX;
			c.deltaDeltaY = v - c.pageY;
			c.pageX = o;
			c.pageY = v;
			c.deltaX = o - c.startX;
			c.deltaY = v - c.startY;
			t.drag(u);
			u.preventDefault()
		};
		c.startDrag = function (p, o) {
			this.dragObject = o;
			l(p)
		}
	}
		());
	var h = function (l) {
		k.extra(this, l);
		k.Observable(this);
		k.UICompBase.prototype.getId.call(this);
		k.ComponentManager.add(this);
		if (!this.lazyRender) {
			this.render()
		}
	};
	var b = function (l) {
		return parseFloat(l) || 0
	};
	h.prototype = {
		ctype : "SimpleDrag",
		dragEl : null,
		handleEl : null,
		capture : false,
		proxyEl : null,
		xAttr : "left",
		yAttr : "top",
		maxXAttr : null,
		minXAttr : null,
		maxYAttr : null,
		minYAttr : null,
		xFixed : false,
		yFixed : false,
		withProxy : true,
		offsetProxy : false,
		getProxy : function () {
			var l = this.proxyEl || k.getDom("_DragProxy");
			if (!l) {
				l = document.createElement("div");
				l.id = "_DragProxy";
				l.className = "z-proxy-dd";
				document.body.appendChild(l);
				l.style.display = "none"
			}
			this.proxyEl = l;
			return l
		},
		dragstart : function (q) {
			var p = this;
			if (p.capture && p.handleEl.setCapture) {
				p.handleEl.setCapture();
				window.event.cancelBubble = true
			}
			p.startXAttr = b(e.getCurrentStyle(p.dragEl, p.xAttr.replace(/^-/, "")));
			p.startYAttr = b(e.getCurrentStyle(p.dragEl, p.yAttr.replace(/^-/, "")));
			p.startXAttrInPage = e.getOffset(p.dragEl).x;
			p.startYAttrInPage = e.getOffset(p.dragEl).y;
			if (p.withProxy) {
				var m,
				t;
				var n = p.getProxy();
				var o = e.getRect(p.dragEl);
				m = o.left;
				t = o.top;
				if (typeof this.offsetProxy == "string") {
					if (this.offsetProxy.indexOf("t") != -1) {
						m = t - c.pageY - o.top - 5
					}
					if (this.offsetProxy.indexOf("l") != -1) {
						m = m - c.pageX - o.left - 5
					}
					if (this.offsetProxy.indexOf("r") != -1) {
						m = m + c.pageX - o.left + 5
					}
					if (this.offsetProxy.indexOf("b") != -1) {
						t = t + c.pageY - o.top + 5
					}
				}
				e.setPosition(n, m, t);
				e.setSize(n, o.width, o.height);
				e.css(n, "z-index", (parseInt(e.css(p.dragEl, "z-index"), 10) || 0) + 1);
				p.startXAttrProxy = b(n.style[p.xAttr.replace(/^-/, "")]);
				p.startYAttrProxy = b(n.style[p.yAttr.replace(/^-/, "")]);
				n.__deltaX = n.__deltaY = 0;
				var l;
				var r = function () {
					var u = 0;
					if (p != c.dragObject || n.style.display != "none") {
						u = -1
					}
					if (c.deltaX * c.deltaX + c.deltaY * c.deltaY > 4 || (new Date() - c.startDate) > 500) {
						u = 1
					}
					if (u === 1) {
						n.style.display = "block"
					}
					if (u === 1 || u === -1) {
						clearInterval(l)
					}
				};
				if (isIE) {
					n.style.display = "block"
				} else {
					l = setInterval(r, 20)
				}
			}
			p.fireEvent("dragstart", q)
		},
		drag : function (q) {
			var o = this,
			p = {
				X : 1,
				Y : 1
			};
			for (var n in p) {
				var l = n.toLowerCase();
				if (!o[l + "Fixed"]) {
					var r = (o[l + "Attr"].indexOf("-") === 0 ? -1 : 1) * c["delta" + n];
					if (o["max" + n + "Attr"] !== null) {
						r = Math.min(r, o["max" + n + "Attr"] - o["start" + n + "AttrInPage"])
					}
					if (o["min" + n + "Attr"] !== null) {
						r = Math.max(r, o["min" + n + "Attr"] - o["start" + n + "AttrInPage"])
					}
					if (o.withProxy) {
						try {
							e.setStyle(o.proxyEl, o[l + "Attr"], (o["start" + n + "AttrProxy"] + r) + "px")
						} catch (m) {}

						o.proxyEl["__delta" + n] = r
					} else {
						e.setStyle(o.dragEl, o[l + "Attr"].replace(/^-/, ""), (o["start" + n + "Attr"] + r) + "px")
					}
				}
			}
			o.fireEvent("drag", q)
		},
		dragend : function (n) {
			var m = this;
			if (m.capture && m.handleEl.releaseCapture) {
				m.handleEl.releaseCapture()
			}
			if (m.withProxy) {
				var l = m.proxyEl;
				l.style.display = "none";
				if (!m.xFixed) {
					e.setStyle(m.dragEl, m.xAttr.replace(/^-/, ""), (m.startXAttr + l.__deltaX) + "px")
				}
				if (!m.yFixed) {
					e.setStyle(m.dragEl, m.yAttr.replace(/^-/, ""), (m.startYAttr + l.__deltaY) + "px")
				}
			}
			m.fireEvent("dragend", n)
		},
		render : function () {
			var l = this;
			if (l._rendered) {
				return
			}
			l.addEvents("dragstart", "drag", "dragend");
			if (l.delegateContainer) {
				$(l.delegateContainer).delegate(l.handleSelector, "mousedown.drag", function (m) {
					l.handleEl = this;
					if (l.dragSelector) {
						l.dragEl = $(this).closest(l.dragSelector)[0]
					} else {
						l.dragEl = l.handleEl
					}
					c.startDrag((m && m.core) || m, l)
				})
			} else {
				l.handleEl = l.handleEl || l.dragEl;
				$(l.handleEl).on("mousedown.drag", function (m) {
					c.startDrag(m, l)
				})
			}
			l._rendered = true
		},
		destroy : function () {
			var l = this;
			if (!l._rendered) {
				return
			}
			if (l.delegateContainer) {
				$(l.delegateContainer).off("mousedown.drag")
			} else {
				$(l.handleEl).off("mousedown.drag")
			}
			k.ComponentManager.unregister(this)
		}
	};
	var f = function (l) {
		h.call(this, l)
	};
	f.MENTOR_CLASS = h;
	f.prototype = {
		ctype : "SimpleResize",
		xAttr : "width",
		yAttr : "height",
		minXAttr : 0,
		minYAttr : 0
	};
	k.extraIf(f.prototype, h.prototype);
	var j = function (l) {
		k.extra(this, l);
		k.Observable(this);
		if (!this.lazyRender) {
			this.render()
		}
	};
	j.prototype = {
		ctype : "RectSelector",
		proxyEl : null,
		handleEl : null,
		getProxy : (function () {
			var l = null;
			return function () {
				var m = this.proxyEl || l;
				if (!m) {
					m = k.Dom.createNode("div", {
							className : "z-proxy-rectselector"
						});
					document.body.appendChild(m);
					m.style.display = "none";
					this.proxyEl = m
				}
				return this.proxyEl
			}
		}
			()),
		dragstart : function (l) {
			this.proxyEl = this.getProxy();
			this.proxyEl.style.display = "block";
			if (this.handleEl.setCapture) {
				this.handleEl.setCapture()
			}
			e.setPosition(this.proxyEl, c.startX, c.startY);
			e.setSize(this.proxyEl, 1, 1);
			this.fireEvent("dragstart", l)
		},
		drag : function (l) {
			e.setPosition(this.proxyEl, Math.min(c.startX, c.pageX), Math.min(c.startY, c.pageY));
			e.setSize(this.proxyEl, Math.abs(c.deltaX), Math.abs(c.deltaY));
			this.fireEvent("drag", l)
		},
		dragend : function (l) {
			if (this.handleEl.releaseCapture) {
				this.handleEl.releaseCapture()
			}
			this.proxyEl.style.display = "none";
			this.fireEvent("dragend", l)
		},
		render : function () {
			var l = this;
			if (l._rendered) {
				return
			}
			l.addEvents("dragstart", "drag", "dragend");
			$(l.handleEl).on("mousedown.drag", function (m) {
				if (m.target == l.handleEl) {
					c.startDrag(m, l)
				}
			});
			l._rendered = true
		}
	};
	var a = function (l) {
		h.call(this, l)
	};
	a.MENTOR_CLASS = h;
	a.prototype = {
		ctype : "LayoutDrag",
		withProxy : true,
		isInline : false,
		dragstart : function (l) {
			e.addClass(this.dragEl, "dragingModule");
			h.prototype.dragstart.call(this, l)
		},
		dragend : function (l) {
			e.removeClass(this.dragEl, "dragingModule");
			h.prototype.dragend.call(this, l)
		},
		adjustLayout : function (p) {
			var r = this,
			A = c.pageX,
			u = c.pageY,
			w = r.siblings,
			l = r.containers,
			t = r.isInline ? "deltaDeltaX" : "deltaDeltaY",
			v;
			if (p.type == "dragstart" || p.name == "dragstart") {
				if (r.__elAnim) {
					r.__elAnim.pause()
				}
			} else {
				if (p.type == "drag" || p.name == "drag") {
					if (l || w) {
						var B = false,
						o,
						n;
						v = e.getRect(r.dragEl);
						var q = e.getMargin(r.dragEl);
						if (A >= v.left - q[3] && A <= v.right + q[1] && u >= v.top - q[0] && u <= v.bottom + q[2]) {
							return
						}
						for (o = 0; w !== null && o < w.length; o++) {
							n = w[o];
							if (n == r.dragEl) {
								continue
							}
							v = e.getRect(n);
							q = e.getMargin(n);
							if (A >= v.left - q[3] && A <= v.right + q[1] && u >= v.top - q[0] && u <= v.bottom + q[2]) {
								if (c[t] > 0) {
									n.parentNode.insertBefore(r.dragEl, n.nextSibling)
								} else {
									if (c[t] < 0) {
										n.parentNode.insertBefore(r.dragEl, n)
									}
								}
								B = true;
								break
							}
						}
						for (o = 0; !B && l !== null && o < l.length; o++) {
							n = l[o];
							v = e.getRect(n);
							if (A > v.left + 1 && A < v.right - 1 && u > v.top + 1 && u < v.bottom - 1) {
								if (n.lastChild != r.dragEl) {
									n.appendChild(r.dragEl);
									B = true
								}
								break
							}
						}
						if (B && r.handleEl.setCapture) {
							r.handleEl.setCapture()
						}
					}
				}
			}
			if (p.type == "dragend" || p.name == "dragend") {
				if (r.needAnim && Zving.Animate) {
					v = e.getRect(r.dragEl);
					r.proxyEl.style.display = "block";
					var m = new Zving.Animate(r.proxyEl, {
							width : {
								to : v.width
							},
							height : {
								to : v.height
							},
							left : {
								to : v.left
							},
							top : {
								to : v.top
							}
						}, 300);
					m.on("end", function () {
						r.proxyEl.style.display = "none"
					});
					m.play();
					r.proxyEl.__elAnim = m
				}
			}
		},
		render : function () {
			var m = this;
			h.prototype.render.call(m);
			var l = m.adjustLayout;
			if (l) {
				m.on("dragstart", l);
				m.on("drag", l);
				m.on("dragend", l)
			}
		}
	};
	k.extraIf(a.prototype, h.prototype);
	$(window).on("unload", function () {
		c.dragObject = null;
		c.mouseDownTarget = null;
		c.startWindow = null;
		k.currentDragDropManager = null
	});
	k.DragDropManager = c;
	k.SimpleDrag = h;
	k.SimpleResize = f;
	k.LayoutDrag = a;
	k.RectSelector = j
})();
(function () {
	var z = window.Zving,
	isIE = z.isIE,
	getEvent = z.getEvent,
	Misc = z.Misc,
	DragManager = {};
	DragManager.DragFlag = false;
	DragManager.DragSource = null;
	DragManager.DragProxy = null;
	DragManager.dragObjects = {};
	DragManager.onMouseDown = function (evt, ele, opts) {
		evt = getEvent(evt);
		ele = z.getDom(ele);
		if (!evt.srcElement || !ele) {
			return
		}
		var dragFlag = z.Node.getParentByAttr(evt.srcElement, "drag", "false");
		if (dragFlag) {
			return
		}
		var dragObj;
		dragObj = DragManager.dragObjects[z.id(ele)];
		if (!dragObj) {
			var options = z.extraIf({
					dragEl : ele
				}, opts);
			dragObj = DragManager.dragObjects[z.id(ele)] = new z.SimpleDrag(options);
			DragManager.DragSource = ele;
			dragObj.on("dragstart", DragManager.onDragStart, ele);
			dragObj.on("drag", DragManager.onDrag, ele);
			dragObj.on("dragend", DragManager.onDragEnd, ele);
			z.DragDropManager.startDrag(evt, dragObj)
		}
		DragManager.DragSource = ele
	};
	DragManager.onMouseOver = function (evt, ele) {
		if (DragManager.DragFlag) {
			var dragOver = ele.getAttribute("dragover");
			if (dragOver) {
				var func = eval(dragOver);
				func.apply(ele, arguments)
			}
		}
	};
	DragManager.onMouseOut = function (evt, ele) {
		if (DragManager.DragFlag) {
			var dragOut = ele.getAttribute("dragout");
			if (dragOut) {
				var func = eval(dragOut);
				func.apply(ele, arguments)
			}
		}
	};
	DragManager.onMouseUp = function (evt, ele) {
		evt = getEvent(evt);
		var dragFlag = z.Node.getParentByAttr(evt.srcElement, "drag", "false");
		if (dragFlag) {
			return
		}
		if (DragManager.DragFlag) {
			DragManager.onMouseOut.apply(ele, arguments);
			var dragEnd = ele.getAttribute("dragend");
			if (dragEnd) {
				var func = eval(dragEnd);
				func.apply(ele, arguments)
			}
			if (DragManager.DragProxy && DragManager.DragSource != DragManager.DragProxy) {
				DragManager.DragProxy.innerHTML = ""
			}
		}
		Misc.unlockSelect();
		DragManager.DragProxy = null;
		DragManager.DragFlag = false;
		DragManager.DragSource = null
	};
	DragManager.onDragStart = function (evt) {
		var func,
		pos = null;
		evt = getEvent(evt);
		DragManager.DragFlag = true;
		var dragStart = DragManager.DragSource.getAttribute("dragstart");
		if (dragStart) {
			func = eval(dragStart);
			func.apply(DragManager.DragSource, arguments)
		}
	};
	DragManager.onDrag = function (evt) {
		var func,
		pos = null;
		evt = getEvent(evt);
		if (DragManager.DragFlag) {
			if (DragManager.DragProxy) {
				if (!evt.srcElement) {
					return
				}
			}
			var draging = DragManager.DragSource.getAttribute("draging");
			if (draging) {
				func = eval(draging);
				func.apply(DragManager.DragSource, arguments)
			}
		} else {
			var pw = z.rootWin;
			if (pw.Zving && pw.Zving.DragManager && pw.Zving.DragManager.DragFlag) {
				pw.Zving.DragManager.onDrag(evt)
			}
		}
	};
	DragManager.onDragEnd = function () {
		var func,
		pw = z.rootWin;
		DragManager.DragFlag = false;
		pw.Zving.DragManager.DragFlag = false;
		if (!DragManager.DragSource) {
			return
		}
		var dragExit = DragManager.DragSource.getAttribute("dragexit");
		if (dragExit) {
			func = eval(dragExit);
			func.apply(DragManager.DragSource, arguments)
		}
		var dragOut = DragManager.DragSource.getAttribute("dragout");
		if (dragOut) {
			func = eval(dragOut);
			func.apply(DragManager.DragSource, arguments)
		}
		if (DragManager.DragProxy && DragManager.DragSource != DragManager.DragProxy) {
			DragManager.DragProxy.innerHTML = ""
		}
		Misc.unlockSelect();
		DragManager.DragProxy = null;
		DragManager.DragFlag = false;
		DragManager.DragSource = null
	};
	z.DragManager = DragManager
})();
(function () {
	var b = Zving.CONTEXTPATH;
	var a = {};
	a.Spliter = "_ZVING_SPLITER_";
	a.get = function (f) {
		var h = document.cookie.split("; ");
		for (i = 0; i < h.length; i++) {
			var c = h[i].split("=");
			var j = c[0].trim();
			var e = c[1] ? c[1].trim() : "";
			if (j === f) {
				return decodeURIComponent(e)
			}
		}
		return null
	};
	a.getAll = function () {
		var k = document.cookie.split("; ");
		var l = [];
		for (i = 0; i < k.length; i++) {
			var e = k[i].split("=");
			var o = e[0].trim();
			var f = e[1] ? e[1].trim() : "";
			if (o.indexOf(a.Spliter) >= 0) {
				continue
			}
			if (f.indexOf("^" + a.Spliter) === 0) {
				var c = f.substring(a.Spliter.length + 1, f.indexOf("$"));
				var h,
				m = [f];
				for (h = 1; h < c; h++) {
					m.push(a.get(o + a.Spliter + h))
				}
				f = m.join("");
				f = f.substring(f.indexOf("$") + 1)
			}
			l.push([o, decodeURIComponent(f)])
		}
		return l
	};
	a.set = function (e, o, f, p, j, c, n) {
		if (e === undefined || o === undefined) {
			return false
		}
		if (!n) {
			o = encodeURIComponent(o)
		}
		if (!p) {
			p = b
		}
		p = p.replace(/^\w+:\/\/[.\w]+:?\d*/g, "");
		if (f) {
			if (/^[0-9]+$/.test(f)) {
				f = new Date(new Date().getTime() + f * 1000).toGMTString()
			} else {
				var h = Zving.DateTime.parseDate(f);
				if (h) {
					f = h.toGMTString()
				} else {
					f = undefined
				}
			}
		}
		if (!n) {
			a.remove(e, p, j)
		}
		var l = e + "=" + o + ";" + ((f) ? " expires=" + f + ";" : "") + ((p) ? "path=" + p + ";" : "") + ((j) ? "domain=" + j + ";" : "") + ((c && c !== 0) ? "secure" : "");
		if (l.length < 4096) {
			document.cookie = l
		} else {
			var k,
			m = Math.ceil(o.length / 3800);
			for (k = 0; k < m; k++) {
				if (k === 0) {
					a.set(e, "^" + a.Spliter + "|" + m + "$" + o.substr(0, 3800), f, p, j, c, true)
				} else {
					a.set(e + a.Spliter + k, o.substr(k * 3800, 3800), f, p, j, c, true)
				}
			}
		}
		return true
	};
	a.remove = function (f, j, h) {
		var e = a.get(f);
		if (!f || e === null) {
			return false
		}
		if (!j) {
			j = b
		}
		j = j.replace(/^\w+:\/\/[.\w]+:?\d*/g, "");
		if (encodeURIComponent(e).length > 3800) {
			var c = Math.ceil(encodeURIComponent(e).length / 3800);
			for (i = 1; i < c; i++) {
				document.cookie = f + a.Spliter + i + "=;" + ((j) ? "path=" + j + ";" : "") + ((h) ? "domain=" + h + ";" : "") + "expires=" + new Date(0) + ";"
			}
		}
		document.cookie = f + "=;" + ((j) ? " path=" + j + ";" : "") + ((h) ? " domain=" + h + ";" : "") + " expires=" + new Date(0) + ";";
		return true
	};
	Zving.Cookie = a
})();
(function () {
	var h = window.Zving,
	l = {};
	var e;
	var j = {
		SUCCESS : 0,
		FAILURE : 1,
		OVERFLOW : 2
	};
	function k(n) {
		return n.replace(/[_\s]/g, function (o) {
			return o == "_" ? "__" : "_s"
		})
	}
	function b() {
		return document.getElementById("z-storage")
	}
	function a() {
		var n;
		if (window.localStorage && window.localStorage.setItem && window.localStorage.getItem) {
			n = f()
		} else {
			if (window.ActiveXObject && h.ieVersion < 9) {
				n = m()
			} else {
				n = c()
			}
		}
		return n
	}
	function m() {
		var n = document.createElement("div");
		n.id = "z-storage";
		n.style.display = "none";
		document.body.appendChild(n);
		n.addBehavior("#default#userData");
		return {
			set : function (q, t, p) {
				var o = j.SUCCESS,
				r = b(),
				w = k(q),
				v = p ? p : new Date().getTime() + 365 * 24 * 60 * 60 * 1000;
				if (h.isDate(v)) {
					v = v.getTime()
				} else {
					v = new Date().getTime() + (v - 0)
				}
				r.expires = new Date(v).toUTCString();
				try {
					r.setAttribute(w, t);
					r.save(w)
				} catch (u) {
					o = j.OVERFLOW
				}
				r = null;
				return o
			},
			get : function (p) {
				var o = j.SUCCESS,
				q = b(),
				u = k(p),
				t = null;
				try {
					q.load(u);
					t = q.getAttribute(u)
				} catch (r) {
					o = j.FAILURE;
					throw "Zving.Storage.get error!"
				}
				return t
			},
			del : function (p) {
				var o = j.SUCCESS,
				q = b(),
				u = k(p),
				t;
				try {
					q.load(u);
					t = q.getAttribute(u);
					if (t) {
						q.removeAttribute(u);
						q.expires = new Date(315532799000).toUTCString();
						q.save(u)
					} else {
						o = j.FAILURE
					}
				} catch (r) {
					o = j.FAILURE
				}
				return o
			}
		}
	}
	function f() {
		return {
			set : function (p, q, o) {
				var n = j.SUCCESS,
				v = window.localStorage,
				u = k(p),
				t = o || 0;
				if (h.isDate(t)) {
					t = t.getTime()
				} else {
					if (t > 0) {
						t = new Date().getTime() + (t - 0)
					}
				}
				try {
					v.setItem(u, t + "|" + q)
				} catch (r) {
					n = j.OVERFLOW
				}
				return n
			},
			get : function (p) {
				var n = j.SUCCESS,
				v = window.localStorage,
				u = k(p),
				t = null,
				o,
				r;
				try {
					t = v.getItem(u)
				} catch (q) {
					n = j.FAILURE
				}
				if (t) {
					o = t.indexOf("|");
					r = parseInt(t.substring(0, o), 10);
					if (new Date(r).getTime() > new Date().getTime() || r === 0) {
						t = t.substring(o + 1, t.length)
					} else {
						t = null;
						n = j.FAILURE;
						this.del(p)
					}
				} else {
					n = j.FAILURE
				}
				return t
			},
			del : function (o) {
				var n = j.SUCCESS,
				t = window.localStorage,
				r = k(o),
				q = null;
				try {
					q = t.getItem(r)
				} catch (p) {
					n = j.FAILURE
				}
				if (q) {
					q = q.substring(q.indexOf("|") + 1, q.length);
					n = j[q ? "SUCCESS" : "FAILURE"];
					if (q) {
						t.removeItem(r)
					}
				} else {
					n = j.FAILURE
				}
				return n
			}
		}
	}
	function c() {
		return {
			set : function (o, p, n) {
				h.Cookie.set(k(o), p, n);
				return j.SUCCESS
			},
			get : function (n) {
				var o = h.Cookie.get(k(n));
				return o
			},
			del : function (n) {
				var p = k(n),
				o = h.Cookie.get(p);
				h.Cookie.remove(p);
				return j[o ? "SUCCESS" : "FAILURE"]
			}
		}
	}
	l.set = function (o, p, n) {
		if (!e) {
			e = a()
		}
		return e.set.apply(e, arguments)
	};
	l.get = function (n) {
		if (!e) {
			e = a()
		}
		return e.get.apply(e, arguments)
	};
	l.remove = function (n) {
		if (!e) {
			e = a()
		}
		return e.del.apply(e, arguments)
	};
	h.Storage = l
})();
(function () {
	var b = window.Zving,
	a = b.Storage;
	b.Statable = function (e) {
		var c = e,
		f;
		if (b.isElement(e)) {
			f = b.id(e);
			$(window).on("unload", function () {
				e.stateId = e._states = e._allStates = e._eventsHandler = e.addState = e.addStates = e.getState = e.setState = e.removeState = null
			})
		} else {
			f = c.id
		}
		c.stateId = c.stateId || b.pageId + "-" + f;
		c._states = c._states || {};
		c._allStates = ["hidden", "disabled"];
		c._eventsHandler = {
			hidden : {
				show : [function () {
						c.setState("hidden", c.hidden)
					}
				],
				hidden : [function () {
						c.setState("hidden", c.hidden)
					}
				]
			},
			disabled : {
				disable : [function () {
						c.setState("hidden", c.hidden)
					}
				],
				enable : [function () {
						c.setState("hidden", c.hidden)
					}
				]
			}
		};
		c.addState = function (l, h, j) {
			c._states[l] = c[l];
			c._eventsHandler[l] = c._eventsHandler[l] || {};
			var k = c._eventsHandler[l];
			if (h) {
				k[h] = k[h] || [];
				if (!j) {
					j = function () {
						c.setState(l, c[l])
					}
				}
				k[h].push(j);
				if (b.isElement(c)) {
					$(c).on(h, j)
				} else {
					c.on(h, j)
				}
			}
		};
		c.addStates = function () {
			var h = arguments,
			j = h.length;
			while (j--) {
				c._states[h[j]] = c[h[j]];
				c._eventsHandler[h[j]] = {}

			}
		};
		c.getState = function (j) {
			var h = a.get(c.stateId);
			if (h) {
				h = b.JSON.parse(h);
				return h[j]
			}
		};
		c.setState = function (j, k) {
			var h = a.get(c.stateId);
			if (h) {
				h = b.JSON.parse(h)
			} else {
				h = {}

			}
			h[j] = k;
			return a.set(c.stateId, b.JSON.stringify(h))
		};
		c.removeState = function (j) {
			var h = a.get(c.stateId);
			if (h) {
				h = b.JSON.parse(h);
				delete h[j];
				return a.set(c.stateId, b.JSON.stringify(h))
			}
		}
	}
})();
(function () {
	var c = window.Zving,
	b = c.getDom,
	a = {};
	c.getForm = a.getForm = function (e) {
		if (!e) {
			return document.forms[0]
		} else {
			e = b(e);
			if (e && e.tagName.toLowerCase() !== "form") {
				return null
			}
			return e
		}
	};
	c.selectAllCheckbox = function (k, f) {
		var h = c.Node.getValue(k);
		var e = c.getByName(f);
		if (e) {
			for (var j = 0; j < e.length; j++) {
				e[j].checked = h
			}
		}
	};
	a.setValue = function (h, f) {
		f = c.getForm(f);
		var e;
		for (e = 0; e < f.elements.length; e++) {
			var k = b(f.elements[e]);
			if (k.getAttribute("ztype") == "select" && k.parentElement.getAttribute("ztype") == "Select") {
				k = k.parentElement
			}
			if (k.type == "checkbox" || k.type == "radio") {
				if (k.name) {
					if (typeof h.get(k.name) !== "undefined" && h.get(k.name) !== null) {
						c.setValues(k.name, h.get(k.name))
					}
					continue
				}
			}
			var j = k.name || k.id;
			if (h.get(j)) {
				c.Node.setValue(k, h.get(j))
			}
		}
	};
	a.setValuesFromJson = function (j, h) {
		j = c.getForm(j);
		var f,
		e;
		for (f = 0; f < j.elements.length; f++) {
			var l = b(j.elements[f]);
			if (l.getAttribute("ztype") == "select" && l.parentElement.getAttribute("ztype") == "Select") {
				l = l.parentElement
			}
			if (l.type == "checkbox" || l.type == "radio") {
				if (l.name) {
					if (typeof h[l.name] !== "undefined") {
						c.setValues(l.name, h[l.name])
					}
					continue
				}
			}
			var k = l.name || l.id;
			if (typeof h[k] !== "undefined") {
				c.Node.setValue(l, h[k])
			}
		}
	};
	a.getJson = function (f, e) {
		el = c.getForm(f);
		if (!el) {
			alert("查找表单元素失败!" + f);
			return
		}
		return Zving.Node.getJson(el, null, e)
	};
	a.getJsonString = function (f, e) {
		return c.JSON.stringify(a.getJson(f, null, e))
	};
	a.getData = function (r, f) {
		el = c.getForm(r);
		if (!el) {
			alert("查找表单元素失败!" + r);
			return
		}
		f = f || function (j) {
			return false
		};
		var q = {};//new DataCollection();
		var m = el.elements,
		h = m.length,
		n,
		o = function (j, l) {
			if (typeof q[j] != "undefined") {
				l = q[j] + "," + l + ""
			}
			if(j.indexOf(".")<0){
				q[j] = l+"";
			}
		};
		for (n = 0; n < h; n++) {
			el = m[n];
			var p = el.name || el.id;
			if (!el.type || !p || f(el)) {
				continue
			}
			switch (el.type) {
			case "text":
			case "textarea":
				o(p, el.value)
				break;
			case "hidden":
			case "password":
				o(p, el.value);
				break;
			case "checkbox":
			case "radio":
				if (el.name && el.checked) {
					o(p, el.value)
				}
				break;
			case "select-one":
				if (el.selectedIndex > -1) {
					o(p, el.value)
				}
				break;
			case "select-multiple":
				var e = el.options;
				for (var k = 0; k < e.length; ++k) {
					if (e[k].selected) {
						o(p, e[k].value)
					}
				}
				break;
			case "button":
				break;
			default:
				if (el.name && el.id && el.value) {
					o(p, el.value)
				}
				break
			}
		}
		return q
	};
	a.isChanged = function (n) {
		el = b(n);
		var k = el.elements,
		e = k.length,
		h = 0,
		f = 0,
		m;
		for (; h < e; ++h, f = 0) {
			el = k[h];
			switch (el.type) {
			case "text":
			case "textarea":
				if (el.defaultValue != el.value) {
					if (el.getAttribute("placeholder") && el.defaultValue == el.getAttribute("placeholder")) {
						continue
					}
					return true
				}
				break;
			case "hidden":
			case "password":
				if (el.defaultValue != el.value) {
					return true
				}
				break;
			case "radio":
			case "checkbox":
				if (el.defaultChecked != el.checked) {
					return true
				}
				break;
			case "select-one":
				f = 1;
				break;
			case "select-multiple":
				m = el.options;
				for (; f < m.length; ++f) {
					if (m[f].defaultSelected != m[f].selected) {
						return true
					}
				}
				break
			}
		}
		return false
	};
	c.Form = a
})();
function obj2string(o) {
	var r = [];
	if (typeof o == "string") {
		return "\"" + o.replace(/([\'\"\\])/g, "\\$1").replace(/(\n)/g, "\\n").replace(/(\r)/g, "\\r").replace(/(\t)/g, "\\t") + "\"";
	}
	if (typeof o == "object") {
		if (!o.sort) {
			for (var i in o) {
				r.push(i + ":" + obj2string(o[i]));
			}
			if (!!document.all && !/^\n?function\s*toString\(\)\s*\{\n?\s*\[native code\]\n?\s*\}\n?\s*$/.test(o.toString)) {
				r.push("toString:" + o.toString.toString());
			}
			r = "{" + r.join() + "}";
		} else {
			for (var i = 0; i < o.length; i++) {
				r.push(obj2string(o[i]))
			}
			r = "[" + r.join() + "]";
		}
		return r;
	}
}